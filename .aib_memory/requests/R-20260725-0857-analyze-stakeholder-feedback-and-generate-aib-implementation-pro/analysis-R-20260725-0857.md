# Analysis: R-20260725-0857

## Overview

### Background

The ATISTAT website stakeholder reviewed thirteen AI-generated improvement proposals (workdir/index-ai.html) and twenty-nine additional ideas (workdir/index-ai-marketing.html, workdir/index-ai-ux.html, workdir/index-ai-critique.html, workdir/ai-critique.html) and recorded explicit decisions in workdir/ATISTAT_idei_za_podobrenie_Versia_5, 24.07.2026 - HH.xlsx. The developer needs a precise, verifiable AIB implementation prompt that reflects only stakeholder-approved items without fabricating content or misrepresenting deferred decisions.

### Scope

The deliverable for this request is a single Markdown file: `workdir/improvements-20260725_0736.md`. That file must function as a self-contained AIB implementation request prompt covering:

- Hero sub-text update (Bulgarian + English)
- Elemag hero image caption update (3 lines, Bulgarian + English)
- Montekanal about image caption update (3 lines, Bulgarian + English)
- Removal of the trust statistics strip (`id="stats"`)
- Removal of the testimonials section (`id="testimonials"`, "КАЗВАТ ЗА НАС")
- Removal of the "Следете ни" Facebook/LinkedIn block within `id="contacts"`
- Addition of Viber contact action alongside WhatsApp
- Addition of a responsive CTA block after the Services section
- Addition of a "Selected Projects" (ИЗБРАНИ ПРОЕКТИ) button in the Experience section with the 7 submitted project image sets
- Partners section with Инженерни Системи, Корект Проект, and Адрео logos linked to their respective sites
- Timeline entry content updates per the stakeholder's xlsx (all 9 buttons and panels, including a new АТИСТАТ entry at 2024)
- Replacement of the WhatsApp floating action button (FAB) with Viber (or both — DP-05)
- Language switcher improvement
- Service comparison display
- Retention and preservation of the "ЗАЩО АТИСТАТ" and "ВЪПРОСИ И ОТГОВОРИ" sections
- Application of all changes to every synchronized localized homepage copy

The prompt must also include independent verification instructions, bilingual requirements, asset-handling rules, accessibility and performance constraints, and exact affected route families.

### Out of scope

- Testimonials / reviews section (deferred by stakeholder: "не искам да заблуждавам клиентите")
- Team presentation (deferred)
- Video content (deferred)
- Interactive map of project locations (deferred)
- Awards/certificates section (deferred)
- News/blog (deferred)
- Online inquiry/quote form (deferred)
- Detailed case studies (deferred)
- Media mentions (deferred)
- Email subscription / newsletter (deferred)
- Capacity indicator ("Приемаме проекти за...") (deferred)
- Referral program (deferred)
- Retargeting pixel installation (deferred)
- Seasonal/thematic blog content (deferred)
- Online qualification questionnaire (deferred)
- Budget transparency section (deferred / "ще го мислим")
- Targeted content for client profiles (deferred)
- Active social-media profile maintenance (deferred)
- Local SEO and Google Business Profile (conditional "при възможност" — excluded from immediate implementation scope unless DP-12 resolves otherwise)
- Implementation of actual website changes (this task only produces the prompt)
- Modification of workbook, proposal pages, or supplied source images

---

## Input Interpretation

### User Original Inputs

The developer submitted `input.md ## Input` dated 2026-07-25 containing:

- A goal statement: analyze five ATISTAT improvement-proposal pages, the stakeholder workbook, supplied project images, the current website, and product context; produce one implementation-ready AIB prompt at `workdir/improvements-20260725_0736.md` that faithfully represents every approved stakeholder requirement.
- A stakeholder value statement identifying the ATISTAT owner representative as primary stakeholder and prospective BG/EN clients as affected users.
- A precedence chain: (1) clarifications in the request, (2) stakeholder workbook decisions, (3) existing factual website content, (4) proposal-page descriptions, (5) implementation judgment limited to presentation/technical execution.
- An approved homepage changes section covering: Hero sub-text replacement with exact Bulgarian text, Elemag hero caption (3 lines), Montekanal about caption (3 lines), CTA button preservation, removal of trust statistics strip, removal of testimonials section, removal of "Следете ни" Facebook/LinkedIn block, addition of Viber (number +359 884 074 048) alongside WhatsApp, addition of a responsive CTA block after Services. The input was truncated at line 123 at "Keep the existing hero CTA buttons unchanged" — items after that line are recovered from the xlsx.
- A deliverable specification: `workdir/improvements-20260725_0736.md` must be self-contained, include goal/rationale, authoritative sources, in-scope requirements, out-of-scope list, content constraints, bilingual requirements, asset-handling rules, accessibility/responsive/performance constraints, affected routes, measurable acceptance criteria, independent verification instructions, and context.md update directives.
- A scope rule: include only items explicitly approved in the workbook or clarified in the request; list all rejected/deferred/conditional/unreviewed items as out of scope.

### AI Interpretation

The request is a **prompt-generation task**, not a website-implementation task. The output is a Markdown file (`workdir/improvements-20260725_0736.md`) designed to be fed into a subsequent AIB implementation workflow.

The source of truth for approved requirements is the stakeholder workbook (xlsx), cross-referenced against input.md clarifications (level 1) and the proposal pages. The following requirements have been confirmed as approved for immediate implementation by reconciling the xlsx decisions column against the input.md:

**Content changes:**
- Hero sub-text: replace current `<p class="at-hero__sub">` content from "Строително-инвестиционна компания с 26 години опит. Реализираме проекти с отговорност, качество и дългосрочна визия — от идея до ключ." to "Строително-инвестиционна компания, базирана на 26 години професионален опит. Реализираме проекти с отговорност, качество и дългосрочна визия — от идея до ключ." (adds "базирана на" and "професионален"). English variant: faithful translation without added claims.
- Elemag hero caption (`.at-sketch__cap` within `at-hero__art`): replace "Проект: Елемаг" with exactly three lines: "Проект Елемаг / Строителство / Опит с Инженерни Системи". English: "Project Elemag / Construction / Expertise with Engineering Systems".
- Montekanal about caption (`.at-sketch__cap` within `at-about__art`): replace "Проект: Монтеканал" with exactly three lines: "Проект Монтеканал / Строителство / Опит с Инженерни Системи". English: "Project Montekanal / Construction / Expertise with Engineering Systems".

**Structural removals** (apply to all synchronized BG/EN homepage copies):
- Remove the entire `<section class="at-stats" id="stats">` block from `index.html` (only file that currently contains it).
- Remove the entire `<section class="at-section at-testimonials" id="testimonials">` block from `index.html`.
- Remove the `<li class="at-contact">` block containing "Следете ни", the Facebook link, and the LinkedIn link from `id="contacts"` in `index.html`.

**Contact additions:**
- Add a Viber contact entry (using `viber://chat?number=359884074048` or `viber://add?number=359884074048`) in the contact list in `id="contacts"`.
- Add a responsive CTA block as a new `<section>` immediately after `<section id="services">` in all BG homepage copies.

**New interactive feature:**
- Add a "Selected Projects" (ИЗБРАНИ ПРОЕКТИ) button in the `id="opit"` Experience section. When activated it reveals a gallery of 7 project image sets supplied in `workdir/Избрани снимки за сайт - 24.07.2026/`. The reveal mechanism is subject to DP-03.

**Partners section:**
- Add a new partners section with logos for Инженерни Системи (engsys.bg), Корект Проект (correctproject.com), and Адрео (URL subject to DP-01). Placement subject to DP-09.

**Timeline updates** (per xlsx "Линия с опит - добавки и корекции" table):
- Button/Panel 1 — Корект Проект (2006): type "Компания за управление на проекти", desc "correctproject.com" → no change needed for type; desc currently shows "correctproject.com". Update `at-tlpanel__type` to "Компания за управление на проекти".
- Button/Panel 2 — ЕОС Матрикс (2007): type "Офис сграда", desc: update to include "Управление и контрол на строително-инвестиционен проект" and add line "Опит с Корект Проект".
- Button/Panel 3 — Инженерни Системи (2008): type "Строителна компания", desc "engsys.bg" → no structural change needed.
- Button/Panel 4 — Монтеканал (2011): type "Жилищна сграда", add desc "Довършителни работи / Опит с Инженерни Системи" (currently only "Довършителни работи").
- Button/Panel 5 — ЕМА (2015): type "Логистична сграда", add "Строителство / Опит с Инженерни Системи" (currently only "Строителство").
- Button/Panel 6 — Елемаг (2021): type "Жилищна сграда", add "Строителство / Опит с Инженерни Системи" (currently only "Строителство").
- **New Button/Panel 7 — АТИСТАТ (2024)**: type "Строително-инвестиционна компания". Link target and logo image subject to DP-02 and DP-13.
- Button/Panel 7→8 — ОББ Интерлийз (2025): type "Офис сграда", add "Строителство / Опит с Инженерни Системи" (currently only "Строителство").
- Button/Panel 8→9 — Британско училище в София (2026): type "Училищна сграда", add "Строителство / Опит с Инженерни Системи" (currently only "Строителство").
- All timeline changes apply to both the desktop (`at-tl-desktop`) and mobile (`at-tl-mobile`) renderings.

**Floating action button:**
- Subject to DP-05 (WhatsApp FAB → Viber FAB or both).

**Language switcher:**
- Improvement specifics subject to DP-11.

**Service comparison:**
- Design subject to DP-08.

**English homepage parity:**
- Scope of new section additions subject to DP-10.

---

## Research Results

### Workspace Pattern Scan

**Affected files identified:**

- `index.html` — primary Bulgarian homepage; contains all sections including stats, testimonials, WhatsApp FAB, and social links that are targeted for removal/change (904 lines, confirmed present via grep).
- `index-bg.html` — parallel Bulgarian explicit variant; identical to `index.html?lang=bg.html` (diff confirmed 0 lines between them); does NOT currently contain stats/testimonials/FAB/social-links sections (grep confirms 0 occurrences). Changes that add new sections to `index.html` must be applied to `index-bg.html` as well, but removals only apply to `index.html`.
- `index.html?lang=bg.html` — identical to `index-bg.html`; changes to `index-bg.html` automatically cover this file if they are kept in sync.
- `index-en.html` — English homepage; does not have stats, testimonials, FAB, or social-links sections (confirmed via grep). Additions for bilingual parity depend on DP-10.
- `index.html?lang=en.html` — differs from `index-en.html` by only 20 lines (metadata/attribute differences); should track `index-en.html`.
- `index.html?lang=bg.html` and `index.html?lang=en.html` and `index.html?p=*.html` — query-style snapshots; these need to be kept in sync with their language counterparts.
- `opit/index.html` — English experience-route snapshot reproducing the core homepage; scope inclusion for this file is subject to DP-10.
- `wp-content/themes/atistat/assets/images/` — contains `correctproject.png`, `engsys.png`, `elemag.webp`, `montekanal.webp`; no ATISTAT logo PNG exists (relevant to DP-13).
- `wp-content/uploads/2026/07/` — contains 6 existing WebP project images. New project images from `workdir/Избрани снимки за сайт - 24.07.2026/` need to be prepared for web use (DP-07).
- `workdir/Избрани снимки за сайт - 24.07.2026/` — contains 33 images across 7 project folders (Аркадия 6 PNG, Луй Айер 4 PNG, Бебелан 6 JPG, Елемаг 4 PNG, Монтеканал 4 PNG, ОББ Интерлийз 4 JPG, Пауър Пропъртис 4 PNG). Four of these projects (Аркадия, Луй Айер, Бебелан, Пауър Пропъртис) have no existing project pages on the site.

**Cross-reference issues:**
- `index.html` has inline `<style>` block (lines 35–243) that adds CSS for stats, testimonials, social links, WhatsApp FAB. When those sections are removed, the corresponding CSS rules become dead code and should also be removed or scoped.
- `index-bg.html` lacks this inline style block; it relies entirely on `main.css`. Changes adding new sections to `index.html` must include the required CSS (either inline or via `main.css`).
- The WhatsApp FAB in `index.html` is defined with `class="at-whatsapp-fab"` and `.at-whatsapp-fab` CSS rules appear in the inline style block. If the FAB is replaced or modified, both the HTML element and the CSS rule must be updated together.
- Hero sub-text `<p class="at-hero__sub">` has CSS defined in the inline style block (`main.css` does not contain `.at-hero__sub`). When `index.html` and `index-bg.html` are synchronized, the CSS for `.at-hero__sub` must either be added to `main.css` or included inline in `index-bg.html`.
- The `at-sketch__cap` and `at-sketch__caplabel` elements in `index.html` render captions as a `<span class="at-sketch__caplabel">Проект:</span> Елемаг` pattern. A 3-line caption requires restructuring this span to either a `<br>`-separated span or a flex column with three child spans.

**Relevant prior solutions:**
- The `at-tlpanel--logo` class pattern already exists for Корект Проект (2006) and Инженерни Системи (2008). The new АТИСТАТ entry should follow this exact HTML structure with `<a class="at-tlb at-tlb--logo">` and `<article class="at-tlpanel at-tlpanel--logo">`. Both desktop and mobile renderings must include the new entry.
- The existing 4-step mobile card (`at-tlcard`, `at-tlcard--logo`) pattern must also be extended for the new АТИСТАТ card in the `<ul class="at-tl-mobile">` list.

### Industry Knowledge

**Finding 1 — Viber deep-link schemes (RFC-compatible custom URI schemes):**
Viber supports two URI schemes for direct messaging actions: `viber://chat?number=<e164>` (opens a chat) and `viber://add?number=<e164>` (adds a contact). Per Viber's developer documentation and tested browser behavior, `viber://chat?number=359884074048` is the correct format for Bulgaria (+359) with the leading 0 dropped in E.164 format. WhatsApp uses `https://wa.me/<e164>` which is an HTTPS URL rather than a custom scheme, ensuring it degrades gracefully on desktop; Viber's `viber://` scheme does not open a fallback on desktop but is the standard used by Bulgarian business sites. The implementation prompt must address cross-platform behavior for Viber links.

**Finding 2 — Accessible image captions and multi-line figure text (WCAG 2.1 / HTML Living Standard):**
The HTML Living Standard recommends `<figure>` and `<figcaption>` for image captions, but the existing site uses custom `<span class="at-sketch__cap">` elements. Changing the caption structure to support three lines is best accomplished by replacing the single text node inside `at-sketch__cap` with three `<span>` elements each on a new line (rendered as a flex column or using `<br>`). Screen-reader compatibility requires the caption to remain a single `aria-label`-equivalent text node or that the parent `<a>` element carries a descriptive `aria-label`. The implementation prompt must specify that `alt` text on the image and `aria-label` on the anchor reflect the full three-line caption meaning.

**Finding 3 — Floating action button (FAB) UX patterns (Material Design / Nielsen Norman Group):**
Nielsen Norman Group research on floating action buttons indicates that a single FAB is understood immediately while multiple FABs create visual clutter. If both WhatsApp and Viber FABs are shown, they should be grouped in a single expandable FAB container (speed-dial pattern) or stacked vertically with a spacing of at least 8px. The industry best practice for mobile-first contact FABs in the Bulgarian market strongly favors Viber over WhatsApp for informal client initiation, while WhatsApp is used for international contacts. A single Viber FAB is more appropriate for ATISTAT's primary Bulgarian audience per the stakeholder's explicit decision in the xlsx.

**Finding 4 — Static-site content synchronization (no build pipeline):**
The workspace has no build system. All HTML files are independent static exports. Content synchronized across `index.html`, `index-bg.html`, `index.html?lang=bg.html`, `index-en.html`, and `index.html?lang=en.html` must be manually replicated per file. The implementation prompt must enumerate each file that requires a given change, making the AIB implement agent's work unambiguous.

**Finding 5 — WebP image optimization (Google Web Fundamentals / Lighthouse):**
Lighthouse performance guidelines recommend serving images in WebP/AVIF formats for modern browsers with a fallback `<picture>` element for older browsers. All six existing project images in `wp-content/uploads/2026/07/` are already WebP. New images supplied in PNG/JPG format should be converted to WebP before deployment. The `<img>` element naming convention follows `<project>-1-<width>x<height>.webp`. The implementation prompt should specify this conversion requirement and naming convention.

### AI Agent Critique

- **input.md file truncated at line 123**: The input.md file ends abruptly mid-sentence in the "Contact access" section. Content after "Keep the existing hero CTA buttons unchanged." is missing. Requirements for language switcher improvements, service comparison design, "Selected Projects" button mechanics, timeline specifics, and partner section details are recoverable from the xlsx but the developer's explicit clarifications on these items are absent. This is a quality concern for the request artifact itself.

- **Hero sub-text already partially updated**: The current `index.html` hero sub-text reads "Строително-инвестиционна компания с 26 години опит. Реализираме проекти с отправорност, качество и дългосрочна визия — от идея до ключ." — this is very close to the stakeholder's requested text but missing "базирана на" and "професионален". The implementation must make a surgical two-word change, not a full replacement of an unrelated string.

- **index-bg.html divergence**: `index.html` has 400 lines of difference from `index-bg.html` because `index.html` contains an inline `<style>` block (209 lines) and additional sections (stats, testimonials, why, FAQ, WhatsApp FAB) that were added as part of prior AI-proposals work but never synchronized to `index-bg.html`. The implementation will widen this divergence further unless a sync strategy is defined. The implementation prompt should address whether `index-bg.html` should be brought to parity with `index.html` during this implementation pass.

- **`opit/index.html` scope gap**: Context.md notes that `opit/index.html` is "an English experience-route snapshot that reproduces the core homepage." It is not listed explicitly in input.md's scope. Changes to the English homepage should assess whether `opit/index.html` also requires updates.

- **No ATISTAT logo PNG asset for timeline**: The existing logo-type timeline entries (Корект Проект, Инженерни Системи) use PNG files. No ATISTAT-specific PNG exists in `wp-content/themes/atistat/assets/images/`. Creating the АТИСТАТ timeline entry requires either a new logo asset or an alternative rendering approach.

- **"Адрео" partner URL unknown**: The xlsx approves a partners section with Инженерни Системи, Корект Проект, and Адрео. URLs for the first two are known (engsys.bg, correctproject.com). The Адрео URL is not provided anywhere in the inputs. The prompt cannot include this action without the URL.

- **Conflict between input.md and xlsx on WhatsApp FAB**: The xlsx (row 1) says "Replace WhatsApp button with Viber." The input.md (level 1 precedence) says "Retain the existing WhatsApp actions." These are in conflict. The input.md takes precedence per the defined source hierarchy, but the floating button's disposition (WhatsApp FAB vs Viber FAB vs both) is not resolved by either source unambiguously enough for autonomous resolution.

- **New project images have no existing routes**: Four of the seven project image sets (Аркадия, Луй Айер, Бебелан, Пауър Пропъртис) have no corresponding project pages. The Selected Projects feature requires a decision on whether to create new project pages or show images in a gallery-only overlay.

- **"Следете ни" block removal vs. WhatsApp contact entry**: The contact list in `id="contacts"` contains both the "Следете ни" `<li>` (to be removed) and the WhatsApp `<li>` (to be retained). The implementation must surgically remove only the social-links entry, not the WhatsApp entry. These are adjacent `<li>` elements and the prompt must clearly differentiate them.

- **Dead CSS after section removals**: Removing `id="stats"`, `id="testimonials"`, and the social-links block leaves orphaned CSS rules in `index.html`'s inline `<style>` block for `.at-stats`, `.at-stats__list`, `.at-stats__item`, `.at-testimonials`, `.at-social-links`, `.at-social-link`. The implementation prompt should address dead-code cleanup.

- **Viber URI scheme desktop fallback**: `viber://` URI scheme does not launch anything on desktop browsers where Viber is not installed. The implementation prompt should specify whether a fallback label or `tel:` alternative is acceptable for desktop contexts.

- **Language switcher improvement definition absent**: The xlsx approves improving the language switcher (idea #13: "По-ясен и достъпен превключвател между езиците") but neither the xlsx nor input.md describes the specific improvement. Without specifics, the implementation prompt cannot define this requirement.

### Edge Cases

- **Single-file change vs. multi-file sync**: `index.html?lang=bg.html` is byte-identical to `index-bg.html`; applying changes to both `index.html` and `index-bg.html` covers both BG variants but the query-route file may need to be re-exported or manually updated.
- **Timeline panel index re-numbering**: Adding the new АТИСТАТ entry at position 7 (2024) requires shifting the existing ОББ Интерлийз from `timeline-panel-6` to `timeline-panel-7` and Британско училище from `timeline-panel-7` to `timeline-panel-8`. This affects `id` attributes, `data-panel` values, `aria-controls` references, and both `at-tl-desktop` and `at-tl-mobile` renderings. The total timeline end-year `<strong>2026</strong>` and "26 години постижения" label remain valid as they are not tied to panel count.
- **"Selected Projects" button accessibility**: If the reveal mechanism uses a `<dialog>` or `aria-expanded` toggle, it must be keyboard operable (Enter/Space to open, Escape to close) and focus-managed per ARIA modal dialog pattern.
- **Viber link on desktop**: `viber://chat?number=359884074048` produces a "no app installed" browser error on desktop. The implementation should wrap this in a `<a href="viber://...">` with appropriate title text so users understand it's a Viber link.
- **Caption multi-line rendering on small screens**: The `.at-sketch__cap` container has limited width. A 3-line caption with "Опит с Инженерни Системи" (26 characters) may overflow or wrap unexpectedly on narrow screens. CSS `white-space: nowrap` or `font-size` reduction may be needed.
- **Browser prefetch speculation rules**: `index.html` contains `<script type="speculationrules">` for prefetch. Adding new routes for selected project pages (if DP-04 chooses option A) may require updating this block.

### Requirements Gate Evaluation

Requirements Gate: 8/8 PASS — all categories satisfied.

---

## Proposed Solution

### High-Level Concept

Generate a single Markdown file (`workdir/improvements-20260725_0736.md`) that functions as a complete, self-contained AIB implementation prompt, translated from the stakeholder's xlsx decisions and input.md clarifications into precise, unambiguous implementation instructions covering all approved homepage changes and zero deferred items. After generation, execute an independent verification pass to confirm every stakeholder requirement is represented and no unsupported content has been invented.

### Execution Steps

#### Task 1: Generate the AIB implementation prompt file

- `workdir/improvements-20260725_0736.md`: Create the file containing a complete AIB implementation prompt with the following required sections:
  - Goal and business rationale (translate from input.md goal and stakeholder value statements)
  - Authoritative sources list with precedence chain (replicate from input.md)
  - Exact in-scope requirements (enumerate all approved items from both xlsx tables, cross-referenced with input.md; include all 9 timeline entry specifications; include all caption and hero text exact strings)
  - Explicit out-of-scope list (enumerate all deferred/rejected/conditional items from xlsx)
  - Content and non-invention constraints (no fabricated metrics, reviews, names, awards, social-proof claims)
  - Bilingual requirements (every BG string has a faithful EN equivalent; no added claims in translation)
  - Asset-handling rules: Copy all 33 project images (PNG/JPG) from `workdir/Избрани снимки за сайт - 24.07.2026/` to `wp-content/uploads/2026/07/` without conversion; use `loading="lazy"` on all gallery images (DP-07: Option C resolved). ATISTAT logo: copy `workdir/atistat-logo-outlined.png` to `wp-content/themes/atistat/assets/images/atistat-logo.png`; `workdir/atistat-logo-outlined.svg` available as reference; font spec from `workdir/font.txt` (Bookman Old Style, green #538135, grey #30372C) (DP-13: resolved by user).
  - Accessibility constraints: `<dialog>` modal for Selected Projects must be keyboard operable (Enter/Space to open, Escape to close), focus-managed per ARIA modal dialog pattern; Viber links must include descriptive `title` or `aria-label`; caption `aria-label` on anchor reflects full 3-line caption meaning; WCAG 4.5:1 contrast for all new elements.
  - Responsive design constraints: 3-line caption CSS must handle narrow-screen wrapping for `.at-sketch__cap`; CTA block must use responsive grid with 4 icon-buttons and headline "Свържете се с нас" (DP-06: Option A resolved); partner logo section must be horizontally responsive.
  - Performance constraints: PNG/JPG files in uploads path (not converted to WebP per DP-07 Option C); use `loading="lazy"` for all gallery images; no new JavaScript libraries; no new third-party CDN resources.
  - Exact affected route families: enumerate all files for each change — `index.html` (primary BG), `index-bg.html` (parallel BG), `index.html?lang=bg.html` (query BG), `index-en.html` (English, full parity per DP-10 Option A), `index.html?lang=en.html` (query EN); specify whether each file receives additions, removals, or both.
  - Selected Projects: implemented as in-page `<dialog>` modal overlay with photo gallery of all 7 project sets (DP-03: Option A resolved); all 7 displayed as gallery-only — no dedicated project pages for new projects (DP-04: Option B resolved).
  - Partners section: insert between `id="services"` and `id="why"` (DP-09: Option A resolved); include Инженерни Системи (engsys.bg), Корект Проект (correctproject.com), and Адрео (https://www.adreo.bg/) (DP-01: Option A resolved).
  - АТИСТАТ timeline entry (2024): link to `/` (DP-02: Option A resolved); use `<img src="wp-content/themes/atistat/assets/images/atistat-logo.png">` following the existing `at-tlb--logo` pattern.
  - FAB: replace `at-whatsapp-fab` entirely with Viber FAB using `viber://chat?number=359884074048` (DP-05: Option A resolved).
  - CTA block: add visually prominent `<section>` after `id="services"` with headline "Свържете се с нас", brief sub-line, and 4 icon-buttons for phone, email, WhatsApp, Viber (DP-06: Option A resolved).
  - Service comparison: add differentiator bullet points within each existing service card (`at-svc`) describing what distinguishes that service (DP-08: Option B resolved).
  - English parity: full parity — add all new sections (CTA block, partners, Selected Projects, АТИСТАТ timeline entry, all timeline panel updates) to `index-en.html` and `index.html?lang=en.html` with faithful English translations (DP-10: Option A resolved).
  - Language switcher: increase button size and add active-language highlight (background/border on current language) in both BG and EN variants (DP-11: Option A resolved).
  - SEO meta tags: include as required implementation item — add meta description, Open Graph, Twitter Card, and organization structured data to `<head>` of `index.html`, `index-bg.html`, `index-en.html`, and their query-route variants (DP-12: Option A resolved).
  - Measurable acceptance criteria (one criterion per requirement, specifying file, element, and verifiable state).
  - Independent verification instructions (cross-check step listing each xlsx row against the prompt, confirming presence and accuracy).
  - Context.md update directives (exact `edit-context.py` invocations for all post-implementation context changes).

#### Task 2: Independent verification of the prompt

- `workdir/improvements-20260725_0736.md`: Re-read the generated prompt in a separate execution context; verify each xlsx-approved row is present in the in-scope requirements list; verify each xlsx-deferred/rejected row appears in the out-of-scope list; verify no content from proposal pages that was not approved by the stakeholder appears in the in-scope list; output a verification summary confirming pass/fail per item.

#### Task 3: Update context.md to reflect the analyzed changes

- `.aib_memory/context.md`: Run `edit-context.py` invocations (see Context Update Analysis) to register the solution facts, updated concepts, and new issues identified during this analysis.

---

## Context Update Analysis

### Context Elements to Add

- Solution fact: A selected-projects gallery feature will be added to the Experience section of the Bulgarian homepage.
- Solution fact: A partners section will be added listing Инженерни Системи, Корект Проект, and Адрео with outbound links.
- Solution fact: The floating contact button will be updated from WhatsApp to Viber (or both, pending DP-05).
- Solution fact: A responsive CTA block will be added after the Services section.
- Solution fact: The homepage trust-statistics strip and testimonials section will be removed.
- Concept: The homepage timeline will include an АТИСТАТ entry at 2024 in addition to the existing eight entries.

### Context Elements to Modify/Remove

- `## Concepts` entry: "Homepage trust model uses experience duration, project count, awards, group-company count, testimonials, service process steps, and FAQ responses." — must be updated to remove "awards, group-company count, testimonials" after implementation removes the stats strip and testimonials section.
- `## Concepts` entry: "Direct-contact model offers phone, email, office map, WhatsApp, Facebook, and LinkedIn paths without an on-site submission form." — must be updated to replace WhatsApp-only with WhatsApp+Viber and remove Facebook/LinkedIn (following "Следете ни" removal).
- `## Solution` entry: "Primary Bulgarian homepage extends the shared theme with document-local CSS for hero actions, stats, testimonials, differentiation cards, FAQ, social links, and a floating WhatsApp action." — must be updated to remove "stats, testimonials, social links" and replace "floating WhatsApp action" with "floating Viber action" (or "floating Viber/WhatsApp action" pending DP-05).

### Conflict Resolution and Intent Preservation

- **Trust model concept**: The original intent of documenting the trust model is to capture what evidence-of-quality elements the homepage presents. After implementation, the trust model will rely on project portfolio evidence, "ЗАЩО АТИСТАТ" differentiation, and FAQ — not on unsupported numeric claims. The updated concept statement should reflect this shifted model while preserving the documentation intent.
- **Direct-contact concept**: The original intent is to document all outbound contact paths. After implementation, Facebook/LinkedIn paths are removed from the contacts section but the outbound Facebook/LinkedIn footer links (if any exist) remain in scope. The updated concept should reflect the removal of the social block from the contacts section specifically.
- **Solution entry for BG homepage**: The original intent captures the CSS and JS scope of the primary homepage. The update preserves the accurate description of the page's technical composition after section removals.

---

## Decision Register

### Decision Points

#### Decision Point: DP-01 — Адрео website URL

- Tag: resolved-by-user
- Resolution: Option A — the developer provided the URL: **https://www.adreo.bg/**. The Адрео partners section entry will use `href="https://www.adreo.bg/"` with `target="_blank" rel="noopener"`.
- Rationale/Resolution: URL provided directly in Q001 answer.

**Chosen: Option A** — Provide the exact URL https://www.adreo.bg/ for inclusion in the partners section.
- Trade-offs: Prompt can be complete and unambiguous; no placeholder needed.
- Impact: `href="https://www.adreo.bg/"` in the partners section HTML `<a>` element.

---

#### Decision Point: DP-02 — АТИСТАТ timeline button link target

- Tag: resolved-by-user
- Resolution: Option A — Link to the site root `/` (internal self-reference). The `<a>` element uses `href="/"` without `target="_blank"`.
- Rationale/Resolution: Developer selected Option A in Q002.

**Chosen: Option A** — Link to the site root `/` (internal self-reference).
- Trade-offs: Semantically clear as a self-link; stays on site.
- Impact: `href="/"` on the anchor element; non-outbound.

---

#### Decision Point: DP-03 — Selected Projects (ИЗБРАНИ ПРОЕКТИ) reveal mechanism

- Tag: resolved-by-user
- Resolution: Option A — In-page `<dialog>` modal overlay with a photo gallery of all 7 project sets. Must be keyboard operable (Enter/Space to open, Escape to close) with ARIA focus management.
- Rationale/Resolution: Developer selected Option A in Q003.

**Chosen: Option A** — In-page modal/dialog (`<dialog>`) overlay with a photo gallery of the 7 project sets.
- Trade-offs: Keyboard-accessible with native `<dialog>` semantics; no new routes needed; JavaScript required for open/close. Must manage focus trap and Escape to close per ARIA.
- Impact: New `<dialog>` element + open/close JavaScript function in main.js.

---

#### Decision Point: DP-04 — New project entries for gallery (Аркадия, Луй Айер, Бебелан, Пауър Пропъртис)

- Tag: resolved-by-user
- Resolution: Option B — All 7 projects appear as gallery-only entries; no dedicated project pages are created for the four new projects.
- Rationale/Resolution: Developer selected Option B in Q004.

**Chosen: Option B** — Include all 7 projects as gallery-only entries in the Selected Projects feature — no dedicated project pages.
- Trade-offs: Lower implementation scope; no new routes; existing project pages remain unchanged.
- Impact: Gallery displays project name + images only; no "Read more" link for new projects.

---

#### Decision Point: DP-05 — Floating action button: Viber vs WhatsApp vs both

- Tag: resolved-by-user
- Resolution: Option A — Replace the WhatsApp FAB entirely with a Viber FAB using `viber://chat?number=359884074048`. The `at-whatsapp-fab` element and its CSS rules are replaced with `at-viber-fab`.
- Rationale/Resolution: Developer selected Option A in Q005.

**Chosen: Option A** — Replace the WhatsApp FAB entirely with a Viber FAB.
- Trade-offs: Follows xlsx row 1 and row 17; removes WhatsApp from the FAB position; WhatsApp remains available in the inline contact list.
- Impact: Replace `at-whatsapp-fab` with `at-viber-fab`; update CSS class and SVG icon.

---

#### Decision Point: DP-06 — Responsive CTA block after Services: visual design

- Tag: resolved-by-user
- Resolution: Option A — Visually prominent full-width section with a short headline ("Свържете се с нас"), a brief sub-line (phone + email text), and four CTA icon-buttons for phone, email, WhatsApp, Viber.
- Rationale/Resolution: Developer selected Option A in Q006.

**Chosen: Option A** — Visually prominent full-width section with headline, sub-line, and 4 CTA icon-buttons.
- Trade-offs: High visibility; consistent with site's existing section pattern; requires more CSS.
- Impact: New `<section>` with heading, sub-text, and 4 link buttons.

---

#### Decision Point: DP-07 — New project image asset handling

- Tag: resolved-by-user
- Resolution: Option C — Copy the original PNG/JPG files from `workdir/Избрани снимки за сайт - 24.07.2026/` to `wp-content/uploads/2026/07/` without format conversion; apply `loading="lazy"` on all gallery images for performance mitigation.
- Rationale/Resolution: Developer selected Option C in Q007.

**Chosen: Option C** — Copy PNG/JPG to `wp-content/uploads/2026/07/` without conversion; use `loading="lazy"`.
- Trade-offs: Simple copy; no tooling required; sub-optimal performance vs WebP but acceptable for this pass.
- Impact: PNG/JPG files in uploads path; `loading="lazy"` applied to all gallery `<img>` elements.

---

#### Decision Point: DP-08 — Service comparison display

- Tag: resolved-by-user
- Resolution: Option B — Add brief differentiator bullet points within each existing service card (`at-svc`) describing what distinguishes that service from the others.
- Rationale/Resolution: Developer selected Option B in Q008.

**Chosen: Option B** — Add differentiator bullet points within each existing service card.
- Trade-offs: Minimal structural change; integrates into existing layout; less formal comparison.
- Impact: Additional `<ul>` or `<p>` per service card with service-distinguishing characteristics.

---

#### Decision Point: DP-09 — Partners section placement in page flow

- Tag: resolved-by-user
- Resolution: Option A — Insert the new partners section between `id="services"` and `id="why"`. The CTA block (DP-06) is placed immediately after `id="services"` and the partners section follows it, both appearing before `id="why"`.
- Rationale/Resolution: Developer selected Option A in Q009.

**Chosen: Option A** — Insert a new partners section between `id="services"` and `id="why"`.
- Trade-offs: Partners appear after services; commercially relevant grouping; CTA block (DP-06) is also placed after Services — ordering is CTA block first, then partners section, then `id="why"`.
- Impact: New section inserted after the CTA block in `index.html`; approximately at line ~692 in the current file.

---

#### Decision Point: DP-10 — English homepage new section parity

- Tag: resolved-by-user
- Resolution: Option A — Full parity: add all new sections (CTA block, partners section, Selected Projects button + modal, АТИСТАТ timeline entry at 2024, all timeline panel text updates) to `index-en.html` and `index.html?lang=en.html` with faithful English translations. `opit/index.html` (English experience-route snapshot) is also assessed for update.
- Rationale/Resolution: Developer selected Option A in Q010.

**Chosen: Option A** — Full parity — add all new sections to English variants with faithful translations.
- Trade-offs: Consistent user experience across languages; higher implementation scope.
- Impact: All BG additions replicated in English in `index-en.html` and `index.html?lang=en.html`; faithful translations required for all Bulgarian section headings and content.

---

#### Decision Point: DP-11 — Language switcher improvement specifics

- Tag: resolved-by-user
- Resolution: Option A — Increase the visual size of the BG/EN toggle buttons and add a border or background highlight on the active language button. This applies to both BG and EN homepage variants.
- Rationale/Resolution: Developer selected Option A in Q011.

**Chosen: Option A** — Increase button size and add active-language highlight (background/border on current language).
- Trade-offs: Immediately more visible; minimal structural change; no new content.
- Impact: CSS change to `at-lang` element and its children; `aria-current="true"` or active class applied to the current-language button.

---

#### Decision Point: DP-12 — SEO meta tags: include or exclude

- Tag: resolved-by-user
- Resolution: Option A — Include SEO meta tag implementation (meta description, Open Graph, Twitter Card, and organization structured data) as a required item in the implementation prompt.
- Rationale/Resolution: Developer selected Option A in Q012.

**Chosen: Option A** — Include as a required implementation item.
- Trade-offs: Resolves a documented known gap; directly actionable; valuable for discoverability.
- Impact: `<head>` content additions in `index.html`, `index-bg.html`, `index-en.html`, and their query-route variants (`index.html?lang=bg.html`, `index.html?lang=en.html`).

---

#### Decision Point: DP-13 — АТИСТАТ logo asset for timeline entry

- Tag: resolved-by-user
- Resolution: Use `workdir/atistat-logo-outlined.png` (copy to `wp-content/themes/atistat/assets/images/atistat-logo.png`) as the `<img>` source in the `at-tlb--logo` pattern. The SVG at `workdir/atistat-logo-outlined.svg` is available as reference. Font specification from `workdir/font.txt`: Bookman Old Style, green #538135, grey #30372C.
- Rationale/Resolution: Developer provided explicit asset files in Q013 answer: "Use `workdir/atistat-logo-outlined.png`, `workdir/atistat-logo-outlined.svg` and font descriptions in `workdir/font.txt`."

**Chosen: Use workdir/atistat-logo-outlined.png** — Copy to `wp-content/themes/atistat/assets/images/atistat-logo.png` and reference via `<img>` in the timeline button, consistent with existing `at-tlb--logo` pattern.
- Trade-offs: Consistent with existing logo-entry pattern; requires a file copy step; PNG file (59KB) is already optimized and available.
- Impact: `atistat-logo.png` at `wp-content/themes/atistat/assets/images/`; `<img src="wp-content/themes/atistat/assets/images/atistat-logo.png" alt="ATISTAT">` in timeline button and mobile card.
