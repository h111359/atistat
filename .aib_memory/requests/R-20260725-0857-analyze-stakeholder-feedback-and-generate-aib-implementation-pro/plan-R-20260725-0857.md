# Plan: R-20260725-0857

## Goal

Generate a single, self-contained Markdown file at `workdir/improvements-20260725_0736.md` that functions as a complete AIB implementation prompt. The prompt must faithfully represent all stakeholder-approved homepage changes from the ATISTAT stakeholder workbook (ATISTAT_idei_za_podobrenie_Versia_5, 24.07.2026 - HH.xlsx), cross-referenced with the developer's input.md clarifications, and translated into precise, unambiguous implementation instructions that a subsequent AIB implement agent can execute without additional context. After generation, perform an independent verification pass to confirm every approved requirement is represented and no unsupported content has been introduced.

**Background:** The ATISTAT website stakeholder reviewed 13 AI-generated improvement proposals and 29 additional ideas, then recorded explicit decisions in the workbook. The developer submitted the workbook decisions and five proposal pages as the source of truth and requested a verifiable prompt as the sole deliverable of this AIB request. The prompt must include all approved structural, content, feature, accessibility, performance, and bilingual requirements.

**Impacted components:**
- `workdir/improvements-20260725_0736.md` — new file created by this request (primary deliverable)
- `.aib_memory/context.md` — updated with solution facts, modified concepts, and new issues

---

## Constraints

- The deliverable is a Markdown prompt file only; no website HTML, CSS, JS, or image files are created by this request.
- All in-scope requirements must trace to a specific xlsx row or explicit input.md clarification. Content not approved in the xlsx must not appear in the in-scope list.
- The prompt must specify exact file paths for every change it mandates; the implement agent must not need to discover affected files independently.
- All 13 Decision Points are resolved-by-user (no outstanding ask DPs); the prompt must reflect resolved choices: Адрео URL = https://www.adreo.bg/, ATISTAT timeline link = `/`, Selected Projects = dialog modal, new projects = gallery-only, FAB = Viber only, CTA = prominent section with headline, images = copy PNG/JPG to uploads, service comparison = card bullets, partners placement = between services and why, English parity = full, language switcher = size + active highlight, SEO = required, ATISTAT logo = workdir/atistat-logo-outlined.png copied to wp-content/themes/atistat/assets/images/atistat-logo.png.
- The prompt must enumerate the exact file set affected by each change: `index.html` (primary BG), `index-bg.html` (parallel BG), `index.html?lang=bg.html` (query BG), `index-en.html` (English full parity), `index.html?lang=en.html` (query EN).
- No new JavaScript libraries may be required by any instruction in the prompt.
- Content constraints: no fabricated metrics, testimonials, reviews, names, awards, social-proof claims beyond those already approved.
- The prompt's bilingual requirements must specify that every Bulgarian string has a faithful English translation without added claims.

---

## Success criteria

- `workdir/improvements-20260725_0736.md` exists and is non-empty.
- The file contains the following top-level sections: Goal and business rationale; Authoritative sources with precedence chain; In-scope requirements; Out-of-scope list; Content and non-invention constraints; Bilingual requirements; Asset-handling rules; Accessibility constraints; Responsive design constraints; Performance constraints; Affected route families; Acceptance criteria; Independent verification instructions; Context.md update directives.
- Every xlsx-approved row (all stakeholder "Да" decisions) appears in the in-scope requirements section, traceable by row reference or description.
- Every xlsx-deferred, xlsx-rejected, and xlsx-conditional row appears in the out-of-scope list.
- No content from the proposal pages (workdir/index-ai.html, index-ai-marketing.html, index-ai-ux.html, index-ai-critique.html, ai-critique.html) appears in-scope unless explicitly approved by the xlsx.
- The independent verification pass in Task 2 produces a pass result for every xlsx row checked.
- `context.md` is updated: three Concepts statements modified, one Concepts statement modified for timeline entry count, three Solution statements added, three Issues entries added.

---

## Plan

### Task 1: Generate the AIB implementation prompt file

#### Intent
Create `workdir/improvements-20260725_0736.md` as a complete, self-contained AIB implementation prompt covering all stakeholder-approved homepage changes.

#### Procedure
Open `workdir/improvements-20260725_0736.md` for creation (file must not exist prior to this task).

Write the **Goal and business rationale** section: state that the goal is to implement all ATISTAT stakeholder-approved homepage improvements recorded in the workbook. Include the stakeholder value statement identifying the ATISTAT owner representative as primary stakeholder and prospective BG/EN clients as affected users.

Write the **Authoritative sources and precedence chain** section, listing sources in decreasing precedence: (1) clarifications in this prompt, (2) stakeholder workbook decisions (xlsx), (3) existing factual website content, (4) proposal-page descriptions, (5) implementation judgment limited to presentation and technical execution.

Write the **In-scope requirements** section with the following subsections:

*Content changes (apply to all synchronized BG/EN homepage copies unless noted):*
- Hero sub-text in `index.html`: change `<p class="at-hero__sub">` from "Строително-инвестиционна компания с 26 години опит. Реализираме проекти с отговорност, качество и дългосрочна визия — от идея до ключ." to "Строително-инвестиционна компания, базирана на 26 години професионален опит. Реализираме проекти с отговорност, качество и дългосрочна визия — от идея до ключ." (adds "базирана на" and "професионален"). English homepage variant: faithful translation without added claims.
- Elemag hero image caption in `index.html`: change the `.at-sketch__cap` element within `at-hero__art` from the current "Проект: Елемаг" single-text pattern to a three-line caption with exact text: line 1 "Проект Елемаг", line 2 "Строителство", line 3 "Опит с Инженерни Системи". The caption element must be restructured to hold three child `<span>` elements or `<br>`-separated spans. The parent `<a>` element or `<img>` must carry an `aria-label` reflecting the full caption meaning. English: "Project Elemag / Construction / Expertise with Engineering Systems".
- Montekanal about image caption in `index.html`: change the `.at-sketch__cap` element within `at-about__art` from the current "Проект: Монтеканал" single-text pattern to a three-line caption: line 1 "Проект Монтеканал", line 2 "Строителство", line 3 "Опит с Инженерни Системи". English: "Project Montekanal / Construction / Expertise with Engineering Systems".

*Structural removals (apply only to files that currently contain these sections):*
- Remove the entire `<section class="at-stats" id="stats">` block from `index.html`. Also remove all corresponding inline CSS rules for `.at-stats`, `.at-stats__list`, and `.at-stats__item` from the `<style>` block in `index.html`.
- Remove the entire `<section class="at-section at-testimonials" id="testimonials">` block from `index.html`. Also remove corresponding inline CSS rules for `.at-testimonials` and related selectors from the `<style>` block in `index.html`.
- Remove the `<li class="at-contact">` block containing "Следете ни", the Facebook `<a>` link, and the LinkedIn `<a>` link from `id="contacts"` in `index.html`. Do NOT remove the adjacent WhatsApp `<li>` entry. Also remove corresponding inline CSS rules for `.at-social-links` and `.at-social-link` from the `<style>` block in `index.html`.

*Contact additions (apply to all BG homepage copies: `index.html`, `index-bg.html`, `index.html?lang=bg.html`):*
- Add a Viber contact `<li>` entry in the contact list within `id="contacts"`, adjacent to the existing WhatsApp entry. Use URI `viber://chat?number=359884074048`. The link element must include a descriptive `title` attribute (e.g., "Свързване с нас чрез Viber") for desktop users where the Viber app may not be installed.
- Add a Viber `<li>` to the same contact list in `index-en.html` and `index.html?lang=en.html` with English title text (e.g., "Contact us via Viber").

*Responsive CTA block (apply to all BG homepage copies: `index.html`, `index-bg.html`, `index.html?lang=bg.html`; full English parity in `index-en.html`, `index.html?lang=en.html`):*
- Insert a new visually prominent `<section>` immediately after the closing `</section>` tag of `<section id="services">`. The section must include: a heading "Свържете се с нас", a brief sub-line with phone number and email, and four CTA icon-link buttons for phone (`tel:` URI), email (`mailto:` URI), WhatsApp (`https://wa.me/359884074048`), and Viber (`viber://chat?number=359884074048`). All four buttons must be keyboard-accessible and include `aria-label` attributes. CSS for this section must be added to `wp-content/themes/atistat/assets/css/main.css` (or as an inline style block that matches the existing pattern in `index.html`) to ensure it is available for `index-bg.html` which uses only `main.css`. English section heading: "Contact Us".

*Selected Projects feature (apply to all BG homepage copies; full English parity in EN copies):*
- Add an "ИЗБРАНИ ПРОЕКТИ" button within the `id="opit"` Experience section in `index.html`, `index-bg.html`, and `index.html?lang=bg.html`. English label: "SELECTED PROJECTS" in `index-en.html` and `index.html?lang=en.html`.
- Clicking the button opens a native `<dialog>` element modal that displays a photo gallery of all 7 project image sets. The dialog must be keyboard operable: Enter or Space to open, Escape to close. The dialog must implement ARIA focus management (focus moves into dialog on open; focus returns to trigger button on close). The dialog must contain a close button.
- The 7 project sets and their image source paths (after copy to uploads per asset-handling rules) are: Аркадия (6 PNG images), Луй Айер (4 PNG images), Бебелан (6 JPG images), Елемаг (4 PNG images), Монтеканал (4 PNG images), ОББ Интерлийз (4 JPG images), Пауър Пропъртис (4 PNG images). All displayed as gallery-only entries — no links to project detail pages for any of the 7 projects.
- All 33 gallery images must use `loading="lazy"`.
- The dialog open/close JavaScript must be added to `wp-content/themes/atistat/assets/js/main.js`. No new JavaScript libraries may be introduced.

*Partners section (apply to all BG homepage copies; full English parity in EN copies):*
- Insert a new partners section immediately after the CTA block (i.e., between `id="services"` and `id="why"`). The section displays three partner logos with outbound links: Инженерни Системи → `href="https://engsys.bg" target="_blank" rel="noopener"`, Корект Проект → `href="https://correctproject.com" target="_blank" rel="noopener"`, Адрео → `href="https://www.adreo.bg/" target="_blank" rel="noopener"`. Use existing partner PNG images from `wp-content/themes/atistat/assets/images/` for Инженерни Системи (`engsys.png`) and Корект Проект (`correctproject.png`). For Адрео, use a logo image if available or display the company name as styled text. The section must be responsive (horizontal row on desktop, wrapped on mobile).

*Timeline updates (apply to both `at-tl-desktop` and `at-tl-mobile` renderings in all BG homepage copies; full English parity in EN copies):*
- Button/Panel 1 — Корект Проект (2006): update `at-tlpanel__type` to "Компания за управление на проекти".
- Button/Panel 2 — ЕОС Матрикс (2007): update panel description to include "Управление и контрол на строително-инвестиционен проект" and add new line "Опит с Корект Проект".
- Button/Panel 4 — Монтеканал (2011): update panel description to "Довършителни работи / Опит с Инженерни Системи".
- Button/Panel 5 — ЕМА (2015): update panel description to "Строителство / Опит с Инженерни Системи".
- Button/Panel 6 — Елемаг (2021): update panel description to "Строителство / Опит с Инженерни Системи".
- Insert new Button/Panel at position 7 — АТИСТАТ (2024): type "Строително-инвестиционна компания"; logo image `<img src="wp-content/themes/atistat/assets/images/atistat-logo.png" alt="ATISTAT">` using `at-tlb--logo` HTML pattern; link `href="/"` (no `target="_blank"`). Both desktop `<ul class="at-tl-desktop">` and mobile `<ul class="at-tl-mobile">` renderings must include the new entry. Existing entries after position 6 must be renumbered: ОББ Интерлийз → position 8, Британско училище → position 9. Update all `id` attributes, `data-panel` values, and `aria-controls` references accordingly.
- Button/Panel 8 → ОББ Интерлийз (2025): update panel description to "Строителство / Опит с Инженерни Системи".
- Button/Panel 9 → Британско училище в София (2026): update panel description to "Строителство / Опит с Инженерни Системи".
- ATISTAT logo asset: before inserting the timeline entry, copy `workdir/atistat-logo-outlined.png` to `wp-content/themes/atistat/assets/images/atistat-logo.png`.

*Floating action button (apply to `index.html` only — the only file that currently has the FAB):*
- Replace the entire `<a class="at-whatsapp-fab" ...>` element with a Viber FAB using `href="viber://chat?number=359884074048"`, class `at-viber-fab`, and an appropriate Viber SVG icon. Update the corresponding CSS rule in the `<style>` block from `.at-whatsapp-fab` to `.at-viber-fab`. The FAB must include a descriptive `aria-label` (e.g., "Свържете се чрез Viber").

*Language switcher (apply to all BG and EN homepage copies):*
- Increase the visual size of the BG/EN toggle buttons (larger font size or padding). Add a visible background or border highlight to the active-language button. Apply `aria-current="true"` (or equivalent active CSS class) to the current-language `<a class="at-lang__opt">` element. Changes must be applied in `main.css`.

*Service comparison (apply to all BG homepage copies; full English parity in EN copies):*
- Within the `id="services"` section, add a short bullet list of differentiating characteristics inside each of the four service cards (`at-svc` elements). Bullet content must be factually accurate and traceable to ATISTAT's known service specializations; no fabricated claims. English service cards must receive English bullet text.

*SEO meta tags (apply to `index.html`, `index-bg.html`, `index.html?lang=bg.html`, `index-en.html`, `index.html?lang=en.html`):*
- Add `<meta name="description">` with factually accurate Bulgarian description for BG files, English description for EN files.
- Add Open Graph tags: `og:title`, `og:description`, `og:type` (website), `og:url`, `og:image` (use the hero WebP image already present in uploads) for each file variant with appropriate language-specific values.
- Add Twitter Card tags: `twitter:card` (summary_large_image), `twitter:title`, `twitter:description`, `twitter:image`.
- Add JSON-LD structured data block (`<script type="application/ld+json">`) with `Organization` schema including name "ATISTAT EOOD", url "https://atistat.com", and available contact/social links. Place in `<head>` above `</head>`.

Write the **Out-of-scope list** section enumerating every item the stakeholder explicitly deferred, rejected, or marked conditional (not approved): testimonials/reviews section, team presentation, video content, interactive project location map, awards/certificates section, news/blog, online inquiry/quote form, detailed case studies, media mentions, email subscription/newsletter, capacity indicator, referral program, retargeting pixel, seasonal blog content, online qualification questionnaire, budget transparency section, targeted content for client profiles, active social-media profile maintenance, Google Business Profile setup, modification of the workbook itself, modification of proposal pages or source images.

Write the **Content and non-invention constraints** section: all content added must be factually accurate; no fabricated metrics, testimonials, reviews, named individuals, awards, project names, or social-proof claims may be introduced. Timeline description text must match what is specified in this prompt exactly.

Write the **Bilingual requirements** section: every Bulgarian string introduced or modified must have a faithful English equivalent in `index-en.html` and `index.html?lang=en.html`; translations must not add claims not present in the Bulgarian source.

Write the **Asset-handling rules** section: (a) copy all 33 project images (PNG/JPG) from `workdir/Избрани снимки за сайт - 24.07.2026/` subfolders to `wp-content/uploads/2026/07/` preserving filenames; (b) copy `workdir/atistat-logo-outlined.png` to `wp-content/themes/atistat/assets/images/atistat-logo.png`; (c) no image format conversion required; (d) all gallery `<img>` elements must use `loading="lazy"`.

Write the **Accessibility constraints** section: WCAG 2.1 Level AA compliance for all new elements; `<dialog>` modal must be keyboard operable and focus-managed; Viber links must include `title` or `aria-label`; caption `aria-label` on anchors reflects full three-line meaning; color contrast ≥ 4.5:1 for all new text; `aria-current` on active language button.

Write the **Responsive design constraints** section: CTA block must stack gracefully on narrow viewports; partner logos must wrap responsibly; three-line captions must not overflow `.at-sketch__cap` container on narrow screens (add `word-break: break-word` or reduce font-size if needed); dialog modal must be usable on mobile.

Write the **Performance constraints** section: no new JavaScript libraries; no new CDN resources; all gallery images use `loading="lazy"`; the dialog element is native HTML (`<dialog>`); no inline-script event handlers.

Write the **Affected route families** section: explicitly list for each change which files are affected (add, remove, or modify).

Write the **Acceptance criteria** section: one measurable criterion per requirement, each specifying file path, element selector or attribute, and verifiable state.

Write the **Independent verification instructions** section: after implementation, re-read the generated implementation prompt and cross-check each xlsx-approved row against the in-scope requirements list; cross-check each xlsx-deferred/rejected row against the out-of-scope list; verify no proposal-page content appears in-scope without xlsx approval; output a summary table with pass/fail per row.

Write the **Context.md update directives** section: include the exact `edit-context.py` command invocations listed in Task 3 of this plan so that the implement agent can run them after implementation without reading `context.md` independently.

---

### Task 2: Independent verification of the prompt

#### Intent
Re-read `workdir/improvements-20260725_0736.md` in a fresh read and verify completeness and accuracy against the xlsx source.

#### Procedure
Read `workdir/improvements-20260725_0736.md` in full.

For each xlsx row marked approved ("Да"), confirm the corresponding requirement appears in the in-scope section. Record pass/fail per row.

For each xlsx row marked deferred, rejected, or conditional, confirm it appears in the out-of-scope list. Record pass/fail per row.

Confirm no section of the in-scope requirements references content not approved by the xlsx (e.g., no fabricated metrics, reviews, or ideas from proposal pages that lacked xlsx approval).

Confirm all 14 required top-level sections are present in the file.

Confirm all 5 affected-file route families are named (index.html, index-bg.html, index.html?lang=bg.html, index-en.html, index.html?lang=en.html).

If any item fails: revise `workdir/improvements-20260725_0736.md` to correct the failure; re-run verification until all items pass.

Output a verification summary inside `workdir/improvements-20260725_0736.md` (as a final appendix section) listing each xlsx row and its verification status.

---

### Task 3: Update context.md

#### Intent
Update `.aib_memory/context.md` to reflect the solution approach resolved during analysis and register new issues discovered in the AI Agent Critique.

#### Procedure
Run the following commands in sequence against `.aib_memory/context.md`. Each command targets the workspace root at `.` (run from the workspace root).

Delete the outdated trust model concept statement:
`python .aib_brain/tools/edit-context.py --workspace . --operation delete --area Concepts --text "Homepage trust model uses experience duration, project count, awards, group-company count, testimonials, service process steps, and FAQ responses."`

Insert the updated trust model concept statement:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Concepts --text "Homepage trust model uses experience duration, project count, project portfolio evidence, ЗАЩО АТИСТАТ differentiation, service process steps, and FAQ responses."`

Delete the outdated direct-contact concept statement:
`python .aib_brain/tools/edit-context.py --workspace . --operation delete --area Concepts --text "Direct-contact model offers phone, email, office map, WhatsApp, Facebook, and LinkedIn paths without an on-site submission form."`

Insert the updated direct-contact concept statement:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Concepts --text "Direct-contact model offers phone, email, office map, WhatsApp, Viber, and Facebook/LinkedIn footer links without an on-site submission form."`

Delete the outdated portfolio timeline concept statement:
`python .aib_brain/tools/edit-context.py --workspace . --operation delete --area Concepts --text "Portfolio timeline combines two related-company milestones with six delivered project records: EOS Matrix, Montekanal, EMA, Elemag, UBB Interlease, and British School of Sofia."`

Insert the updated portfolio timeline concept statement:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Concepts --text "Portfolio timeline combines three company milestones (Корект Проект 2006, Инженерни Системи 2008, ATISTAT 2024) with six delivered project records: EOS Matrix, Montekanal, EMA, Elemag, UBB Interlease, and British School of Sofia."`

Delete the outdated Bulgarian homepage solution statement:
`python .aib_brain/tools/edit-context.py --workspace . --operation delete --area Solution --text "Primary Bulgarian homepage extends the shared theme with document-local CSS for hero actions, stats, testimonials, differentiation cards, FAQ, social links, and a floating WhatsApp action."`

Insert the updated Bulgarian homepage solution statement:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Primary Bulgarian homepage extends the shared theme with document-local CSS for hero actions, differentiation cards, FAQ, a selected-projects gallery dialog, a CTA contact block, and a floating Viber action."`

Insert new solution statement for the selected-projects gallery:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Selected-projects gallery opens as a native dialog modal triggered from the Experience section and displays all 7 submitted project image sets as a photo gallery without dedicated project routes."`

Insert new solution statement for the partners section:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "A partners section with Инженерни Системи, Корект Проект, and Адрео logo-links appears between the Services and Why sections on all BG and EN homepage copies."`

Insert new solution statement for the ATISTAT timeline entry:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Homepage timeline includes an ATISTAT entry at 2024 using atistat-logo.png from wp-content/themes/atistat/assets/images/ linked to the site root."`

Insert new Issue for index-bg.html divergence:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Issues --text "index-bg.html diverges from index.html by approximately 400 lines because index.html carries an inline CSS block and additional sections not replicated in index-bg.html; each implementation pass widens this divergence further. Source: diff between index.html and index-bg.html."`

Insert new Issue for dead CSS after section removals:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Issues --text "Removing id=stats, id=testimonials, and the Следете ни social block leaves orphaned CSS rules in the index.html inline style block for .at-stats, .at-testimonials, and .at-social-links selectors. Source: index.html inline style block."`

Insert new Issue for Viber desktop fallback:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Issues --text "Viber viber:// URI scheme produces no action on desktop browsers where Viber is not installed; all Viber link elements must include a descriptive title or aria-label to inform desktop users. Source: Viber URI scheme behavior."`

Confirm all commands exit with code 0. If any command fails, inspect the exact text mismatch and retry with the corrected text substring.

Acceptance test: run `python .aib_brain/tools/verify-context.py --workspace .` and confirm all checks pass.
