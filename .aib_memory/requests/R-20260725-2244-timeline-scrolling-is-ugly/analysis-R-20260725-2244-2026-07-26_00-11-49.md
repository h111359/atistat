## Overview

- Request ID: R-20260725-2244
- Request title: Timeline scrolling is ugly.

### Background

The developer reports that the homepage timeline's current scrolling experience is visually unattractive and asks for a modern bilingual UX proposal. The developer also wants the Apartment Arcadia timeline image to reproduce the Correct Project milestone's interaction state: grayscale while idle and color when hovered or selected.

### Scope

The request covers the desktop timeline's navigation, overflow affordances, selection behavior, keyboard behavior, responsive presentation, and consistent Bulgarian/English rendering. It also covers the Apartment Arcadia milestone asset and visual states, the shared CSS and JavaScript copies, regression coverage, and the corresponding product-context records.

### Out of scope

This analysis does not redesign unrelated homepage sections, change project gallery content, modify project-detail routes, add third-party libraries, or implement any source changes. Exact page coverage, mobile behavior, and the final timeline interaction model remain pending the Decision Points below.

## Input Interpretation

### User Original Inputs

- "Timeline scrolling is ugly. Make a modern UX design solution for timelines (same for BG and EN)."
- "Propose solutions/"
- "In the timeline the image first entry for \"Корект Проект\" is gray when not selected and in color when hoovered. Make same effect for \"ап. Аркадия\""

### AI interpretation

The ATISTAT homepage needs a deliberately designed timeline-navigation pattern in place of the current bare horizontal overflow rail. The result must expose discoverable movement controls and position feedback, keep the active milestone visible, work with pointer, touch, and keyboard input, honor reduced-motion settings, and present equivalent behavior and localized labels in Bulgarian and English.

Apartment Arcadia must use a color-capable timeline asset and an explicit idle-versus-interactive visual-state rule. Its desktop milestone should be fully grayscale at rest and reveal its original color on hover, keyboard focus, and active selection; the intended mobile behavior and source asset remain to be selected.

## Research Results

### Workspace Pattern Scan

- `index.html`, `index-bg.html`, `index-en.html`, `index.html?lang=bg.html`, `index.html?lang=en.html`, and `opit/index.html` each contain the same thirteen-milestone timeline structure with localized Bulgarian or English copy.
- `wp-content/themes/atistat/assets/css/main.css` gives each desktop milestone a fixed responsive basis and makes `.at-tl-buildings` a smooth horizontal overflow container. It provides no previous/next controls, snap points, edge fades, scroll-position indicator, or deliberate active-item centering.
- The desktop design switches to stacked cards at 860 CSS pixels. A touch-capable device wider than that breakpoint receives the desktop rail even when hover is unavailable.
- `wp-content/themes/atistat/assets/js/main.js` activates panels on mouse enter, focus, and click. Left and Right Arrow navigation clamps at the rail ends, does not support Home or End, and does not explicitly scroll the newly active marker into the optimal viewing region.
- The shared rail mixes project buttons with partner-company anchors inside one `role="tablist"`. Only project buttons have `role="tab"`, while the script applies `aria-selected` to every `.at-tlb`; the tab panels have no `aria-labelledby`.
- `index.html` currently uses `arcadia-timeline.png` for the desktop Arcadia marker but `arcadia-timeline.webp` for its mobile card. The PNG is an intrinsically sepia line-art rendering, so removing a CSS grayscale filter cannot reveal full color. The other five timeline documents use the WebP in both positions.
- The default project-image rule uses `grayscale(.18)`, while Correct Project's logo-specific rule uses `grayscale(1)`. Arcadia therefore cannot match the Correct Project idle state without a targeted state class or broader state rule.
- `wp-content/themes/atistat/assets/css/main.css?ver=1784544801.css` and `wp-content/themes/atistat/assets/js/main.js?ver=1784544801` are byte-identical deployment copies of the canonical shared assets and must stay synchronized.
- `tests/test_static_site.py` and `tests/fixtures/interaction_harness.html` provide static integrity and basic two-item timeline interaction checks, but they do not cover real overflow, scroll controls, snap/centering behavior, complete tab semantics, all six timeline documents, touch-width behavior, or Arcadia's visual-state invariant.
- The current `context.md` correctly records the thirteen milestones, bilingual variants, shared vanilla implementation, keyboard and reduced-motion requirements, and horizontally scrollable rail. It is stale where it says the workspace has no automated tests and repeats that claim in `## Issues`.

### Industry Knowledge

- The [WAI-ARIA Tabs Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/tabs/) requires every item inside a tablist to be a tab, associates each tabpanel with its tab through `aria-labelledby`, wraps Left and Right Arrow movement, and optionally supports Home and End. Applicability: the current marker-and-panel relationship already behaves like automatic-activation tabs, so either the markup must be completed as a valid tabs widget or the component must adopt a different semantic pattern consistently.
- The [WAI-ARIA Carousel Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/carousel/) identifies native previous and next buttons, an accessible container label, meaningful item names, and controlled focus order as appropriate mechanisms for sequential content. Applicability: explicit navigation buttons can make overflow discoverable without introducing automatic rotation; a grouped or tabbed carousel model is viable only if its semantics remain consistent with the detail panels.
- The [CSS Scroll Snap Module Level 1](https://www.w3.org/TR/css-scroll-snap-1/) defines snap positions, `scroll-padding`, and `scroll-margin` to prevent imprecise scrolling from ending at awkward partially visible positions. Applicability: proximity snapping and a defined optimal viewing region can preserve free touch/trackpad scrolling while making each milestone settle predictably.
- [WCAG 2.2 Target Size (Minimum)](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum) requires pointer targets to be at least 24 by 24 CSS pixels or sufficiently separated. Applicability: any added arrow, page, or position controls need measurable hit areas and spacing at every responsive breakpoint.
- [WCAG 2.2 Focus Not Obscured (Minimum)](https://www.w3.org/WAI/WCAG22/Understanding/focus-not-obscured-minimum) requires the focused component not to be entirely hidden by author-created content. Applicability: keyboard movement through an overflow rail must also move the viewport so the newly focused milestone and its focus outline remain visible.

### AI Agent critique

- The request uses subjective terms such as "ugly" and "modern" without observable acceptance criteria, so several materially different designs could all claim compliance.
- "Propose solutions/" has an extraneous slash, and "hoovered" appears to mean "hovered"; neither prevents interpretation, but both reduce specification precision.
- The input combines a broad interaction redesign with an Arcadia visual-state correction. They share one timeline component, so they are implementable together, but acceptance criteria must cover each concern separately.
- The input names BG and EN but does not identify whether this means two canonical pages, all five root homepage copies, or those copies plus `opit/index.html`.
- `context.md` contains an accurate accessibility requirement but also records known timeline violations—mixed tab/link semantics, missing panel labels, and clamped Arrow navigation—that a modernized interaction should not preserve.
- `context.md` says there is no automated test suite even though `tests/test_static_site.py` and its browser harness exist; this would misdirect later planning if left unchanged.
- `index.html` is the only timeline document whose desktop Arcadia marker uses the sepia PNG, and its own mobile card uses a different WebP. This creates an intra-page visual inconsistency and cross-language drift.
- The Arcadia PNG cannot produce a grayscale-to-color transition through CSS alone because it does not contain the desired full-color pixel information.
- `main.css` exposes a native overflow area without visible movement controls or edge cues, so mouse users may not discover horizontal scrolling and browser scrollbar appearance determines a material part of the design.
- `main.css` enlarges hovered and active marker images into neighboring columns. Inside the overflow viewport, this can create clipping and overlap precisely where the user already reports visual roughness.
- `main.css` makes all project images only 18 percent grayscale at rest, which does not match the fully grayscale Correct Project rule requested as the Arcadia reference.
- `main.css` removes motion under `prefers-reduced-motion`, but any new programmatic centering and control transitions must be included in the same invariant.
- `main.js` uses hover to change the detail panel and its polite live region, which can create rapid uncommitted content changes while a pointer traverses the rail.
- `main.js` applies tab state to anchors that are not tabs, does not maintain roving `tabindex`, does not wrap Arrow navigation, and omits Home and End behavior expected by the APG tabs model.
- `main.js` relies on native focus scrolling rather than explicitly aligning active markers; pointer selection can therefore update a panel while leaving its marker partly hidden.
- `tests/test_static_site.py` has a useful shared-asset identity invariant but its representative routes omit three root homepage variants and its interaction test does not use real overflow dimensions.
- `tests/fixtures/interaction_harness.html` verifies only click and one left-arrow transition between two project buttons, so it cannot detect end behavior, mixed element types, control labels, viewport movement, or selected-item visibility.
- `analysis-convention.md` and the S05 prompt both say Proposed Solution contains exactly three `###` subsections but enumerate only `High-Level Concept` and `Execution Steps`. This analysis follows the two explicitly named subsections and does not invent an undocumented third heading.
- `plan-convention.md` defines four exact top-level plan sections and no `## Decisions` section, while the analyze prompt's objective mentions updating `## Plan` and `## Decisions`. Plan generation should follow the normative plan convention after the questions are resolved.
- `requirements-analysis-convention.md` correctly exposes that the raw request lacks explicit stakeholders, priority, exclusions, constraints, and measurable success criteria; the Decision Points below collect the choices that cannot be recovered from the workspace.
- `q-block-convention.md` requires one recommended option in every multiple-choice question, while the Decision Register convention prohibits steering inside an unresolved `ask` Decision Point. The register therefore presents neutral alternatives; recommendations will appear only in the generated Q-blocks as the questionnaire convention requires.

### Edge Cases

- At 861 CSS pixels the interface changes abruptly from stacked cards to a thirteen-item desktop rail, producing the greatest overflow pressure immediately above the breakpoint.
- Wide tablets, touch laptops, and devices reporting no hover can receive a hover-oriented desktop design.
- Trackpads, Shift-plus-wheel input, touch panning, keyboard focus movement, and explicit arrow-button activation can each create different scroll endpoints unless they share one alignment rule.
- First and last milestones cannot be perfectly centered without intentional inline scroll padding; navigation controls must expose their disabled or wrapped state consistently.
- Multiple milestones share the year 2024 or 2025, so year-only position feedback is ambiguous.
- Company milestones currently navigate externally while project milestones select local panels; making all markers one semantic widget changes where outbound activation occurs.
- Hover-driven activation can cause repeated live-region announcements and accidental panel changes while the user is merely moving toward a control.
- Focus, hover, active selection, and pointer press can overlap; the Arcadia color-state cascade must define which state wins.
- A grayscale CSS filter cannot reconstruct color from an intrinsically monochrome or sepia source asset.
- The canonical Bulgarian page currently differs from its Bulgarian copy and from both English copies, so changing only shared CSS or JavaScript will not repair the Arcadia source inconsistency.
- Hiding the native scrollbar without adding operable, labelled controls would reduce discoverability and accessibility.
- Programmatic smooth scrolling and image transitions must become instant under `prefers-reduced-motion: reduce`.
- The page must remain usable without JavaScript: timeline content cannot become unreachable merely because enhanced navigation controls fail to initialize.
- Browser scrollbars, overlay scrollbars, zoom, text enlargement, and localized control-label length can change available rail width.
- Version-suffixed shared assets must remain byte-identical to their canonical files or the existing integrity test will fail.
- User-owned uncommitted edits already change Arcadia asset references in `index.html`; implementation must preserve the intended mobile change while resolving the desktop asset decision.

### Requirements Gate Evaluation

| Category | Result | Evaluation |
| --- | --- | --- |
| 1. Goal Clarity | PARTIAL | The pain point and component are clear, but "modern" and "ugly" are not measurable and the desired interaction model is not selected. |
| 2. Stakeholder and User Identification | PARTIAL | Website visitors are inferable from `context.md`, but affected pointer, touch, keyboard, and assistive-technology users are not explicitly confirmed. |
| 3. Business Value and Justification | PARTIAL | Better discoverability and consistent brand presentation are inferable benefits, but priority and urgency are unstated. |
| 4. Scope Definition | PARTIAL | Timeline scrolling, bilingual parity, and Arcadia states are named, but exact documents, breakpoints, mobile behavior, and semantics remain open. |
| 5. Out of Scope | FAIL | The developer input supplies no explicit exclusions. |
| 6. Constraints and Assumptions | PARTIAL | `context.md` supplies no-library, responsive, keyboard, and reduced-motion constraints, but load-bearing UX and asset assumptions are unresolved. |
| 7. Success Criteria and Acceptance | FAIL | No measurable pass/fail behavior is supplied for scrolling, control visibility, selection, parity, or Arcadia states. |
| 8. Context Adequacy | PARTIAL | The product context is substantial, but its test-suite statements are stale and its recorded timeline accessibility issues conflict with preserving the current markup unchanged. |

## Proposed Solution

### High-Level Concept

Replace the bare overflow rail with a guided, accessible timeline viewport that retains direct milestone selection while adding discoverable movement controls, predictable alignment, visible position cues, and deliberate pointer/touch/keyboard behavior. Use a color-capable Arcadia source with an explicit full-grayscale idle state and original-color interactive state, synchronized across the approved Bulgarian and English documents.

> Pending: depends on Decision Point Timeline navigation model, Decision Point Responsive milestone density, Decision Point Hover and selection contract, Decision Point Active-item scroll alignment, Decision Point Scrollbar presentation, Decision Point Position and edge affordance, Decision Point Wide-screen touch behavior, Decision Point Mobile timeline pattern, Decision Point Timeline semantic model, Decision Point Company milestone activation, Decision Point Arcadia asset strategy, Decision Point Arcadia state coverage, and Decision Point Bilingual document coverage.

### Execution Steps

#### Task 1: Harmonize timeline markup and localized controls

- index.html: Add the selected guided-navigation structure and Bulgarian accessible labels, normalize all milestone semantics, associate panels with controls, and apply the selected Arcadia asset/state hook.
- index-bg.html: Mirror the approved Bulgarian timeline structure, semantics, controls, and Arcadia asset.
- index.html?lang=bg.html: Mirror the approved Bulgarian timeline structure, semantics, controls, and Arcadia asset.
- index-en.html: Add the equivalent English timeline structure, semantics, controls, labels, and Arcadia asset.
- index.html?lang=en.html: Mirror the approved English timeline structure, semantics, controls, labels, and Arcadia asset.
- opit/index.html: Apply or intentionally exclude the same English timeline contract according to the document-coverage decision, preserving its relative asset paths.
  - The included documents must retain identical milestone order and equivalent behavior while localizing only user-facing copy.

> Pending: depends on Decision Point Timeline semantic model, Decision Point Company milestone activation, Decision Point Arcadia asset strategy, Decision Point Arcadia state coverage, and Decision Point Bilingual document coverage.

#### Task 2: Implement the shared visual and responsive timeline system

- wp-content/themes/atistat/assets/css/main.css: Define the selected viewport density, navigation-control layout, scroll snapping, edge/position affordances, scrollbar treatment, Arcadia grayscale/color states, focus visibility, touch behavior, mobile pattern, and reduced-motion overrides.
- wp-content/themes/atistat/assets/css/main.css?ver=1784544801.css: Synchronize the finalized canonical stylesheet byte-for-byte.

> Pending: depends on Decision Point Timeline navigation model, Decision Point Responsive milestone density, Decision Point Scrollbar presentation, Decision Point Position and edge affordance, Decision Point Wide-screen touch behavior, Decision Point Mobile timeline pattern, and Decision Point Arcadia state coverage.

#### Task 3: Implement deterministic timeline interaction

- wp-content/themes/atistat/assets/js/main.js: Add the selected previous/next, selection, viewport-alignment, keyboard, company-activation, overflow-state, and no-hover behavior while preserving progressive enhancement and reduced motion.
- wp-content/themes/atistat/assets/js/main.js?ver=1784544801: Synchronize the finalized canonical script byte-for-byte.

> Pending: depends on Decision Point Timeline navigation model, Decision Point Hover and selection contract, Decision Point Active-item scroll alignment, Decision Point Position and edge affordance, Decision Point Wide-screen touch behavior, Decision Point Timeline semantic model, and Decision Point Company milestone activation.

#### Task 4: Extend regression coverage

- tests/fixtures/interaction_harness.html: Model a genuinely overflowing timeline with selected semantics, navigation controls, boundary items, panels, and Arcadia state hooks.
- tests/test_static_site.py: Add deterministic assertions for included document parity, Arcadia asset/state wiring, canonical/deployment asset identity, keyboard boundaries, control state, selected-item visibility, reduced motion, and JavaScript-free fallback.
- python -m unittest discover -s tests -v: Run the complete static and browser regression suite and require a zero exit status with all tests passing.

#### Task 5: Update product memory

- .aib_memory/context.md: Replace the bare-scroll timeline description with the selected navigation and state contract, record Arcadia behavior and document coverage, correct the stale automated-test statements, and record any residual critique findings in `## Issues`.

## Context Update Analysis

- **Context Elements to Add:** Add a `## Solution` statement describing the selected guided timeline navigation, viewport-alignment, responsive, semantic, and Arcadia grayscale-to-color contract. Add a precise statement of which Bulgarian and English documents participate in the invariant.
- **Context Elements to Modify/Remove:** Replace the current Solution statement that the timeline is merely "a horizontally scrollable single rail on desktop" with the selected enhanced interaction contract. Replace the stale Solution statement that the workspace has no automated tests with an accurate description of `tests/test_static_site.py` and its browser harness. Remove or rewrite the stale `## Issues` claim that no automated test suite exists; record instead any timeline overflow and state scenarios that remain untested after implementation.
- **Conflict Resolution & Intent Preservation:** The current horizontal-rail statement preserves all thirteen milestones at useful visual scale, while the proposal adds controls, snap/alignment, and position affordances without compressing or removing milestones. The existing keyboard and reduced-motion Requirements remain authoritative; the chosen semantics and script must close, rather than perpetuate, the recorded tab-pattern gaps and must disable smooth movement for reduced-motion users. The existing stacked-mobile-card statement is preserved unless the mobile-pattern decision explicitly replaces it with an equally accessible alternative. The existing no-third-party/build-pipeline constraint remains unchanged because the solution uses shared CSS and vanilla JavaScript. Correcting the test statements preserves their original intent—documenting delivery infrastructure—while making them factually accurate.

## Decision Register

### Decision Points

#### Decision Point: Timeline navigation model

- Tag: ask
- Applies to: Proposed Solution Tasks 1–3; determines the component's primary desktop movement and layout contract.
- Rationale/Resolution: The developer asks to "Propose solutions" and does not select one navigation model. The alternatives produce materially different markup, CSS, JavaScript, and acceptance behavior.
- Alternatives:
  - Guided snap rail: Keep one continuous milestone rail, add previous/next controls, and settle movement on milestone snap points. Benefits include direct continuity and touch/trackpad support; drawbacks include continued overflow and more state synchronization. Codebase impact spans every timeline document plus shared CSS, JavaScript, and tests.
  - Paginated milestone groups: Display a responsive group of milestones per page and move by group. Benefits include stable composition and clear boundaries; drawbacks include less direct chronological panning and pagination logic. Codebase impact spans timeline controls, group-state JavaScript, CSS, labels, and tests.
  - Compressed all-items overview: Fit all thirteen milestones into one non-scrolling overview and use a separate active detail area. Benefits include immediate chronology visibility; drawbacks include small targets and reduced image presence at medium widths. Codebase impact is primarily timeline CSS and responsive markup, with selection tests.

#### Decision Point: Responsive milestone density

- Tag: ask
- Applies to: Proposed Solution Tasks 1, 2, and 4; determines the number and scale of simultaneously visible desktop markers.
- Rationale/Resolution: The current responsive fixed basis preserves image size but gives no target density, and "modern" does not define one.
- Alternatives:
  - Centered feature with neighbor peeks: Emphasize one selected milestone and expose partial previous/next markers. Benefits include strong focus and obvious direction; drawbacks include reduced overview. Codebase impact includes larger marker sizing, center alignment, and peek-spacing tests.
  - Responsive five-item viewport: Keep roughly five complete markers visible above the mobile breakpoint. Benefits include balanced context and target size; drawbacks include more navigation for thirteen items. Codebase impact includes deterministic viewport sizing and breakpoint tests.
  - Maximum-fit viewport: Show as many fixed-minimum-width markers as each viewport allows. Benefits include broad chronology context; drawbacks include variable composition and more complex acceptance thresholds. Codebase impact includes CSS sizing rules and multi-width tests.

#### Decision Point: Hover and selection contract

- Tag: ask
- Applies to: Proposed Solution Tasks 2–4; determines whether pointer hover changes the active detail panel.
- Rationale/Resolution: The current implementation commits selection on hover, focus, and click, while the input mentions hover only for the Arcadia color effect and does not specify panel activation.
- Alternatives:
  - Hover emphasis without panel activation: Hover changes marker appearance only; click, keyboard activation, or Arrow navigation selects a panel. Benefits include stable content and fewer live announcements; drawbacks include an extra click for mouse users. Codebase impact changes event handling, CSS states, and interaction tests.
  - Hover preview with click commitment: Hover temporarily previews a panel and restores the committed selection on pointer exit. Benefits include fast exploration; drawbacks include two concurrent states and greater complexity. Codebase impact adds preview state, restoration logic, and detailed tests.
  - Immediate hover selection: Preserve the current rule where hover permanently selects the panel. Benefits include low-friction browsing; drawbacks include accidental changes and noisy live announcements. Codebase impact is smallest but still needs scroll-state integration and tests.

#### Decision Point: Active-item scroll alignment

- Tag: ask
- Applies to: Proposed Solution Tasks 2–4; determines where selection and keyboard movement position the marker in the overflow viewport.
- Rationale/Resolution: The workspace does not define whether active markers should center, merely become visible, or never move automatically.
- Alternatives:
  - Center active marker: Align the selected marker to the viewport center except at rail boundaries. Benefits include stable focus and neighbor context; drawbacks include more viewport motion. Codebase impact adds scroll padding, programmatic alignment, and boundary tests.
  - Nearest-edge visibility: Scroll only enough to reveal a hidden or clipped marker. Benefits include minimal motion; drawbacks include inconsistent marker position. Codebase impact adds visibility measurement and reduced movement tests.
  - Manual rail position: Never move the rail after selection. Benefits include user-controlled scrolling; drawbacks include hidden active/focused markers and weaker keyboard UX. Codebase impact is small but requires focus-obscuration safeguards.

#### Decision Point: Scrollbar presentation

- Tag: ask
- Applies to: Proposed Solution Tasks 2 and 4; determines whether the browser's native horizontal scrollbar remains part of the final design.
- Rationale/Resolution: The complaint targets ugly scrolling, but the input does not identify whether the scrollbar itself, the movement, or the lack of controls is objectionable.
- Alternatives:
  - Hide the native bar after enhancement: Suppress its visual track only when labelled controls and free scrolling are available. Benefits include a clean composition; drawbacks include reliance on replacement affordances. Codebase impact adds progressive-enhancement selectors and fallback tests.
  - Style a thin branded scrollbar: Keep a visible native scroll track with supported browser-specific styling. Benefits include familiar discoverability; drawbacks include browser variation and partial styling support. Codebase impact is mainly CSS plus cross-browser review.
  - Keep the operating-system scrollbar: Preserve native rendering and improve only surrounding controls and alignment. Benefits include maximum familiarity and platform accessibility; drawbacks include the least visual control. Codebase impact is minimal.

#### Decision Point: Position and edge affordance

- Tag: ask
- Applies to: Proposed Solution Tasks 1–4; determines how users recognize hidden milestones and current position.
- Rationale/Resolution: Neither input nor context specifies whether the design should use edges, arrows, a progress indicator, or a textual position label.
- Alternatives:
  - Arrows plus edge fades: Use disabled-state previous/next buttons and subtle directional fades when more content exists. Benefits include immediate discoverability with low visual weight; drawbacks include no exact position count. Codebase impact adds controls, overflow observation, CSS masks, localized labels, and tests.
  - Viewport minimap: Show a compact track indicating the visible portion of the full chronology. Benefits include global position awareness; drawbacks include extra visual and state complexity. Codebase impact adds scroll-ratio calculations, track markup, CSS, and tests.
  - Textual position status: Show selected milestone position and total, optionally with the current year range. Benefits include precise accessible feedback; drawbacks include less direct directional imagery. Codebase impact adds a localized live/status element and update tests.

#### Decision Point: Wide-screen touch behavior

- Tag: ask
- Applies to: Proposed Solution Tasks 2–4; determines interaction above 860 CSS pixels when hover is unavailable.
- Rationale/Resolution: The current breakpoint is width-only, so wide tablets receive a desktop interaction whose hint and selection behavior assume hover.
- Alternatives:
  - Swipe and visible buttons: Support direct touch panning plus previous/next buttons. Benefits include flexibility and discoverability; drawbacks include two synchronized input paths. Codebase impact includes pointer-capability CSS, button state, snapping, and touch tests.
  - Swipe-first rail: Preserve touch panning and show edge cues without persistent buttons. Benefits include a lighter interface; drawbacks include lower discoverability. Codebase impact centers on snapping, touch hints, and edge-state tests.
  - Button-driven paging: Disable free touch panning and move through explicit controls. Benefits include deterministic composition; drawbacks include reduced gesture familiarity. Codebase impact includes overflow locking, paging logic, and target-size tests.

#### Decision Point: Mobile timeline pattern

- Tag: ask
- Applies to: Proposed Solution Tasks 1, 2, and 4; determines whether the redesign changes layouts at or below 860 CSS pixels.
- Rationale/Resolution: The input says "timelines" but reports a scrolling problem that does not occur in the current stacked-card mobile layout.
- Alternatives:
  - Retain stacked cards: Keep all milestones vertically visible and modernize only shared state styling. Benefits include scanability and no horizontal trap; drawbacks include a long page. Codebase impact is limited to state and parity updates plus tests.
  - Horizontal card carousel: Present one card with neighboring peeks and swipe/buttons. Benefits include compactness and consistent navigation language; drawbacks include hidden content and more accessibility work. Codebase impact substantially changes mobile markup, CSS, JavaScript, and tests.
  - Year accordion: Show a chronological list of headings that expand details. Benefits include compact vertical navigation; drawbacks include more activation and a second component model. Codebase impact changes mobile markup, disclosure scripting, styles, and tests.

#### Decision Point: Timeline semantic model

- Tag: ask
- Applies to: Proposed Solution Tasks 1, 3, and 4; determines the accessible roles, focus order, and keyboard contract.
- Rationale/Resolution: The current mixed link/tab implementation is invalid as a complete tabs pattern, and the workspace does not explicitly select tabs versus carousel semantics.
- Alternatives:
  - Complete automatic-activation tabs: Make every milestone marker a tab with roving focus and labelled tabpanels. Benefits include a direct match to the existing panel relationship; drawbacks include moving outbound links into panels. Codebase impact changes all timeline markup, keyboard logic, and accessibility tests.
  - Grouped carousel controls: Treat markers as named picker buttons within a carousel group and panels as labelled slides. Benefits include explicit sequential-navigation semantics; drawbacks include more extensive role and announcement changes. Codebase impact changes containers, controls, panel semantics, JavaScript, and tests.
  - Split semantic groups: Keep project markers as tabs and place company links outside the tablist as separate milestones. Benefits include preserving direct links; drawbacks include a visually unified rail with two keyboard models. Codebase impact adds group boundaries, focus logic, and mixed-pattern tests.

#### Decision Point: Company milestone activation

- Tag: ask
- Applies to: Proposed Solution Tasks 1, 3, and 4; determines how Correct Project, Engineering Systems, and ATISTAT markers combine panel selection with navigation.
- Rationale/Resolution: Company markers currently open destinations while also participating in panel activation, and the requested Correct Project visual reference does not define the intended click result.
- Alternatives:
  - Select marker, link from panel: Marker activation displays the company panel, whose explicit link opens the destination. Benefits include consistent milestone behavior; drawbacks include one additional activation to leave the site. Codebase impact normalizes marker elements and panel links.
  - Navigate directly from marker: Preserve immediate outbound navigation while hover/focus previews its panel. Benefits include existing behavior continuity; drawbacks include mixed semantics and possible accidental navigation. Codebase impact keeps anchors but requires a non-tabs semantic model and tests.
  - Separate marker and external-link control: The marker selects the panel and a dedicated adjacent icon opens the destination. Benefits include explicit actions; drawbacks include more controls and target crowding. Codebase impact adds markup, labels, layout, and focus-order tests.

#### Decision Point: Arcadia asset strategy

- Tag: ask
- Applies to: Proposed Solution Tasks 1, 2, and 4; determines how the idle-to-color effect obtains color information.
- Rationale/Resolution: `index.html` uses a sepia line-art PNG on desktop, while other timeline copies and the mobile card use a WebP; CSS cannot reveal color absent from the selected source.
- Alternatives:
  - Existing color-capable WebP plus CSS filter: Use `arcadia-timeline.webp` consistently and apply full grayscale only in the idle desktop state. Benefits include no new asset and straightforward state logic; drawbacks include a more photographic look than the PNG. Codebase impact changes HTML references, CSS state hooks, and tests.
  - New color line-art asset plus CSS filter: Produce a color-capable illustration matching the line-art aesthetic, then grayscale it at rest. Benefits include visual consistency and a true color reveal; drawbacks include asset creation and approval work. Codebase impact adds one image asset plus HTML, CSS, and asset tests.
  - Paired idle and active assets: Keep a dedicated grayscale illustration and swap to a separate color image for interactive states. Benefits include precise art direction; drawbacks include duplicate payload, preload concerns, and more complex state management. Codebase impact adds asset references, swap CSS/markup, and loading tests.

#### Decision Point: Arcadia state coverage

- Tag: ask
- Applies to: Proposed Solution Tasks 1, 2, and 4; determines which devices and interaction states use grayscale versus color.
- Rationale/Resolution: The developer explicitly identifies idle and hover behavior, but touch has no hover and keyboard focus must receive an equivalent cue under the workspace accessibility requirements.
- Alternatives:
  - Desktop idle grayscale; interactive color: Use grayscale at rest and color for hover, focus-visible, and active selection; leave stacked mobile-card imagery in color. Benefits include a direct desktop analogue to Correct Project and no hidden mobile cue; drawbacks include different idle treatment by layout. Codebase impact is a targeted desktop state rule and tests.
  - All layouts idle grayscale; activated color: Use grayscale everywhere and reveal color on hover, focus, selection, or tap. Benefits include consistent brand behavior; drawbacks include persistent mobile selection state and extra scripting. Codebase impact spans desktop and mobile state markup, JavaScript, CSS, and tests.
  - Desktop hover/focus only: Reveal color transiently on pointer hover or keyboard focus but not for active selection or mobile. Benefits include simple behavior; drawbacks include losing the selected-state analogy in the input. Codebase impact is mostly CSS.

#### Decision Point: Bilingual document coverage

- Tag: ask
- Applies to: Proposed Solution Tasks 1, 4, and 5; determines the exact files governed by BG/EN parity.
- Rationale/Resolution: The workspace contains five root homepage copies and an additional English experience page, while the input says only "same for BG and EN."
- Alternatives:
  - All six timeline documents: Update all five root homepage documents and `opit/index.html`. Benefits include complete behavior parity; drawbacks include the largest synchronization surface. Codebase impact touches six HTML files plus parity tests and context.
  - Five root homepage documents: Update the canonical and query-style BG/EN root pages but exclude `opit/index.html`. Benefits include root-route parity; drawbacks include leaving the experience route divergent. Codebase impact touches five HTML files and scoped tests.
  - Canonical BG and EN only: Update `index.html` and `index-en.html`. Benefits include the smallest immediate scope; drawbacks include visible drift in alternate public copies. Codebase impact touches two HTML files and documents intentional exclusions.
