---
state:
  request_id: R-20260731-0719
  title: Static investment instrument website mockup
  status: analysis_ready
  input_verification_result: null
  context_verification_result: null
options:
  minimum_questions: 13
  input_verification_enabled: true
  context_verification_enabled: true
---

## Input

Create a folder `workdir/investment_instrument`. In it create a static web site with landing file `index.html` which is, which is a complete mockup of `workdir/investment_instrument_input.md`. Also create a well visible link on the landing page to a sub-page which is complete HTML representation of `workdir/investment_instrument_input.md` so the user to be able to read in convenient way these requirements. 

Should be used Bulgarian

Try to find accessible real data and mockup their export from external sites and store the data in JSON files in `workdir/investment_instrument`. If some data are not accessible - fabricate them.

The mockup folder `workdir/investment_instrument` should be able to be archived as a zip and sent to the user to unzip it on their laptop and run it.

No installation and external dependencies (besides very standard JS/styling libraries like bootstrap which can entirely be downloaded and included in the zip) shall be used.

The goal is to demonstrate the idea to the user in as much as possible vivid and complete form, without implementing it entirely and without to be ready for production. Still if some functionality can be mimic in the conditions described - it should be realized.

As it is experimental, do not change the context.md yet

Avoid complicated navigation, unnecessary complexity. Make the tool self explanatory.