---
state:
  request_id: R-20260725-1211
  title: ATISTAT Stakeholder-Approved Homepage Improvements
  status: questions_generated
  input_verification_result: valid
  context_verification_result: valid
options:
  minimum_questions: 33
  input_verification_enabled: true
  context_verification_enabled: true
---

## Input

## Questions

**Q001**: Which HTML pattern should be used for the FAQ disclosure controls?
> **Why this matters:** The current `<details>/<summary>` pattern does not expose an explicit `aria-expanded` attribute. Switching to a `<button>`-controlled disclosure adds `aria-expanded` at the cost of requiring JavaScript. The choice directly affects accessibility acceptance criterion #26 and implementation scope.
- [x] Option A: Retain `<details>/<summary>` — native keyboard-operable, browser-managed disclosure state, WCAG 2.1 AA compliant, no JS required. *(recommended)*
- [ ] Option B: Convert to `<button>` + `<div>` controlled disclosure — explicit `aria-expanded` attribute, requires JS toggle, higher implementation effort.
- [ ] Other: ___

**Q002**: Which gallery grid layout approach should be used inside the Selected Projects dialog?
> **Why this matters:** The exact column count and breakpoint strategy affect how portrait (1086×1448) and landscape (1448×1086) images display across viewport widths, including the 320px minimum.
- [x] Option A: Auto-fill intrinsic grid — `repeat(auto-fill, minmax(220px, 1fr))` — responsive without explicit breakpoints, column count adapts naturally. *(recommended)*
- [ ] Option B: Fixed column count per breakpoint — 3 cols ≥768px, 2 cols ≥480px, 1 col below — predictable layout, avoids orphan images.
- [ ] Other: ___

**Q003**: What should the dialog close button position be?
> **Why this matters:** A sticky close button ensures it is always reachable as the user scrolls the gallery. An inline button at the top requires scrolling back to close.
- [x] Option A: Sticky close button at the dialog top — `position: sticky; top: 0` within the dialog content area — always visible while scrolling the gallery. *(recommended)*
- [ ] Option B: Inline close button at the top of the dialog content — scrolls with the gallery content.
- [ ] Option C: Fixed-position header bar inside the dialog with close button on the right — always visible, visually distinct.
- [ ] Other: ___

**Q004**: What backdrop style should the Selected Projects dialog use?
> **Why this matters:** The backdrop visually communicates that the main content is blocked. A solid semi-transparent overlay is the standard; a frosted-glass blur is more visually polished but may have higher GPU cost.
- [ ] Option A: Solid dark semi-transparent backdrop — `::backdrop { background: rgba(0,0,0,.65) }` — standard, high-contrast, accessible. *(recommended)*
- [x] Option B: Frosted glass blur backdrop — `::backdrop { backdrop-filter: blur(4px); background: rgba(0,0,0,.45) }` — visually modern, heavier rendering.
- [ ] Other: ___

**Q005**: What background treatment should the Contact CTA section use?
> **Why this matters:** The CTA section appears between Services and Partners. Its background distinguishes it from adjacent sections and influences conversion prominence.
- [ ] Option A: Light cream background matching `var(--cream)` with green heading — subtle, consistent with page palette.
- [x] Option B: Green background (`var(--green)`) with white text — high-visibility, strongest call-to-action signal. *(recommended)*
- [ ] Option C: Paper background (`var(--paper)`) with a bordered card containing the CTA content — structured, neutral.
- [ ] Other: ___

**Q006**: What visual style should the Contact CTA action buttons use?
> **Why this matters:** The four action buttons (phone, email, WhatsApp, Viber) need to be clearly actionable. Their shape and size affect visual prominence and tap-target size.
- [x] Option A: Circular icon-only buttons (48px × 48px) with SVG icon and `aria-label` — compact, icon-focused. *(recommended)*
- [ ] Option B: Pill-shaped buttons with icon + text label — more descriptive, wider.
- [ ] Option C: Plain icon links with hover underline — minimal, text-like.
- [ ] Other: ___

**Q007**: Which non-color-only treatment should indicate the active language option?
> **Why this matters:** WCAG 1.4.1 requires that information not be conveyed by color alone. The active language indicator must have a non-color cue (shape, weight, border, or underline).
- [x] Option A: Background chip — active option receives a small rounded background (`border-radius: 4px; padding: 4px 8px`) in green with white text — the chip shape is the non-color cue. *(recommended)*
- [ ] Option B: Underline — active option receives `text-decoration: underline` — the underline is the non-color cue.
- [ ] Option C: Outline border — active option receives a `1.5px solid` border around it — the border is the non-color cue.
- [ ] Other: ___

**Q008**: How should the `.at-sketch__cap` three-span children stack vertically?
> **Why this matters:** The parent `<span class="at-sketch__cap">` is inline by default. Block children or flex children are needed for vertical stacking. The choice affects how the caption overflows on narrow screens.
- [x] Option A: Convert `.at-sketch__cap` to `display: flex; flex-direction: column` — clean vertical stacking, easy gap control. *(recommended)*
- [ ] Option B: Set `.at-sketch__cap span { display: block }` and `.at-sketch__cap { display: block }` — simpler, avoids flex.
- [ ] Other: ___

**Q009**: How should timeline entries with a secondary description line render the two lines?
> **Why this matters:** Four project entries need a primary description and an experience attribution line (e.g., "Опит с Инженерни Системи"). The rendering choice affects both desktop panels and mobile cards.
- [x] Option A: Two separate `<p class="at-tlpanel__desc">` elements — clean semantic separation, existing CSS margin applies equally. *(recommended)*
- [ ] Option B: One `<p>` with a `<br>` between the two lines — simpler markup, harder to style independently.
- [ ] Option C: Primary `<p class="at-tlpanel__desc">` plus a styled `<p class="at-tlpanel__exp">` for the attribution line — distinct visual treatment.
- [ ] Other: ___

**Q010**: What visual style should the `.at-svc__features` bullet list use?
> **Why this matters:** The feature bullets appear before the existing numbered process steps. Their visual style must distinguish them from the steps while not competing with them.
- [x] Option A: Custom green checkmark bullets — `::before { content: "✓"; color: var(--green) }` — the green checkmark distinguishes features from numbered steps. *(recommended)*
- [ ] Option B: Native disc bullets with muted text — simple, matches existing `.at-svc__desc` color.
- [ ] Option C: Dash prefix — `::before { content: "–" }` — minimal, understated.
- [ ] Other: ___

**Q011**: What visual layout should the Partners section use?
> **Why this matters:** Three partners with logos (Engineering Systems, Correct Project) and styled text (Adreo) must form a horizontal row on desktop and wrap on mobile. Logo sizing affects the visual weight of the section.
- [x] Option A: Flex row with logos constrained to `max-height: 52px; object-fit: contain` and Adreo as matching-height styled text — compact, balanced. *(recommended)*
- [ ] Option B: Flex row with larger logos (`max-height: 80px`) — more prominent partner branding.
- [ ] Option C: Card tiles (grid with `min-width: 160px`) each containing logo + partner name below — structured card layout.
- [ ] Other: ___

**Q012**: What visual design should the Viber FAB use?
> **Why this matters:** The Viber FAB replaces the WhatsApp FAB. The color choice affects brand consistency between ATISTAT green and Viber purple.
- [x] Option A: ATISTAT green background (`var(--green)`) with white Viber SVG icon — consistent with site brand palette. *(recommended)*
- [ ] Option B: Viber brand purple background (`#7360f2`) with white Viber SVG icon — recognizable Viber brand color.
- [ ] Other: ___

**Q013**: How should the versioned CSS and JS copies be kept byte-identical after edits?
> **Why this matters:** `main.css?ver=1784544801.css` must be byte-identical to `main.css` and `main.js?ver=1784544801` must be byte-identical to `main.js`. The synchronization method determines whether the implementation agent must do this manually or can automate it.
- [x] Option A: After editing each canonical file, run `cp` to overwrite the versioned copy — explicit filesystem copy per edit session, reliable. *(recommended)*
- [ ] Option B: Use a symbolic link from the versioned name to the canonical file — transparent but not portable across all hosting environments.
- [ ] Other: ___

**Q014**: How should gallery images fill grid cells in the dialog?
> **Why this matters:** Portrait (1086×1448) and landscape (1448×1086) images will coexist in the same grid. `object-fit: cover` creates uniform cells with cropping; `contain` preserves aspect ratio with whitespace.
- [x] Option A: `object-fit: contain; height: auto; max-width: 100%` — no cropping, aspect ratios preserved, cells vary in height. *(recommended)*
- [ ] Option B: Fixed-height cells with `object-fit: cover` — uniform grid rows, portrait images are cropped at sides.
- [ ] Option C: Aspect-ratio containers (`aspect-ratio: 4/3`) with `object-fit: cover` — uniform cells with consistent proportions.
- [ ] Other: ___

**Q015**: Should the ATISTAT timeline panel include a description paragraph?
> **Why this matters:** The nine-entry timeline table specifies "no added claim" for ATISTAT (year 2024). It is unclear whether the desktop panel should have no `<p class="at-tlpanel__desc">` at all, or an empty/absent element.
- [x] Option A: Omit the `<p class="at-tlpanel__desc">` element entirely for the ATISTAT panel — cleanest markup, no empty element. *(recommended)*
- [ ] Option B: Include `<p class="at-tlpanel__desc"></p>` empty element — consistent element structure with other entries.
- [ ] Other: ___

**Q016**: Should the English Why ATISTAT section use autonomously translated card content?
> **Why this matters:** The four "ЗАЩО АТИСТАТ" cards have Bulgarian body text containing factual claims about Engineering Systems awards. An autonomous translation must not add or remove claims. Developer confirmation avoids misrepresentation.
- [x] Option A: Translate autonomously — use faithful English rendering of all four card headings and body text as written in `index.html`. *(recommended)*
- [ ] Option B: Provide draft translations for developer review and approval before implementation proceeds.
- [ ] Other: ___

**Q017**: Should the English FAQ section use autonomously translated question-and-answer content?
> **Why this matters:** The five FAQ items contain factual claims about ATISTAT's process and capabilities. An autonomous translation must preserve exact meaning. Developer confirmation ensures accuracy before implementation.
- [x] Option A: Translate all five FAQ items autonomously — faithful English rendering of questions and answers as written in `index.html`. *(recommended)*
- [ ] Option B: Provide draft English FAQ translations for developer review and approval before implementation.
- [ ] Other: ___

**Q018**: What value should `--active-progress` be set to after the ATISTAT timeline entry is inserted?
> **Why this matters:** This CSS variable controls the green progress bar width in the desktop timeline. After inserting ATISTAT at index 6, the total is 9 entries and Montekanal at index 3 becomes 4th of 9. The progress bar should advance to that position.
- [x] Option A: Set to `37.5%` — calculated as 3 intervals / 8 total intervals (from position 0 to position 3 across 9 entries). *(recommended)*
- [ ] Option B: Recalculate using `(active_index / (total - 1)) * 100` = `(3/8) * 100` = `37.5%` — same result via explicit formula.
- [ ] Option C: Keep the current value (`42.857%`) and rely on JS to correct it at runtime.
- [ ] Other: ___

**Q019**: How should Adreo be rendered in the Partners section?
> **Why this matters:** No Adreo logo exists in the local asset directories. The input prescribes styled text as fallback. The exact element affects visual alignment with the two logo images.
- [x] Option A: `<span>` text styled to match the logo height — `font-size: 1.4rem; font-weight: 700; color: var(--ink)` — visually weighted text as logo substitute. *(recommended)*
- [ ] Option B: `<p>` text element — semantic paragraph within the partner link.
- [ ] Option C: Plain text node inside `<a>` with no additional wrapper — minimal.
- [ ] Other: ___

**Q020**: Should the gallery grid use one column at 320px via auto-fill or an explicit media query?
> **Why this matters:** Ensuring no horizontal overflow at 320px is an explicit acceptance criterion. An auto-fill grid with `minmax(220px, 1fr)` at 320px with 16px gutters yields ~288px available — enough for one 220px column. An explicit media query is more predictable.
- [x] Option A: Rely on auto-fill natural behavior — `minmax(220px, 1fr)` produces one column at 320px viewport. *(recommended)*
- [ ] Option B: Add explicit `@media (max-width: 400px) { grid-template-columns: 1fr }` to guarantee one column.
- [ ] Other: ___

**Q021**: What dialog scroll pattern ensures the close button is always reachable?
> **Why this matters:** The dialog must fit within the viewport and have an always-reachable close button. The pattern chosen determines whether users can close without scrolling.
- [x] Option A: Dialog has a sticky/fixed header band containing the close button, with a scrollable content area below — close always visible. *(recommended)*
- [ ] Option B: Full dialog scrolls with `overflow-y: auto; max-height: 90vh` — close button scrolls with content.
- [ ] Other: ___

**Q022**: How should the experience attribution "separate line" render differently from the main description?
> **Why this matters:** The attribution line (e.g., "Опит с Инженерни Системи") identifies a professional context. A distinct style (italic, smaller, or green) helps distinguish it from the primary description. The same treatment applies to mobile cards.
- [x] Option A: Same style as primary `<p class="at-tlpanel__desc">` — same muted color, same font size. *(recommended)*
- [ ] Option B: Italic and slightly smaller text — visually subordinate to the primary description.
- [ ] Option C: Green colored text (`color: var(--green)`) matching the timeline type color — visually connected to the project type.
- [ ] Other: ___

**Q023**: Should the primary hero image loading remain `eager` only for `index.html`, leaving `index-bg.html` and `index.html?lang=bg.html` as `lazy`?
> **Why this matters:** The input says "Preserve `loading='eager'` and `fetchpriority='high'` for the primary `index.html` hero image." The BG companion documents already use `loading='lazy'` for the same hero image. Changing companions could improve LCP on those routes.
- [x] Option A: Keep `index.html` as `loading="eager" fetchpriority="high"` and leave the BG companions unchanged at `loading="lazy"` — strictly follows input scope. *(recommended)*
- [ ] Option B: Apply `loading="eager" fetchpriority="high"` to all three BG documents for performance consistency.
- [ ] Other: ___

**Q024**: Where exactly in `section#opit` should the Selected Projects trigger button be inserted?
> **Why this matters:** The input says "after the section lead near the beginning of the section." The exact DOM position affects visual flow between the section lead and the timeline.
- [x] Option A: After `</header>` of `.at-opit__head` and before `<div class="at-home-timeline">` — the trigger sits between the section header and the timeline. *(recommended)*
- [ ] Option B: Inside `.at-opit__headcopy` after the `<p class="at-lead">` — the trigger is part of the section lead content block.
- [ ] Other: ___

**Q025**: Should the document-local `<style>` block in `index.html` retain the Why ATISTAT and FAQ CSS after the stats/testimonials/social removals?
> **Why this matters:** The inline `<style>` block in `index.html` contains CSS for Why, FAQ, hero sub-text, hero CTAs, and the items being removed. Retaining Why and FAQ inline CSS is correct. Alternatively, those styles could be moved to `main.css`. The prompt says to remove only the specified selectors.
- [x] Option A: Remove only the stat, testimonial, social-link, and WhatsApp FAB selectors; retain Why, FAQ, hero sub-text, and hero CTA inline styles. *(recommended)*
- [ ] Option B: Move all retained inline styles (Why, FAQ, hero sub-text, hero CTAs) to `main.css` and eliminate the document-local `<style>` block entirely — cleaner but beyond stated scope.
- [ ] Other: ___

**Q026**: What heading level should the Contact CTA section use for its primary heading?
> **Why this matters:** All other top-level section headings in the homepage documents use `<h2>`. The CTA section heading (`id="contact-cta-title"`) should follow the same convention.
- [x] Option A: `<h2 id="contact-cta-title">` — consistent with other section headings (`<h2 class="at-h2">` pattern). *(recommended)*
- [ ] Option B: `<h3 id="contact-cta-title">` — subordinate level, treats CTA as a subsection.
- [ ] Other: ___

**Q027**: Should `opit/index.html` also receive the Selected Projects and timeline updates?
> **Why this matters:** `opit/index.html` mirrors the homepage timeline and is the English experience route. It is not in the five-document affected set listed in the input. Updating it achieves broader site consistency; not updating maintains strict scope.
- [x] Option A: Do not modify `opit/index.html` — it is not in the input's affected route list; strict scope is preferred. *(recommended)*
- [ ] Option B: Apply timeline (9 entries) and Selected Projects trigger/dialog to `opit/index.html` for full site consistency.
- [ ] Other: ___

**Q028**: Should `index-bg.html` and `index.html?lang=bg.html` be checked for existing WhatsApp entry before adding it?
> **Why this matters:** The input says "add Viber and WhatsApp if absent." If WhatsApp already exists in the BG companions, adding it again would create a duplicate. The implementation agent needs to verify and conditionally add.
- [x] Option A: Check for existing WhatsApp in each document before inserting — conditional add prevents duplicates. *(recommended)*
- [ ] Option B: Always insert regardless — accept potential duplicates as an implementation verification issue.
- [ ] Other: ___

**Q029**: Should the Selected Projects dialog heading use `<h2>` or `<h3>`?
> **Why this matters:** Inside the dialog overlay, the heading establishes the dialog's accessible name via `aria-labelledby`. As the top-level heading of its own modal context, `<h2>` is appropriate. However, some teams prefer `<h3>` to avoid document outline conflicts.
- [x] Option A: `<h2 id="selected-projects-title">` — top-level heading for the dialog's self-contained context. *(recommended)*
- [ ] Option B: `<h3 id="selected-projects-title">` — subordinate heading that doesn't interfere with the main document outline.
- [ ] Other: ___

**Q030**: Should `sameAs` LinkedIn and Facebook URLs be kept in the JSON-LD Organization block even though the visible "Следете ни" social block is removed?
> **Why this matters:** The `sameAs` in JSON-LD communicates organizational social identity to search engines. The removal of the visible social block changes the page UI, not the organization's actual social presence. The input's JSON-LD block explicitly includes both URLs.
- [x] Option A: Keep LinkedIn and Facebook in JSON-LD `sameAs` — organizational identity is separate from the visible UI block; the input explicitly requires these URLs in the schema. *(recommended)*
- [ ] Option B: Remove them from JSON-LD to maintain full consistency between visible links and structured data.
- [ ] Other: ___

**Q031**: Should navigation links be updated to include anchors for the new Contact CTA and Partners sections?
> **Why this matters:** The current nav links go to `#hero`, `#about`, `#opit`, `#services`, `#contacts`. Adding CTA and Partners is optional but improves discoverability. The input does not explicitly require it.
- [ ] Option A: Do not add nav anchors for CTA or Partners — the input does not require nav changes; strict scope. *(recommended)*
- [x] Option B: Add `#partners` anchor to the navigation alongside the existing links.
- [ ] Other: ___

**Q032**: Should the "Признат от индустрията" Why card content be preserved as-is during the English translation?
> **Why this matters:** The card states "Инженерни Системи — партньорската ни компания — получи две награди „Сграда на годината" 2025." This attributes Engineering Systems' awards, not ATISTAT's. The input says "Preserve factual content already present; do not introduce new claims." The content is factual (correct attribution) but credits a partner.
- [x] Option A: Preserve exactly as written — translate faithfully, attribute Engineering Systems' awards to Engineering Systems as stated. *(recommended)*
- [ ] Option B: Modify the card to remove the award reference, replacing with a generic differentiator about industry recognition without citing specific awards.
- [ ] Other: ___

**Q033**: Should the gallery group headings use `<h3>` or `<h4>` elements?
> **Why this matters:** Within the dialog, seven project groups each need a heading. The dialog heading is `<h2>` (or `<h3>` per Q029 answer). Group headings should be one level below the dialog heading for correct outline hierarchy.
- [x] Option A: `<h3>` group headings if the dialog heading is `<h2>`; adjust if Q029 selects `<h3>` for the dialog heading. *(recommended)*
- [ ] Option B: Always `<h3>` regardless of dialog heading level.
- [ ] Option C: Always `<h4>` to avoid any potential conflict with section headings outside the dialog.
- [ ] Other: ___

