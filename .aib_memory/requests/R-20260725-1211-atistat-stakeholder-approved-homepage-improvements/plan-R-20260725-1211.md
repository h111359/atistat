# plan-R-20260725-1211

## Goal

Apply all 17 stakeholder-approved homepage improvements across the five root homepage documents (`index.html`, `index-bg.html`, `index.html?lang=bg.html`, `index-en.html`, `index.html?lang=en.html`) and the two shared theme assets (`main.css`, `main.js`). The implementation realizes the target state already described in context.md by making the HTML, CSS, and JavaScript files reflect it. Background: a stakeholder workbook (ATISTAT_idei_za_podobrenie_Versia_5, 24.07.2026) provided approved decisions; this plan implements only the in-scope items confirmed by Q-block answers in input-archive-2026-07-25_13-38-44.md in the request folder.

Impacted components: `index.html`, `index-bg.html`, `index.html?lang=bg.html`, `index-en.html`, `index.html?lang=en.html`, `wp-content/themes/atistat/assets/css/main.css`, `wp-content/themes/atistat/assets/css/main.css?ver=1784544801.css`, `wp-content/themes/atistat/assets/js/main.js`, `wp-content/themes/atistat/assets/js/main.js?ver=1784544801`, `wp-content/themes/atistat/assets/images/atistat-logo.png`, `wp-content/uploads/2026/07/` (32 gallery images).

## Constraints

- No third-party JavaScript libraries or build pipeline. All new behavior must use vanilla JavaScript.
- No image optimization or format conversion. Copy source files as-is.
- No modification of files outside the five root homepage documents, shared CSS/JS, and asset upload directories. Project pages under `proekti/` and `opit/index.html` are strictly out of scope.
- No fabrication of content. All text changes use exact strings from the input archive or autonomous faithful translations approved by developer (Q016, Q017, Q032).
- WhatsApp and Viber contact entries in `index-bg.html` and `index.html?lang=bg.html` must be added conditionally — check for existing entry before inserting to avoid duplicates.
- The `index.html` hero image retains `loading="eager" fetchpriority="high"`. BG companion documents retain `loading="lazy"` on their hero images.
- After editing `main.css` and `main.js`, copy each to its versioned sibling to maintain byte-identical pairs.
- Autonomous choices (per resolved Decision Points): Q001 native details/summary; Q002 auto-fill grid; Q003 sticky close button; Q004 frosted glass backdrop; Q005 green CTA background; Q006 48px circular buttons; Q007 background chip language indicator; Q008 flex-column caption; Q009 two-p description; Q010 green checkmark bullets; Q011 max-height 52px partners flex; Q012 green Viber FAB; Q013 cp sync; Q014 object-fit contain; Q015 omit ATISTAT description p; Q016 autonomous Why translation; Q017 autonomous FAQ translation; Q018 37.5% progress; Q019 span for Adreo; Q020 auto-fill 320px; Q021 sticky dialog header; Q022 same-style mobile cards; Q023 keep BG companions lazy; Q024 trigger after opit header; Q025 remove only orphaned CSS; Q026 h2 CTA heading; Q027 opit out of scope; Q028 conditional WhatsApp; Q029 h2 dialog heading; Q030 keep JSON-LD sameAs; Q031 add #partners nav; Q032 preserve Why card content; Q033 h3 group headings.

## Success criteria

- The approved Bulgarian hero sub-text "Строително-инвестиционна компания, базирана на 26 години професионален опит. Реализираме проекти с отговорност, качество и дългосрочна визия — от идея до ключ." appears in `<p class="at-hero__sub">` in all three BG homepage documents.
- The approved English hero sub-text "A construction and investment company built on 26 years of professional experience. We deliver projects responsibly, with quality and a long-term vision — from concept to turnkey completion." appears in `<p class="at-hero__sub">` in both EN homepage documents.
- Both sketch captions (`.at-hero__art .at-sketch__cap` and `.at-about__art .at-sketch__cap`) contain exactly three child span elements in all five files.
- `section#stats`, `section#testimonials`, the "Следете ни" social contact block, and the `.at-whatsapp-fab` wrapper are absent from `index.html`.
- The inline `<style>` block in `index.html` contains no `.at-stats`, `.at-testimonials`, `.at-social-links`, `.at-social-link`, or `.at-whatsapp-fab` selectors.
- All five documents contain exactly nine timeline entries in both `.at-tl-desktop` and `.at-tl-mobile`; entries are ordered 2006–2026 with ATISTAT at index 6 (year 2024).
- `--active-progress: 37.5%` is set in the timeline container in all five documents.
- All five documents contain `<section class="at-contact-cta">` immediately after `section#services`.
- All five documents contain `<section class="at-section at-partners" id="partners">` immediately after `.at-contact-cta`.
- All five documents contain the Selected Projects trigger button in `section#opit` after the section header.
- All five documents contain the native `<dialog id="selected-projects-dialog">` with seven project group headings at `<h3>` level and 32 image entries total.
- All five documents contain a Viber entry in the contacts list. `index.html` has a `.at-viber-fab` floating action button. No `.at-whatsapp-fab` exists in `index.html`.
- All five documents contain `<ul class="at-svc__features">` before each `<ol class="at-svc__steps">` in `section#services`.
- `index-bg.html`, `index.html?lang=bg.html`, `index-en.html`, and `index.html?lang=en.html` each contain `section#why` (Why ATISTAT) and a FAQ section.
- Language switcher active option has `aria-current="page"` and `is-active` class; non-active option has neither. Style uses green background chip.
- All five documents contain `<meta name="description">`, Open Graph, Twitter Card, and a valid JSON-LD Organization script block in `<head>`.
- `wp-content/themes/atistat/assets/css/main.css` and `wp-content/themes/atistat/assets/css/main.css?ver=1784544801.css` are byte-identical after implementation.
- `wp-content/themes/atistat/assets/js/main.js` and `wp-content/themes/atistat/assets/js/main.js?ver=1784544801` are byte-identical after implementation.
- `wp-content/themes/atistat/assets/images/atistat-logo.png` exists.
- All 32 gallery images exist in `wp-content/uploads/2026/07/`.
- All five documents contain a `#partners` anchor link in the main site navigation.

## Plan

### Task 1: Copy source assets to deployment directories

#### Intent
Copy the ATISTAT logo and all 32 gallery images from the workdir source directories to the correct deployment paths so the homepage documents can reference them by their final URLs.

#### Procedure

Copy `workdir/atistat-logo-outlined.png` to `wp-content/themes/atistat/assets/images/atistat-logo.png`.

Recursively copy all image files from `workdir/Избрани снимки за сайт - 24.07.2026/` and its seven subdirectories to `wp-content/uploads/2026/07/`, placing every file directly in that flat directory (not preserving subdirectory structure). The seven subdirectories and their expected file counts are: `ап. Аркадия - интериор` (6 PNG), `ап. Луй Айер - интериор` (4 PNG), `Бебелан - интериор` (6 JPEG), `Елемаг - строителство` (4 PNG), `Монтеканал - интериор` (4 PNG), `ОББ Интерлийз - интериор` (4 JPEG), `Пауър Пропъртис - интериор` (4 PNG). Total: 32 files. If a thirty-third image exists in any subdirectory, include it in the corresponding project group. Preserve exact filenames from the source directories.

Verify that `wp-content/uploads/2026/07/` contains exactly 32 files (or 33 if an additional file was present at implementation time) and that `wp-content/themes/atistat/assets/images/atistat-logo.png` exists.

### Task 2: Add new CSS rules to shared stylesheet

#### Intent
Extend `wp-content/themes/atistat/assets/css/main.css` with all styles required by the new sections, components, and accessibility improvements, without altering existing rules.

#### Procedure

In `wp-content/themes/atistat/assets/css/main.css`, add rules for the language switcher minimum interactive target: `.at-lang__opt` receives `display: inline-flex; min-width: 44px; min-height: 44px; align-items: center; justify-content: center` or equivalent padding expansion to 44×44 CSS pixels.

In `wp-content/themes/atistat/assets/css/main.css`, add the non-color-only active language indicator: `.at-lang__opt.is-active` receives a green background chip using `background: var(--green); color: #fff; border-radius: 4px; padding: 4px 8px`.

In `wp-content/themes/atistat/assets/css/main.css`, add the three-span caption stacking rule: `.at-sketch__cap` receives `display: flex; flex-direction: column`. Add overflow protection for narrow screens (overflow-wrap, max-width constraint, or smaller font size at or below 375px).

In `wp-content/themes/atistat/assets/css/main.css`, add `.at-contact-cta` section styles: green background using `var(--green)`, white text, centered content layout, appropriate padding. The section heading and sub-line text must be readable against the green background. Add `.at-contact-cta__actions` layout for the four circular buttons.

In `wp-content/themes/atistat/assets/css/main.css`, add `.at-contact-cta__btn` styles for circular icon buttons: `width: 48px; height: 48px; border-radius: 50%` with white icon color, centered flex alignment, and sufficient color contrast on the green background.

In `wp-content/themes/atistat/assets/css/main.css`, add `.at-partners` section styles: flex row layout for `.at-partners__list` with wrapping, partner logo images constrained to `max-height: 52px; object-fit: contain`, and Adreo styled text using `font-size: 1.4rem; font-weight: 700; color: var(--ink)` on the `.at-partner__wordmark` element.

In `wp-content/themes/atistat/assets/css/main.css`, add `.at-selected-projects__trigger` button styles: visually prominent, matching the design system's secondary button pattern, with adequate padding and font size.

In `wp-content/themes/atistat/assets/css/main.css`, add `dialog.at-selected-projects` styles: `max-width: min(960px, 92vw); max-height: 90vh; overflow: hidden; display: flex; flex-direction: column` to enable the sticky header + scrollable content pattern. Add a sticky header band inside the dialog containing the close button: `position: sticky; top: 0`.

In `wp-content/themes/atistat/assets/css/main.css`, add `::backdrop` style for the dialog: `backdrop-filter: blur(4px); background: rgba(0,0,0,.45)`.

In `wp-content/themes/atistat/assets/css/main.css`, add `.at-gallery-grid` styles: `display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: 12px`. Add image styles: `max-width: 100%; height: auto; object-fit: contain` to preserve aspect ratios without cropping.

In `wp-content/themes/atistat/assets/css/main.css`, add `.at-svc__features` list styles: `list-style: none; padding: 0; margin-bottom: 1em`. Add green checkmark bullet: `.at-svc__features li::before { content: "✓"; color: var(--green); margin-right: 0.5em }`.

In `wp-content/themes/atistat/assets/css/main.css`, add `.at-viber-fab` floating action button styles: same fixed bottom-right circular pattern as the former `.at-whatsapp-fab` but using `var(--green)` background with white Viber icon. Do not retain `.at-whatsapp-fab` rules.

In `wp-content/themes/atistat/assets/css/main.css`, add or update responsive breakpoint media queries for all new components at 320px, 375px, 768px, 1024px, and 1440px widths, ensuring no horizontal overflow at 320px for CTA buttons, Partners items, gallery grid, and dialog content.

### Task 3: Add dialog behavior to shared JavaScript

#### Intent
Extend `wp-content/themes/atistat/assets/js/main.js` with the Selected Projects dialog open/close behavior using the native `<dialog>` API and fail-safe null checks.

#### Procedure

In `wp-content/themes/atistat/assets/js/main.js`, at the end of the DOMContentLoaded handler (or in an equivalent initialization context), add dialog behavior. The behavior must: locate the button element with class `at-selected-projects__trigger`; locate the `<dialog id="selected-projects-dialog">` element; locate the close button inside the dialog. If any element is absent, return early without throwing. When the trigger button is clicked, call `showModal()` on the dialog. Move focus to the close button immediately after opening. When the close button is clicked, call `close()` on the dialog. Listen for the dialog's `cancel` event (Escape key) and call `close()` to allow default browser handling to proceed. After the dialog closes, return focus to the trigger button.

### Task 4: Sync versioned asset copies

#### Intent
Keep the versioned CSS and JS copies byte-identical to their canonical files after Tasks 2 and 3 have modified the canonical files.

#### Procedure

After completing Task 2, copy `wp-content/themes/atistat/assets/css/main.css` to `wp-content/themes/atistat/assets/css/main.css?ver=1784544801.css` using a filesystem copy command. Verify byte equality using `cmp`.

After completing Task 3, copy `wp-content/themes/atistat/assets/js/main.js` to `wp-content/themes/atistat/assets/js/main.js?ver=1784544801` using a filesystem copy command. Verify byte equality using `cmp`.

### Task 5: Apply all changes to `index.html` (primary Bulgarian homepage)

#### Intent
Apply all 17 in-scope improvements to `index.html`, including content updates, section additions, removals, timeline expansion, language switcher ARIA, SEO metadata, navigation update, and Viber FAB.

#### Procedure

In `index.html`, replace the text content of `<p class="at-hero__sub">` with the exact string: "Строително-инвестиционна компания, базирана на 26 години професионален опит. Реализираме проекти с отговорност, качество и дългосрочна визия — от идея до ключ."

In `index.html`, in `.at-hero__art .at-sketch__cap`, replace the single-line caption with three child span elements containing: (1) "Проект Елемаг", (2) "Строителство", (3) "Опит с Инженерни Системи". Add `aria-label="Проект Елемаг, строителство, опит с Инженерни Системи"` to the parent `.at-sketch` anchor element.

In `index.html`, remove the entire `<section class="at-stats" id="stats">` element and all its descendant content. In the document-local `<style>` block, remove all CSS rule blocks that begin with or contain `.at-stats`, `.at-stats__list`, `.at-stats__item`, and any associated descendant, state (hover, focus, etc.), and media-query selectors. Do not remove any other selectors from the style block.

In `index.html`, remove the entire `<section class="at-section at-testimonials" id="testimonials">` element and all its descendant content. In the document-local `<style>` block, remove all CSS rule blocks for `.at-testimonials` and any associated descendant, state, and media-query selectors.

In `index.html`, in `section#contacts`, remove the "Следете ни" social block list item (the `<li>` or block containing the social icons/links). In the document-local `<style>` block, remove all rule blocks for `.at-social-links` and `.at-social-link` and their descendants.

In `index.html`, in `.at-about__art .at-sketch__cap`, replace the single-line caption with three child span elements containing: (1) "Проект Монтеканал", (2) "Строителство", (3) "Опит с Инженерни Системи". Add `aria-label="Проект Монтеканал, строителство, опит с Инженерни Системи"` to the parent `.at-sketch` anchor element.

In `index.html`, in `section#opit`, add a `<button type="button" class="at-selected-projects__trigger" aria-haspopup="dialog" aria-controls="selected-projects-dialog">` element with Bulgarian label text immediately after the closing tag of `.at-opit__head` (the section header element) and before the `<div class="at-home-timeline">` element.

In `index.html`, update the desktop timeline in `.at-tl-desktop`: insert the ATISTAT entry at zero-based index 6 with `id="timeline-atistat"`, year 2024, name "АТИСТАТ", type "Строително-инвестиционна компания", no description paragraph, link to `/`. Renumber the former index-6 ОББ Интерлийз entry to index 7 and the former index-7 Британско училище entry to index 8. Update all `data-index`, `data-panel`, `aria-controls`, and panel `id` attributes to match the new 0-based indices 0–8. Use the ATISTAT logo image at `wp-content/themes/atistat/assets/images/atistat-logo.png` with `alt="ATISTAT"`. For the four project entries that require a second description line (ЕОС Матрикс: "Опит с Корект Проект"; Монтеканал, ЕМА, Елемаг, ОББ Интерлийз, Британско училище: "Опит с Инженерни Системи"), add a second `<p class="at-tlpanel__desc">` element in each panel immediately after the existing one. Update `--active-progress` in the timeline container's inline style to `37.5%`.

In `index.html`, update the mobile timeline in `.at-tl-mobile`: insert the ATISTAT logo card between the Елемаг card and the ОББ Интерлийз card. The card must use `at-tlcard--logo` class, `data-href="/"`, `style="cursor:pointer"`, and the ATISTAT logo image. Renumber ОББ Интерлийз to index 7 and Британско училище to index 8. Add experience attribution second description paragraphs to the applicable mobile card description areas using two `<p class="at-tlpanel__desc">` elements.

In `index.html`, in `section#contacts`, add a Viber contact list item adjacent to the existing WhatsApp entry. The Viber link uses `href="viber://chat?number=359884074048"` (no `target="_blank"`), visible label "Viber: +359 884 074 048", and `title` and `aria-label` both equal to "Свързване с нас чрез Viber". Use the same list item structure as the WhatsApp entry.

In `index.html`, insert `<section class="at-contact-cta" aria-labelledby="contact-cta-title">` immediately after the closing tag of `section#services`. The section must contain an `<h2 id="contact-cta-title">Свържете се с нас</h2>` heading, a sub-line paragraph with "+359 884 074 048 · office@atistat.com", and four circular icon link buttons: phone (`href="tel:+359884074048"`, `aria-label="Обадете се на АТИСТАТ"`), email (`href="mailto:office@atistat.com"`, `aria-label="Изпратете имейл на АТИСТАТ"`), WhatsApp (`href="https://wa.me/359884074048"` with `target="_blank" rel="noopener noreferrer"`, `aria-label="Свържете се с АТИСТАТ чрез WhatsApp"`), Viber (`href="viber://chat?number=359884074048"`, `aria-label` and `title` both equal to "Свържете се с АТИСТАТ чрез Viber"). Each button uses an inline SVG icon with `aria-hidden="true" focusable="false"`.

In `index.html`, insert `<section class="at-section at-partners" id="partners">` immediately after the closing tag of `.at-contact-cta`. The section must contain an `<h2>` heading "ПАРТНЬОРИ" and three partner link items: Инженерни Системи (link to `https://engsys.bg` with `target="_blank" rel="noopener"`, image `wp-content/themes/atistat/assets/images/engsys.png`), Корект Проект (link to `https://correctproject.com` with `target="_blank" rel="noopener"`, image `wp-content/themes/atistat/assets/images/correctproject.png`), Адрео (link to `https://www.adreo.bg/` with `target="_blank" rel="noopener"`, rendered as `<span class="at-partner__wordmark">Адрео</span>` since no local logo exists). Each link must have an accessible name that identifies the partner and that it opens the partner website.

In `index.html`, replace the `.at-whatsapp-fab` wrapper and its anchor with a Viber FAB using class `at-viber-fab`, `href="viber://chat?number=359884074048"` (no `target="_blank"`), an inline Viber SVG icon, and both `aria-label="Свържете се чрез Viber"` and `title="Свържете се чрез Viber"`. In the document-local `<style>` block, rename all `.at-whatsapp-fab` selector occurrences to `.at-viber-fab`.

In `index.html`, in `section#services`, for each of the four `article.at-svc` elements, add a `<ul class="at-svc__features">` immediately before the existing `<ol class="at-svc__steps">`. Bulgarian feature bullets per service: residential construction — "Цялостна организация на строителството", "Контрол на срок, бюджет и качество", "Завършване и въвеждане в експлоатация"; renovation and reconstruction — "Обследване на сградата", "Планиране на ресурси и график", "Изпълнение и предаване на ремонта"; project development — "Анализ на терен или имот", "Разработване и координация на концепция", "Финансов и времеви модел"; real-estate investment — "Пазарен анализ и проверка", "Оценка на разход, риск и потенциал", "Структуриране и проследяване на инвестицията".

In `index.html`, in the language switcher, ensure the BG anchor has `aria-current="page"` and `class` containing `is-active`. Ensure the EN anchor has neither `aria-current` nor `is-active`. Do not change the href values.

In `index.html`, add a `<nav>` link or list item with anchor `href="#partners"` and Bulgarian label text to the main site navigation, alongside the existing navigation links.

In `index.html`, in `<head>`, insert the following tags (do not duplicate any existing tag): `<meta name="description">` with Bulgarian description "АТИСТАТ ЕООД е строително-инвестиционна компания, базирана на 26 години професионален опит в строителство, реконструкция, развитие на проекти и инвестиции в имоти."; `<meta property="og:title">` with value "АТИСТАТ — строителство и инвестиции"; `<meta property="og:description">` with the Bulgarian description above; `<meta property="og:type" content="website">`; `<meta property="og:url" content="https://atistat.com/">`; `<meta property="og:image" content="https://atistat.com/wp-content/uploads/2026/07/elemag-1-1100x851.webp">`; `<meta name="twitter:card" content="summary_large_image">`; `<meta name="twitter:title">` with the BG title; `<meta name="twitter:description">` with the BG description; `<meta name="twitter:image">` with the same image URL. Also insert a `<script type="application/ld+json">` block containing an Organization schema with `@type` "Organization", `name` "ATISTAT EOOD", `url` "https://atistat.com", `email` "office@atistat.com", `telephone` "+359884074048", `sameAs` array containing "https://www.facebook.com/atistat" and "https://www.linkedin.com/company/atistat". The JSON must be valid, contain no comments, no trailing commas, and no additional schema properties beyond those listed.

In `index.html`, at the end of `<main>` (before `</main>`), add the native `<dialog id="selected-projects-dialog" aria-labelledby="selected-projects-title">` element. The dialog must contain: a sticky header band with a close button (`aria-label="Затвори"`) and an `<h2 id="selected-projects-title">Избрани проекти</h2>` heading; a scrollable content area containing seven project gallery groups, each with an `<h3>` group heading and a `.at-gallery-grid` grid. Group order and image counts: (1) "Апартамент Аркадия" — 6 images; (2) "Апартамент Луй Айер" — 4 images; (3) "Обект Бебелан" — 6 images; (4) "Елемаг" — 4 images; (5) "Монтеканал" — 4 images; (6) "ОББ Интерлийз" — 4 images; (7) "Пауър Пропъртис" — 4 images. Each image uses its filename from `wp-content/uploads/2026/07/` with an appropriate Bulgarian `alt` text. Images use `loading="lazy"` and explicit `width` and `height` attributes matching the source file dimensions (portrait: 1086×1448; landscape: 1448×1086 for PNGs; 1800×1200 or 1800×1351 for JPEGs). The dialog must have `aria-modal="true"`.

### Task 6: Apply all changes to `index-bg.html`

#### Intent
Apply all in-scope improvements to `index-bg.html`, identical to Task 5 except no removals (stats, testimonials, social block, FAB are absent) and no inline CSS changes; additionally add Why ATISTAT and FAQ sections in Bulgarian.

#### Procedure

In `index-bg.html`, replace the text content of `<p class="at-hero__sub">` with the exact approved Bulgarian string from Task 5.

In `index-bg.html`, apply the Elemag hero sketch caption update (three spans + BG aria-label) identical to Task 5.

In `index-bg.html`, apply the Montekanal about sketch caption update (three spans + BG aria-label) identical to Task 5.

In `index-bg.html`, update the desktop timeline to nine entries using the same content, attribute renumbering, multi-line descriptions, and ATISTAT entry as in Task 5. Update `--active-progress` to `37.5%`.

In `index-bg.html`, update the mobile timeline to nine entries identically to Task 5.

In `index-bg.html`, add the Selected Projects trigger button in `section#opit` after `.at-opit__head` with the same Bulgarian label as Task 5.

In `index-bg.html`, check for an existing WhatsApp contact list item in `section#contacts`. If absent, add it using `href="https://wa.me/359884074048"`, `target="_blank" rel="noopener noreferrer"`, and visible label "WhatsApp: +359 884 074 048". Add the Viber contact list item adjacent to WhatsApp (always add if absent): `href="viber://chat?number=359884074048"`, visible label "Viber: +359 884 074 048", and `title` and `aria-label` both equal to "Свързване с нас чрез Viber".

In `index-bg.html`, insert the `.at-contact-cta` section with Bulgarian content identical to Task 5, immediately after `section#services`.

In `index-bg.html`, insert the `.at-partners` section with Bulgarian content identical to Task 5, immediately after `.at-contact-cta`.

In `index-bg.html`, insert the Why ATISTAT section (`section#why`) with the four BG differentiation cards. Translate all four card headings and body text faithfully from `index.html`'s existing Why ATISTAT section. The four card headings are "Интегриран подход", "Доказан опит", "Отговорност към бюджета и срока", and "Признат от индустрията". Preserve all factual content exactly as written in `index.html`, including the Engineering Systems award attribution in the "Признат от индустрията" card.

In `index-bg.html`, insert the FAQ section with all five BG question-and-answer items. Copy the exact content from the FAQ section in `index.html` using native `<details>/<summary>` elements. Do not add JS-controlled disclosure.

In `index-bg.html`, insert the `<ul class="at-svc__features">` with Bulgarian bullet text before each `<ol class="at-svc__steps">` in all four `article.at-svc` elements, using the exact same bullet text as Task 5.

In `index-bg.html`, update the language switcher ARIA: add `aria-current="page"` and `is-active` class to the BG anchor; remove from EN anchor.

In `index-bg.html`, add `href="#partners"` navigation link with Bulgarian label to the main site navigation.

In `index-bg.html`, add SEO metadata to `<head>`: same BG meta description and OG/Twitter/JSON-LD as Task 5, but with `og:url` value "https://atistat.com/index-bg.html".

In `index-bg.html`, add the Selected Projects Bulgarian dialog at end of `<main>`, identical structure to Task 5.

### Task 7: Apply all changes to `index.html?lang=bg.html`

#### Intent
Apply all the same Bulgarian changes as Task 6 to `index.html?lang=bg.html`, with only the og:url and language switcher EN link differing.

#### Procedure

Apply all content changes from Task 6 to `index.html?lang=bg.html`.

In `index.html?lang=bg.html`, use `og:url` value "https://atistat.com/?lang=bg" in the Open Graph tag.

In `index.html?lang=bg.html`, ensure the language switcher EN anchor points to `index.html?lang=en.html`.

### Task 8: Apply all changes to `index-en.html`

#### Intent
Apply all in-scope improvements to `index-en.html` in English, including faithful translations of Why ATISTAT and FAQ.

#### Procedure

In `index-en.html`, replace the text content of `<p class="at-hero__sub">` with the exact approved English string: "A construction and investment company built on 26 years of professional experience. We deliver projects responsibly, with quality and a long-term vision — from concept to turnkey completion."

In `index-en.html`, in `.at-hero__art .at-sketch__cap`, replace the single-line caption with three child span elements: (1) "Project Elemag", (2) "Construction", (3) "Expertise with Engineering Systems". Add `aria-label="Project Elemag, construction, expertise with Engineering Systems"` to the parent `.at-sketch` anchor.

In `index-en.html`, in `.at-about__art .at-sketch__cap`, replace the single-line caption with three child span elements: (1) "Project Montekanal", (2) "Construction", (3) "Expertise with Engineering Systems". Add `aria-label="Project Montekanal, construction, expertise with Engineering Systems"` to the parent `.at-sketch` anchor.

In `index-en.html`, update the desktop timeline to nine entries with English translations: entry types translate as — "Project management company", "Office building", "Construction company", "Residential building", "Logistics building", "Construction and investment company", "School building"; description lines translate as — "Management and control of a construction and investment project", "Finishing works", "Construction", "Experience with Correct Project", "Experience with Engineering Systems". Entry names remain as-is (they are proper nouns). The ATISTAT entry has no description paragraph (omit `<p class="at-tlpanel__desc">`). Update `--active-progress` to `37.5%` and renumber entries identically to Task 5.

In `index-en.html`, update the mobile timeline to nine entries identically to the desktop update above.

In `index-en.html`, add the Selected Projects trigger button in `section#opit` after `.at-opit__head` with English label (translate "Разгледайте избрани проекти" faithfully).

In `index-en.html`, check for existing WhatsApp and add if absent. Add Viber contact entry: `href="viber://chat?number=359884074048"`, visible label "Viber: +359 884 074 048", `title` and `aria-label` both equal to "Contact us via Viber".

In `index-en.html`, insert `.at-contact-cta` section with English content after `section#services`: heading `<h2 id="contact-cta-title">Contact Us</h2>`, sub-line "+359 884 074 048 · office@atistat.com", four buttons with EN `aria-label` values — phone "Call ATISTAT", email "Email ATISTAT", WhatsApp "Contact ATISTAT via WhatsApp", Viber `aria-label` and `title` "Contact ATISTAT via Viber".

In `index-en.html`, insert `.at-partners` section with EN content after `.at-contact-cta`: heading "PARTNERS", same three partner links and images as Task 5 with English accessible names for each partner link.

In `index-en.html`, insert the Why ATISTAT section with English translations of all four BG cards from `index.html`. Card heading translations: "Интегриран подход" → "Integrated Approach"; "Доказан опит" → "Proven Expertise"; "Отговорност към бюджета и срока" → "Budget and Schedule Responsibility"; "Признат от индустрията" → "Industry-Recognised". Translate all card body text faithfully from Bulgarian, preserving the Engineering Systems award attribution in the "Industry-Recognised" card exactly as stated in the original.

In `index-en.html`, insert the FAQ section with English translations of all five FAQ items. Use native `<details>/<summary>` elements. Translate all questions and answers faithfully from the Bulgarian FAQ in `index.html`. Do not add or remove any factual claim.

In `index-en.html`, insert `<ul class="at-svc__features">` with English bullet text before each `<ol class="at-svc__steps">` in all four `article.at-svc` elements: residential construction — "End-to-end construction organisation", "Control of schedule, budget, and quality", "Completion and commissioning"; renovation and reconstruction — "Building survey", "Resource and schedule planning", "Renovation delivery and handover"; project development — "Land or property analysis", "Concept development and coordination", "Financial and time model"; real-estate investment — "Market analysis and due diligence", "Assessment of cost, risk, and potential", "Investment structuring and performance tracking".

In `index-en.html`, update the language switcher: add `aria-current="page"` and `is-active` class to the EN anchor; remove from BG anchor.

In `index-en.html`, add `href="#partners"` navigation link with English label to the main site navigation.

In `index-en.html`, add SEO metadata to `<head>`: `<meta name="description">` with English description "ATISTAT EOOD is a construction and investment company built on 26 years of professional experience in construction, reconstruction, project development, and real-estate investment."; OG title "ATISTAT — Construction and Investment"; OG/Twitter description same as meta description; OG type "website"; `og:url` "https://atistat.com/index-en.html"; OG and Twitter image "https://atistat.com/wp-content/uploads/2026/07/elemag-1-1100x851.webp"; Twitter card "summary_large_image"; JSON-LD Organization schema identical to Task 5.

In `index-en.html`, add the Selected Projects English dialog at end of `<main>`: same structure as Task 5 but with English group headings and English alt text on images. Group heading translations: "Апартамент Аркадия" → "Apartment Arkadia"; "Апартамент Луй Айер" → "Apartment Louis Ayer"; "Обект Бебелан" → "Bebelan Project"; "Елемаг" → "Elemag"; "Монтеканал" → "Montekanal"; "ОББ Интерлийз" → "UBB Interlease"; "Пауър Пропъртис" → "Power Properties". Dialog heading: `<h2 id="selected-projects-title">Selected Projects</h2>`. Close button `aria-label="Close"`.

### Task 9: Apply all changes to `index.html?lang=en.html`

#### Intent
Apply all the same English changes as Task 8 to `index.html?lang=en.html`, with only the og:url and language switcher BG link differing.

#### Procedure

Apply all content changes from Task 8 to `index.html?lang=en.html`.

In `index.html?lang=en.html`, use `og:url` value "https://atistat.com/?lang=en" in the Open Graph tag.

In `index.html?lang=en.html`, ensure the language switcher BG anchor points to `index.html?lang=bg.html`.

### Task 10: Post-implementation verification

#### Intent
Verify byte equality of versioned asset pairs and validate all 30 acceptance criteria across the five homepage documents.

#### Procedure

Run `cmp wp-content/themes/atistat/assets/css/main.css "wp-content/themes/atistat/assets/css/main.css?ver=1784544801.css"` and confirm output is empty (files are identical).

Run `cmp wp-content/themes/atistat/assets/js/main.js "wp-content/themes/atistat/assets/js/main.js?ver=1784544801"` and confirm output is empty.

For each of the five homepage documents, open or validate the HTML and confirm: hero sub-text matches the approved string; both sketch captions have three child spans; timeline has exactly nine entries; `--active-progress` is set to `37.5%`; the trigger button is present in `section#opit`; the dialog exists with seven `<h3>` group headings; the contact list contains both WhatsApp and Viber entries; the CTA section exists after services; the Partners section exists after CTA; each service card has `.at-svc__features` before `.at-svc__steps`; Why ATISTAT and FAQ sections are present; language switcher active option has `aria-current="page"` and `is-active`; SEO meta tags and JSON-LD are present in `<head>`; no horizontal overflow exists at viewport widths 320, 375, 768, 1024, and 1440px; navigation contains a `#partners` link.

For `index.html` specifically, confirm: `section#stats` is absent; `section#testimonials` is absent; "Следете ни" block is absent; `.at-whatsapp-fab` is absent; inline style block contains no `.at-stats`, `.at-testimonials`, `.at-social-links`, `.at-social-link`, or `.at-whatsapp-fab` selectors; `.at-viber-fab` is present.

### Task 11: Update context.md

#### Intent
Bring `.aib_memory/context.md` in line with the completed implementation by updating the Solution section to reflect the nine-entry timeline and new components, removing resolved Issues, and registering the gallery image payload as a known accepted limitation.

#### Procedure

Run the following `edit-context.py` commands against `.aib_memory/context.md`. Each command must be run from the workspace root.

Delete the outdated eight-milestones statement from Solution:
`python .aib_brain/tools/edit-context.py --workspace . --operation delete --area Solution --text "Homepage timeline renders accessible desktop tabs and alternate stacked mobile cards from the same eight company and project milestones."`

Insert the updated nine-milestones statement:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Homepage timeline renders accessible desktop tabs and alternate stacked mobile cards from the same nine company and project milestones."`

Insert experience attribution note:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Experience attribution secondary lines appear in the desktop panels and mobile cards of four timeline project entries (EOS Matrix, Montekanal, Elemag, and UBB Interlease) identifying the associated partner company."`

Insert service features note:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Service feature bullet lists with green checkmark style appear before the numbered process steps in each of the four service article elements across all five homepage documents."`

Insert SEO metadata note:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "All five homepage documents carry meta description, Open Graph, Twitter Card, and JSON-LD Organization structured-data blocks with language-appropriate values and per-document canonical og:url."`

Insert language switcher note:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Language switcher option controls meet 44×44 CSS pixel tap target size and mark the active language with aria-current=page and a non-color-only green background chip indicator."`

Insert sketch captions note:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Elemag hero sketch and Montekanal about sketch captions each use a three-span vertical structure (label, project name, service classification) with an aria-label on the parent anchor element."`

Insert navigation note:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Main site navigation includes a Partners section anchor link across all five homepage documents."`

Delete the resolved SEO metadata missing issue:
`python .aib_brain/tools/edit-context.py --workspace . --operation delete --area Issues --text "Primary and English homepages lack meta descriptions, Open Graph data, Twitter Card data, and structured organization metadata. Source: index.html and index-en.html contain none of these markers."`

Delete the resolved EN parity issue:
`python .aib_brain/tools/edit-context.py --workspace . --operation delete --area Issues --text "English homepage lacks stats, testimonials, differentiation, FAQ, social links, and WhatsApp enhancements present on the primary Bulgarian homepage. Source: index-en.html has five core sections while index.html has nine sections and extra contact actions."`

Delete the resolved orphaned CSS rules issue:
`python .aib_brain/tools/edit-context.py --workspace . --operation delete --area Issues --text "Removing id=stats, id=testimonials, and the Следете ни social block leaves orphaned CSS rules in the index.html inline style block for .at-stats, .at-testimonials, and .at-social-links selectors. Source: index.html inline style block."`

Insert the gallery payload accepted trade-off:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Issues --text "Gallery image payload is approximately 52.5 MiB across 32 unoptimized PNG and JPEG files; images are lazy-loaded but users who open the Selected Projects modal on slow connections will experience significant download time; image optimization is deferred per stakeholder constraints."`

Verify context.md passes `python .aib_brain/tools/verify-context.py --workspace .` with all checks OK.
