## Overview

- Request ID: R-20260726-0836
- Request title: Prioritize evidence cards and optimize gallery mosaics

### Background

The Experience section currently places a visually dominant desktop timeline before the selected evidence card, hides chronological navigation at widths of 860px or less, and renders tiny mosaic previews from full-resolution gallery files without a persistent visual cue that the mosaics open a gallery. The developer wants the evidence cards to lead the hierarchy, chronological navigation to remain available on all supported layouts, and normal-page gallery previews to become both lightweight and obviously interactive.

### Scope

The change covers the Experience markup in `index.html`, `index-bg.html`, `index-en.html`, `index.html?lang=bg.html`, `index.html?lang=en.html`, and `opit/index.html`; both copies of the shared CSS and JavaScript; 28 new local 160×160 WebP mosaic thumbnails; static regression tests and the browser interaction fixture; and the corresponding current-state and residual-risk entries in `.aib_memory/context.md`. It includes desktop hierarchy and scale changes, a 13-marker responsive navigation rail, localized gallery affordances, thumbnail substitution only in mosaic launchers, cross-route and shared-asset parity, reduced-motion handling, keyboard and focus behavior, and 320px overflow protection.

### Out of scope

The request excludes optimization or replacement of full-resolution images inside the opened gallery dialog, removal of original PNG/JPEG gallery assets, evidence-card redesign or enlargement, removal or filtering of responsive stacked cards, project-detail content changes, changes to gallery-dialog behavior beyond preserving it, and changes to the 13 milestones or their chronology.

## Input Interpretation

### User Original Inputs

The developer requested that the Experience evidence card precede the timeline and receive primary visual emphasis while retaining its present dimensions, typography, content, and styling. Above 860px, each of the six Experience documents must order the selected card first, the localized “Select a stage” instruction second, and a compact desktop timeline third. The existing timeline contract must remain intact: approximately five complete milestones in view, chronological order, active-item centering, free horizontal panning, proximity snapping, edge fades, Previous/Next controls, keyboard operation, automatic tab semantics, and accessible selected/hidden state. Desktop milestone images must become approximately 35–40% shorter, the `2000` and `2026` endpoint main-year labels must become approximately 16–20px, and their captions approximately 9–10px.

At every width of 860px or less, the section must retain all 13 existing stacked evidence cards in their current order and add a horizontally scrollable 13-marker rail between the Experience introduction and those cards. Each marker must reuse the corresponding card image and show its year; approximately three complete markers must fit in view. The responsive rail must omit the desktop endpoint blocks and separate Previous/Next buttons, support touch and trackpad panning, visible edge fades, and keyboard-arrow navigation, and navigate to and focus the associated card or its primary focus target. Reduced-motion users must receive immediate rather than animated card navigation, while the stacked cards must remain a functional fallback when JavaScript is unavailable.

The developer also requested one local 160×160 WebP thumbnail no larger than 20KB for each of the 28 unique images used by the seven four-image mosaics. Center crops must remain suitable for the existing square treatment; duplicated desktop and responsive mosaics must reuse the same thumbnail URLs; mosaics must no longer reference full-resolution PNG/JPEG sources; the dialog must continue to use the original full-resolution sources; and existing lazy loading and asynchronous decoding must be preserved where applicable.

Every mosaic must remain one native button with its existing accessible name and gain a persistent translucent overlay, a recognizable local or inline gallery icon, localized visible text (`Виж галерия` in the three Bulgarian documents and `View gallery` in the three English documents, including `opit/index.html`), clear hover and keyboard-focus feedback, and an overlay that does not intercept pointer input. Native dialog project filtering, opening, closing, keyboard accessibility, and invoker-focus restoration must remain unchanged.

The six HTML documents must remain synchronized with correct `../` asset prefixes in `opit/index.html`; the two shared CSS files and two shared JavaScript files must remain byte-identical; all documents must retain the same 13-milestone data and interaction contract; and the Python static checks and Chrome interaction fixture must be updated. No third-party JavaScript, package dependency, or build pipeline may be introduced. New assets must remain local, JavaScript-free content must remain visible and operable wherever practical, reduced-motion preferences must continue to be honored, and the page must not overflow horizontally at 320px.

The developer assumes that 860px remains the desktop/responsive boundary, the existing timeline and gallery data are authoritative, responsive marker images can reuse browser-cached card image URLs, and thumbnail optimization addresses normal-page loading while the full-dialog payload remains a known separate risk. Completion requires deterministic checks for route order, milestone and card counts, desktop and responsive viewport density, responsive navigation and focus, reduced motion, thumbnail format/dimensions/size/source replacement, localized overlays, dialog preservation, 320px overflow, shared-asset byte identity, passing static tests, passing Chrome smoke tests, and no first-party console errors.

In the follow-up questionnaire, the developer selected static navigation links enhanced by JavaScript for the responsive rail, Arrow-key focus movement with Enter/Space or click activation, card-container focus after activation, persistence of the last explicitly selected marker during ordinary page scrolling, and one dedicated `gallery-thumbnails` directory for all 28 preview assets.

### AI interpretation

The implementation must treat the selected desktop `tabpanel` as the primary Experience artifact by moving the entire panel container before the hint and stage without modifying panel content or card styling. CSS must reduce only the desktop rail’s vertical prominence and endpoint typography while preserving its five-allocation viewport and existing interaction model.

Responsive navigation must be additive: the new rail belongs above the unchanged 13-card list and must never become the sole means of accessing evidence. It must use 13 static native links in a localized navigation region so every marker remains usable without JavaScript; shared JavaScript enhances roving focus, marker centering, edge state, and explicit activation without applying tab semantics to the simultaneously visible cards. Left/Right Arrow keys move and center marker focus without changing page position, while Enter/Space or click activates the link, scrolls to and focuses the mapped card container, and persists that marker as current until another marker is explicitly activated. Stable card IDs and `tabindex="-1"` must provide a uniform destination across company, gallery-capable, and static project cards while avoiding fixed-header obstruction.

Mosaic optimization is a preview-source substitution, not a gallery-data migration. Each of the seven project mosaics has four unique source images, giving the verified total of 28 thumbnails. All preview assets must use flat ASCII project-and-sequence filenames within `wp-content/uploads/2026/07/gallery-thumbnails/`; both desktop and responsive launcher instances for a project must point to the same four thumbnail paths, while every image inside the native dialog retains its current full-resolution URL and sequence. The overlay must be child markup or non-interactive decoration inside the existing button, not a nested control.

The six documents and version-suffixed asset copies are independent static files, so implementation and tests must enforce parity rather than assume it. `opit/index.html` must use `../wp-content/...` paths. The browser fixture must exercise the responsive rail independently of the desktop rail, because the production desktop initializer currently validates exactly one 13-tab/13-panel model and the existing delegated gallery selector would otherwise be capable of treating a new `button[data-project]` marker as a gallery launcher.

## Research Results

### Workspace Pattern Scan

- All six target documents contain exactly 13 desktop `role="tab"` controls, 13 desktop `role="tabpanel"` articles, 13 responsive `<li class="at-tlcard...">` cards, and 14 `.at-gallery-mosaic` buttons: seven desktop launchers plus the same seven projects in responsive cards.
- The current desktop DOM order is `.at-tl-hint`, `.at-tl-stage`, then `.at-tl-panels`; this directly conflicts with the requested card-first order.
- `wp-content/themes/atistat/assets/css/main.css` allocates five desktop controls with `--timeline-control-width: calc(20% - .8rem)` and uses horizontal overflow, proximity snapping, free touch panning, edge fades, and enhancement-gated scrollbar hiding. Those mechanics can remain while the marker-height clamps and stage geometry are reduced.
- Current desktop marker image heights range from `clamp(160px, 16vw, 230px)` to `clamp(205px, 20vw, 285px)`. The requested 35–40% reduction implies new values near 60–65% of each current minimum/fluid/maximum value, including the default applied to markers seven through thirteen.
- Current endpoint main-year text uses `clamp(1.45rem, 2.5vw, 2.15rem)`, which can exceed the requested 16–20px range, while the `.66rem` caption is approximately 10.56px at a 16px root and should be tightened to the requested 9–10px range.
- At `max-width: 860px`, CSS currently hides `.at-tl-desktop` and exposes only the stacked `.at-tl-mobile` grid. No responsive milestone rail or responsive-rail JavaScript exists.
- The two CSS files are byte-identical and the canonical and version-suffixed JavaScript files are byte-identical. Both pairs must be edited in lockstep and verified byte-for-byte.
- `.at-gallery-mosaic` currently renders a bare 2×2 grid of 56px images with only `cursor: pointer` and a focus outline. It has no persistent text/icon affordance and no hover treatment.
- The mosaic launcher markup across all six documents collectively references exactly 28 unique full-resolution PNG/JPEG sources. The same sources remain separately referenced in seven `.at-gallery-project` containers inside each native dialog.
- The gallery initializer currently delegates to `button[data-project]:not(.at-tlb)`. A responsive marker implemented as a `button` with `data-project` would be misidentified as a gallery launcher unless markers avoid that attribute shape or the selector is narrowed to `.at-gallery-mosaic[data-project]`.
- The desktop timeline initializer calls `document.querySelector("[data-timeline]")`, validates exactly 13 tabs and reciprocal panels, manages roving `tabindex`, automatically activates on focus, centers the active marker, toggles edge state, and honors reduced motion during centering. The responsive rail therefore needs distinct selectors and state rather than overloading the desktop data attributes.
- `tests/test_static_site.py` already enforces six-route tab/panel parity, localized desktop control labels, approximately five-item CSS geometry, progressive enhancement, shared-asset identity, dialog project count, and representative Chrome execution. It does not collect responsive marker metadata, validate card mappings, inspect mosaic preview sources or dimensions, verify overlay localization, or test 320px layout overflow.
- `tests/fixtures/interaction_harness.html` exercises desktop click/focus/Arrow/Home/End behavior, boundary controls, edge state, reduced-motion centering, Arcadia styling, gallery filtering, close focus, and invoker-focus restoration. It has no responsive rail fixture or responsive navigation/focus assertions.
- Pillow 10.2.0 with WebP support is available in the current environment; ImageMagick and `cwebp` are not available. Pillow can perform deterministic `ImageOps.fit(..., (160, 160), centering=(0.5, 0.5))` center crops and quality adjustment without adding a runtime site dependency, but the desired asset organization must be confirmed.

### Industry Knowledge

- The WAI-ARIA Authoring Practices [Tabs Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/tabs/) supports the existing desktop automatic-activation behavior because all panels are already present and can display without network latency. It also states that horizontal tablists should leave Up/Down Arrow behavior to normal page scrolling. Applicability: preserve the desktop Left/Right/Home/End contract, but do not automatically label the responsive rail as tabs unless its all-visible-card behavior is intentionally defined as a tab relationship.
- The MDN [CSS scroll snap guide](https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Scroll_snap) identifies horizontal overflow, child snap positions, and scroll padding/margins as the native building blocks for pannable rails. Applicability: reuse proximity snapping for approximately three markers, keep `overflow-x: auto` and touch panning, and add destination `scroll-margin` so focused cards remain visible below the fixed header.
- The web.dev [responsive images guidance](https://web.dev/learn/design/responsive-images) emphasizes serving images sized for their rendered use and retaining lazy loading for below-fold assets. Applicability: replacing 56px mosaic preview sources with bounded 160px WebPs addresses transfer and decode cost at the source; lazy loading alone is not an adequate substitute, and dialog sources remain lazy and full resolution by explicit scope.
- WCAG 2.2 [Focus Visible guidance](https://www.w3.org/WAI/WCAG22/Understanding/focus-visible) requires keyboard users to perceive the focused component. Applicability: both responsive markers and mosaic buttons need non-color-only `:focus-visible` treatment, and programmatically focused evidence destinations need a visible but non-persistent focus style.
- WAI guidance for [On Focus](https://www.w3.org/WAI/WCAG22/Understanding/on-focus) warns against unexpected context changes merely from moving focus. Applicability: the choice between arrow-key focus-only navigation and automatic scroll-to-card behavior is a consequential interaction decision that should be confirmed rather than inferred.

### AI Agent critique

- The opening prose in `.aib_memory/input.md` contains unclear wording and typographical errors (“preveils,” “evindence”), but the subsequent Goal, Scope, requirements, and Success Criteria sections remove the functional ambiguity.
- The request uses “approximately five” and “approximately three” visible markers without naming representative viewport widths or an allowed partial-marker tolerance; automated checks should therefore assert the CSS allocation formulas and use representative browser widths rather than brittle pixel-perfect screenshots.
- The resolved card-container focus policy is uniform across all 13 cards, but programmatically focusable `<li>` elements require restrained focus styling and must not enter the sequential tab order.
- The resolved manual activation model separates marker focus from persistent current state; tests must prove that Arrow keys do not move the page and that Enter/Space or click performs exactly one card navigation.
- The resolved activation-only synchronization avoids observer complexity, but the persistent marker may intentionally differ from the card currently visible after ordinary page scrolling.
- Static responsive navigation links preserve useful no-JavaScript navigation, but they duplicate 13 marker records across six documents and therefore increase the importance of route-parity tests.
- The dedicated `wp-content/uploads/2026/07/gallery-thumbnails/` directory makes the 28-asset boundary auditable, but every root and `opit/index.html` hard-coded path must retain the correct relative prefix.
- Reusing card image URLs for the new responsive rail can cause the browser to request some timeline imagery earlier than before because the rail appears above the card list. Lazy loading and cache reuse mitigate this, but the rail itself has no image-byte budget.
- A persistent overlay can be added without changing evidence-card dimensions only if it is absolutely positioned within the existing mosaic footprint. Adding it to normal flow would enlarge the card or disturb the three-column desktop panel layout.
- `index.html` and the other five static copies duplicate both Experience and dialog markup extensively; the requested change necessarily increases drift risk unless tests validate marker datasets, localized labels, thumbnail URLs, and relative prefixes.
- `wp-content/themes/atistat/assets/css/main.css` still describes the 860px behavior as replacing the desktop timeline with stacked cards; that comment and the rules must be updated together when the rail is added.
- The current CSS hides desktop scrollbars only after successful enhancement, which is a useful fallback invariant that should be mirrored for the new responsive rail rather than hiding its scrollbar unconditionally.
- `wp-content/themes/atistat/assets/js/main.js` has a gallery launcher resolver broader than `.at-gallery-mosaic`; this is safe with current markup but becomes a collision risk for project-aware responsive markers.
- The current JavaScript registers a window resize listener for the desktop rail without teardown or frame coalescing. That is acceptable for one static homepage instance, but the new responsive behavior should avoid duplicating resize work when CSS and `scrollIntoView` can provide layout-dependent positioning.
- `tests/test_static_site.py` is tied to request `R-20260725-1526` in its module docstring; after expanding the suite, that historical request reference will be stale and should be generalized or updated.
- The static parser currently counts desktop tabs/panels and links but has no collections for responsive rail controls, mapped card identifiers, overlay text, or mosaic image sources, so textual substring checks alone would be fragile.
- The existing Chrome harness is desktop-shaped and cannot validate the new ≤860px layout unless the browser helper gains a viewport option or the fixture supplies a deterministic responsive component independent of the default headless viewport.
- `.aib_memory/context.md` correctly records the full-dialog payload as a known issue, but its gallery and timeline solution entries will become stale: preview payload will be solved while the full-resolution dialog risk remains, and responsive navigation will coexist with stacked cards.
- `.aib_brain/prompts/aib-analyze.md` and `.aib_brain/conventions/analysis-convention.md` require exactly three `###` subsections under Proposed Solution while naming only two. This analysis uses `### Validation Strategy` as the necessary third subsection, consistent with the existing issue already recorded in `.aib_memory/context.md`.
- `.aib_brain/prompts/aib-analyze.md` requests a plan `## Decisions` section while `.aib_brain/conventions/plan-convention.md` permits exactly Goal, Constraints, Success criteria, and Plan. The generated plan must preserve the normative four-section structure and carry resolved decisions in `## Constraints`.

### Edge Cases

- At exactly 860px, the responsive rail and stacked cards must be shown and the desktop panels/stage hidden; at 861px, the desktop card-first panels/hint/stage layout must be shown.
- At 320px, marker widths, gaps, overlay copy, card grid collapse, focus outlines, and edge fades must remain within the viewport without causing page-level horizontal overflow.
- First and last responsive markers need correct edge-fade state even when snap padding prevents exact `scrollLeft` values of zero or maximum.
- Repeated years (`2024` and multiple `2025` milestones) require project-stable identifiers and labels rather than year-only mapping.
- Three company milestones, gallery-capable projects, and non-gallery projects have different descendants; the chosen focus destination must work for all 13 without opening links or dialogs accidentally.
- A marker click originating on its nested image or year must resolve to the marker, while the mosaic overlay must never intercept the existing launcher click.
- Arrow navigation must not allow horizontal rail keys to trigger page-horizontal scrolling, but Up/Down keys must retain normal vertical page scrolling.
- Programmatic focus after `scrollIntoView` must not cause a second conflicting browser scroll; `preventScroll` and explicit ordering may be needed.
- Reduced-motion changes can occur at runtime; checking the media query at activation time, as the desktop code does, avoids caching a stale preference.
- JavaScript failure before responsive enhancement must leave the 13 stacked cards visible and in their original order; any enhancement-only scrollbar hiding or state styling must remain gated.
- A missing or malformed marker-to-card mapping must fail safely without hiding content or hijacking gallery behavior.
- Thumbnail output may exceed 20KB at an initial quality setting; generation must lower quality deterministically until the ceiling is met and fail if the dimension, format, or byte invariant still cannot be satisfied.
- EXIF orientation must be normalized before center cropping so portrait source images crop the intended visual orientation.
- Transparent PNG sources converted to WebP need a deliberate alpha/background treatment that does not introduce dark or transparent checker artifacts in the mosaic.
- URL-encoded Cyrillic source paths and the `../` prefix in `opit/index.html` must not be confused with filesystem paths during thumbnail generation or HTML substitution.
- Duplicate desktop/mobile preview instances must not generate duplicate thumbnails; all 84 launchers across six documents must reuse the same 28 asset URLs.
- Full-resolution dialog sources must be excluded from bulk preview substitution even when an identical URL appears in both a mosaic and a dialog.
- The native dialog must still restore focus to the exact desktop or responsive mosaic invoker after the overlay is added.
- Re-running analysis after answers must retain the complete Input Interpretation while updating only the five affected decisions and solution fields.

### Requirements Gate Evaluation

Requirements Gate: 8/8 PASS — all categories satisfied.

## Proposed Solution

### High-Level Concept

Reorder and visually compact the desktop Experience timeline around the unchanged selected card, add a dedicated three-up responsive milestone rail above the unchanged card list, and replace only mosaic preview sources with tightly bounded WebP thumbnails plus a localized overlay. Keep data, dialog sources, static architecture, localization, progressive fallback, and shared-asset parity intact, with tests enforcing the expanded contract.

### Execution Steps

#### Task 1: Generate bounded mosaic thumbnails

- `python`: Read the 28 verified unique mosaic source images, normalize orientation, center-crop each to 160×160, encode WebP under 20KB with deterministic quality fallback, and write one reusable local thumbnail per source to `wp-content/uploads/2026/07/gallery-thumbnails/` with flat ASCII project-and-sequence filenames.
- `python`: Verify that the generated set contains exactly 28 WebP files, every file reports 160×160 dimensions, every file is at most 20KB, and no two intended source mappings collide.

#### Task 2: Restructure and localize all six Experience documents

- `index.html`: Move `.at-tl-panels` before the Bulgarian hint and compact stage; insert the 13-marker responsive rail above the existing 13-card list; add stable card mappings/focus destinations; replace the 28 desktop/mobile mosaic preview references with the shared thumbnail URLs; and add the non-interactive inline gallery icon plus `Виж галерия` overlay within every existing mosaic button.
- `index-bg.html`: Apply the same Bulgarian desktop order, responsive dataset, card mappings, thumbnail references, and `Виж галерия` overlay while preserving this document’s existing content.
- `index-en.html`: Apply the same desktop order, responsive dataset, card mappings, thumbnail references, and English `View gallery` overlay while preserving this document’s existing content.
- `index.html?lang=bg.html`: Apply the same Bulgarian desktop order, responsive dataset, card mappings, thumbnail references, and `Виж галерия` overlay while preserving the query-route document.
- `index.html?lang=en.html`: Apply the same English desktop order, responsive dataset, card mappings, thumbnail references, and `View gallery` overlay while preserving the query-route document.
- `opit/index.html`: Apply the same English desktop order, responsive dataset, card mappings, thumbnail references, and `View gallery` overlay using correct `../wp-content/...` paths.
  - Cross-file invariant: all six documents retain identical milestone order, year/image associations, card content, project keys, thumbnail-to-source mapping, dialog image membership, and dialog source order; only language and relative path prefixes vary.

#### Task 3: Compact the timeline and style responsive/gallery affordances

- `wp-content/themes/atistat/assets/css/main.css`: Reduce all desktop marker height clamps by approximately 35–40%, reduce endpoint year/caption typography into the requested ranges, support the panel-first desktop order without changing panel dimensions, add the responsive three-marker scroll rail with proximity snapping and enhancement-gated edge/scrollbar states, add safe focus and scroll-margin treatment for mapped cards, and implement the persistent translucent mosaic overlay with hover/focus feedback and `pointer-events: none`.
- `wp-content/themes/atistat/assets/css/main.css?ver=1784544801.css`: Mirror the canonical CSS byte-for-byte.

#### Task 4: Add isolated responsive rail behavior

- `wp-content/themes/atistat/assets/js/main.js`: Enhance the static responsive links with validation, roving focus, focus-only Left/Right Arrow handling, marker centering, edge-state updates, explicit Enter/Space/click activation, reduced-motion-aware scrolling to and focus of the mapped card container, and activation-only persistent current state; keep the desktop initializer unchanged in contract; and narrow gallery launcher resolution to `.at-gallery-mosaic[data-project]`.
- `wp-content/themes/atistat/assets/js/main.js?ver=1784544801`: Mirror the canonical JavaScript byte-for-byte.

#### Task 5: Expand static and browser regression coverage

- `tests/test_static_site.py`: Parse and assert the six-route card-first desktop order, 13 responsive markers and 13 mapped cards, localized overlay text/icon presence, 28 shared thumbnail URLs, WebP format/dimensions/byte ceiling, absence of PNG/JPEG mosaic previews, preservation of full-resolution dialog URLs and image order, `opit/index.html` prefixes, responsive CSS contract, narrowed gallery selector, and byte-identical CSS/JavaScript pairs.
- `tests/fixtures/interaction_harness.html`: Add a responsive rail/card fixture and assertions for panning overflow, Arrow-key behavior, edge fades, destination scrolling/focus, reduced-motion immediacy, marker state, 320px overflow, overlay click-through, dialog opening/closing, and invoker-focus restoration.
- `PYTHONDONTWRITEBYTECODE=1 python -m unittest discover -s tests -p 'test_*.py' -v`: Run all static and Chrome smoke checks and expect a successful unittest summary with no failures, errors, or first-party console diagnostics.

#### Task 6: Update durable product context

- `.aib_memory/context.md`: Replace the current desktop-only timeline solution statement with the card-first compact desktop and 13-marker responsive-rail architecture, extend the gallery launcher statement with bounded preview thumbnails and localized overlay behavior, and revise the gallery payload issue so it names only the remaining full-resolution dialog cost.
- `.aib_memory/context.md`: Add unresolved manual cross-browser/physical-touch coverage and responsive-marker image eagerness, if still applicable after implementation, to `## Issues` while preserving all unrelated issue entries.

### Validation Strategy

Validation must combine deterministic file inspection with the existing headless Chrome harness. Static tests should prove exact counts, localized values, URL mappings, asset metadata, forbidden full-resolution preview references, dialog preservation, route parity, and shared-copy identity. Browser checks should exercise both desktop and responsive interaction paths at representative widths above 860px, at 860px, and at 320px; confirm focus/current state, scrolling, reduced motion, edge fades, overlay click-through, dialog focus restoration, and lack of horizontal overflow; and finish without first-party console errors. Physical wide-touch and non-Chromium scrollbar rendering remain manual verification gaps unless additional browsers are made available.

## Context Update Analysis

- Context Elements to Add:
  - Add a `## Solution` statement that all six Experience documents present the unchanged selected evidence panel before the localized hint and a compact five-allocation desktop rail above 860px.
  - Add a `## Solution` statement that widths of 860px or less expose a 13-marker, approximately three-up horizontal rail above the unchanged 13-card list, with the resolved mapping, keyboard, focus, edge, and reduced-motion behavior.
  - Add a `## Solution` statement that every desktop/responsive mosaic uses one of 28 reusable 160×160 WebP thumbnails no larger than 20KB and a persistent localized gallery overlay while the native dialog retains full-resolution sources.
  - Add an `## Issues` statement for any responsive-marker eager-loading risk that remains after final markup selection and retain the existing manual cross-browser/wide-touch verification limitation.

- Context Elements to Modify/Remove:
  - Replace the existing `## Solution` entry beginning “Across index.html, index-bg.html…” because it currently describes the continuous desktop rail and ends only with “the stacked mobile cards remain in use”; the replacement must retain all desktop navigation invariants and add the new card-first order and responsive rail.
  - Replace the existing `## Solution` entry beginning “Gallery-eligible projects…” so it also records the thumbnail preview and localized affordance contract without changing its native-dialog filtering and focus-restoration intent.
  - Replace the existing `## Issues` entry beginning “Gallery image payload is approximately 52.5 MiB…” so it no longer implies that normal-page mosaic previews load the full-resolution sources; it must continue to record the deferred full-dialog payload.

- Conflict Resolution & Intent Preservation:
  - The existing solution says the stacked mobile cards remain in use. The new rail must be additive and placed above those same 13 cards, preserving their order, content, gallery launchers, and JavaScript-free accessibility rather than replacing or filtering them.
  - The existing gallery solution and issue preserve project-scoped dialog membership and full-resolution evidence. Thumbnail substitution must be limited to `.at-gallery-mosaic` descendants so the dialog retains image quality, source order, filtering, close behavior, and invoker-focus restoration.
  - The existing desktop timeline entry preserves approximately five milestones, panning, snapping, centering, controls, fades, and automatic tabs. Reordering and vertical compaction must preserve those behaviors and change only hierarchy and marker/endpoint prominence.
  - The existing static-export architecture duplicates six HTML documents and paired assets. The implementation must preserve that architecture and mitigate drift through exact parity assertions and byte-identity checks, not introduce a runtime framework or build pipeline.

## Decision Register

### Decision Points

#### Decision Point: Responsive rail construction and semantics

- Applies to: Task 2 responsive markup and Task 4 initialization; the cards remain simultaneously visible, so multiple valid control structures exist and produce different fallback, semantics, and maintenance outcomes.
- Tag: resolved-by-user
- Rationale/Resolution: In Q001, the developer selected `Option A: Static navigation links enhanced by JavaScript`.
- Alternative: Static navigation links enhanced by JavaScript.
  - Description: Add a localized list/navigation region containing 13 native anchors mapped to card IDs, then enhance focus, edge, and scroll behavior in JavaScript.
  - Trade-offs: The rail remains useful without JavaScript and has native destinations, but it adds substantial duplicated markup to all six static documents.
  - Expected codebase impact: Six HTML rail copies, card IDs, responsive CSS, a dedicated JavaScript enhancer, and parity tests.

#### Decision Point: Responsive marker activation model

- Applies to: Task 4 keyboard and focus behavior; Arrow keys can either navigate only within the rail or immediately move the page to evidence.
- Tag: resolved-by-user
- Rationale/Resolution: In Q002, the developer selected `Option A: Focus with arrows, activate with Enter/Space or click`.
- Alternative: Focus with arrows, activate with Enter/Space or click.
  - Description: Arrow keys move roving focus and center the marker; explicit activation scrolls and focuses the destination.
  - Trade-offs: It avoids repeated page jumps while exploring markers and aligns activation with native controls, but keyboard users need one additional keystroke.
  - Expected codebase impact: Separate focused and selected states, key handlers for arrows plus activation, and fixture assertions for both.

#### Decision Point: Responsive card focus destination

- Applies to: Task 2 card attributes, Task 3 focus styling, and Task 4 activation; the 13 cards do not share one existing native focus target.
- Tag: resolved-by-user
- Rationale/Resolution: In Q003, the developer selected `Option A: Focus the card container`.
- Alternative: Focus the card container.
  - Description: Give every mapped `<li>` a stable ID and `tabindex="-1"` and focus the card itself after scrolling.
  - Trade-offs: Behavior is uniform across all 13 cards and announces the full evidence region, but it introduces programmatically focusable non-interactive containers.
  - Expected codebase impact: IDs/tabindex on 78 card instances, a shared destination-focus style, and consistent JavaScript mapping.

#### Decision Point: Responsive marker state synchronization

- Applies to: Task 3 marker styling and Task 4 state management; ordinary vertical card scrolling may or may not update the rail’s selected/current marker.
- Tag: resolved-by-user
- Rationale/Resolution: In Q004, the developer selected `Option A: Persist the last explicitly selected marker`.
- Alternative: Persist the last explicitly selected marker.
  - Description: Update current/selected state only after rail activation and leave it unchanged during ordinary page scrolling.
  - Trade-offs: State is simple and deterministic, but it may no longer correspond to the card currently in view after manual scrolling.
  - Expected codebase impact: Activation-only state updates, no observer, and simpler tests.

#### Decision Point: Thumbnail asset organization

- Applies to: Task 1 asset output paths and Task 2 hard-coded preview URLs; 28 new files need a stable naming convention shared across six documents.
- Tag: resolved-by-user
- Rationale/Resolution: In Q005, the developer selected `Option A: One dedicated gallery-thumbnails directory`.
- Alternative: One dedicated gallery-thumbnails directory.
  - Description: Store all assets as `wp-content/uploads/2026/07/gallery-thumbnails/<project-slug>-01.webp` through `-04.webp`.
  - Trade-offs: Preview assets are clearly separated and easy to audit, but every document gains a longer path and a new directory convention.
  - Expected codebase impact: One directory with 28 files, six-route path updates, and directory-aware tests.
