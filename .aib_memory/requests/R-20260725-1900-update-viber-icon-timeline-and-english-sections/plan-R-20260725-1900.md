## Goal

Create a unified thirteen-milestone project timeline with gallery launchers for seven eligible projects, synchronize Why ATISTAT and FAQ sections across all six homepage documents with shared CSS, and replace the existing incorrect Viber icon with the official white asset on ATISTAT green across all five root homepage documents. The current homepage has a disconnected seven-set gallery, a Messenger-style Viber SVG path, and English Why/FAQ sections without source-of-truth presentation styles. Impacted components: index.html, index-bg.html, index-en.html, index.html?lang=bg.html, index.html?lang=en.html, opit/index.html, wp-content/themes/atistat/assets/css/main.css and its version-suffixed mirror, wp-content/themes/atistat/assets/js/main.js and its version-suffixed mirror, and five new static asset files in wp-content/themes/atistat/assets/images/ and wp-content/uploads/2026/07/.

## Constraints

- Static export architecture: no server-side rendering, no build pipeline, no third-party JavaScript libraries.
- All six homepage documents must remain independently valid static HTML files with their own route-specific meta, canonical URL, og:url, and navigation attributes.
- Shared CSS canonical file (main.css) and its version-suffixed mirror (main.css?ver=1784544801.css) must be byte-identical after all changes. Same constraint applies to the shared JS canonical (main.js) and its version-suffixed mirror (main.js?ver=1784544801).
- Official Viber icon asset (viber-icon.png) must not be recolored, filtered, masked, or otherwise modified from the official downloaded original. The surrounding control background color remains the current ATISTAT green.
- New timeline WebP filenames must use ASCII characters only: arcadia-timeline.webp, louis-ayer-timeline.webp, bebelan-timeline.webp, power-properties-timeline.webp.
- Gallery photograph filenames and directories must not be renamed or moved.
- Timeline milestone years are authoritative from workspace (index.html current values: EOS Matrix 2007, EMA 2015, British School of Sofia 2026, UBB Interlease 2025) and from projects.md (Apartment Louis Ayer 2024, Apartment Arcadia 2025, Bebelan 2025, Power Properties 2025). No dates may be invented.
- The Bulgarian Why/FAQ content in index.html is the source of truth. English translations must be faithful; Bulgarian business claim text must not be altered.
- Timeline project data-project identifiers must use ASCII-only, language-independent values (arcadia, louis-ayer, bebelan, power-properties, montekanal, elemag, ubb-interlease) so JavaScript mapping remains stable across Bulgarian and English documents.

## Success criteria

- All six documents render a desktop tab rail with exactly thirteen milestone controls in chronological order: Корект Проект / Correct Project (2006), ЕОС Матрикс / EOS Matrix (2007), Инженерни Системи / Engineering Systems (2008), Монтеканал / Montekanal (2011), ЕМА / EMA (2015), Елемаг / Elemag (2021), ап. Луи Айер / Apt. Louis Ayer (2024), ATISTAT (2024), ОББ Интерлийз / UBB Interlease (2025), ап. Аркадия / Apt. Arcadia (2025), Бебелан / Bebelan (2025), Пауър Пропъртис / Power Properties (2025), Британско училище в София / British School of Sofia (2026).
- The desktop timeline container has horizontal scrolling enabled; all thirteen milestone controls remain at their current size and are accessible by keyboard arrow navigation on the scrollable rail.
- The seven gallery-eligible desktop panels (Montekanal, Elemag, UBB Interlease, Apt. Louis Ayer, Apt. Arcadia, Bebelan, Power Properties) each contain a four-image mosaic gallery launcher button with a matching data-project attribute.
- The seven gallery-eligible mobile cards each contain a four-image mosaic gallery launcher button with a matching data-project attribute.
- Clicking any gallery launcher button opens the native dialog, which shows only the image set for the matching data-project container; the dialog accessible name matches the project name.
- On dialog open, focus lands on the first focusable element inside the active project container or on the close button.
- Pressing Escape or activating the close button dismisses the dialog and returns focus to the exact launcher that triggered it.
- The standalone all-project selected-projects trigger element is absent from all six documents.
- The Bulgarian Why and FAQ sections in index.html, index-bg.html, and index.html?lang=bg.html have structurally identical HTML and identical visible text content.
- The English Why and FAQ sections in index-en.html and index.html?lang=en.html have structurally equivalent HTML and contain faithful English translations of the Bulgarian source.
- All six documents load main.css from the shared stylesheet; no document-local style block containing Why/FAQ rules is present in any document.
- The official white Viber icon (viber-icon.png) is rendered on ATISTAT green background in all six Viber controls across the five root homepage documents; no SVG messenger-bolt path is present in any Viber control.
- cmp confirms the canonical and version-suffixed CSS files are byte-identical.
- cmp confirms the canonical and version-suffixed JS files are byte-identical.
- python3 -m http.server 8765 serves all six documents, shared assets, and the five new asset files without any 404 responses.
- All new CSS transitions and animations are suppressed by prefers-reduced-motion: reduce.
- python .aib_brain/tools/verify-context.py --workspace . reports 12/12 checks passed after context updates.

## Plan

### Task 1: Produce official and generated visual assets

#### Intent
Store the official white Viber icon and generate four lightweight line-art WebP timeline images for the gallery-only projects so Tasks 2–5 can reference them.

#### Procedure
Step 1: Download the official white Viber icon variant from https://www.viber.com/en/brand-center/ without any modification. Save it as `wp-content/themes/atistat/assets/images/viber-icon.png`. Confirm the file is the white icon variant (white phone-bubble mark on transparent background) and that the downloaded file has not been recolored or resized from the original.

Step 2: Generate a lightweight architectural pencil/line-art WebP image from the source file at `workdir/Избрани снимки за сайт - 24.07.2026/ап. Аркадия - интериор/Снимка 1 , интериор Аркадия.png`. Apply a monochrome/desaturated treatment with a pale cream or white background to match the visual style of the existing timeline images (visible in `wp-content/uploads/2026/07/matrix-1-799x900.webp`). Save the result as `wp-content/uploads/2026/07/arcadia-timeline.webp` at a target file size well below 100 KB.

Step 3: Generate a matching line-art WebP from `workdir/Избрани снимки за сайт - 24.07.2026/ап. Луй Айер - интериор/Снимка 1 , интериор Луи Айер.png`. Save as `wp-content/uploads/2026/07/louis-ayer-timeline.webp`.

Step 4: Generate a matching line-art WebP from `workdir/Избрани снимки за сайт - 24.07.2026/Бебелан - интериор/Снимка 1 , интериор Бебелан.jpg`. Save as `wp-content/uploads/2026/07/bebelan-timeline.webp`.

Step 5: Generate a matching line-art WebP from `workdir/Избрани снимки за сайт - 24.07.2026/Пауър Пропъртис - интериор/Снимка 1 , интериор Пауър П.png`. Save as `wp-content/uploads/2026/07/power-properties-timeline.webp`.

### Task 2: Consolidate Bulgarian timeline and gallery markup in index.html

#### Intent
Update `index.html` to a thirteen-milestone timeline, add data-project gallery launchers to seven gallery-eligible projects, restructure the dialog into project-specific containers, remove the standalone trigger, and replace both Viber SVG paths with the official icon.

#### Procedure
Step 1: In `index.html`, locate the Experience section desktop tab rail (the element with `role="tablist"` containing `.at-tlb` buttons). Insert four new tab button elements: Apartment Louis Ayer (2024) after the 2021 Elemag control and before the 2024 ATISTAT control; Apartment Arcadia (2025), Bebelan (2025), and Power Properties (2025) after the 2024 ATISTAT control and before the 2025 UBB Interlease control. Each new button must include the CSS class `at-tlb`; a year indicator element with class `at-tlb__year` containing the confirmed year; an `<img>` with class `at-tlb__img`, `src` pointing to its respective new WebP file (louis-ayer-timeline.webp, arcadia-timeline.webp, bebelan-timeline.webp, power-properties-timeline.webp), `alt` set to the Bulgarian project name (ап. Луи Айер, ап. Аркадия, Бебелан, Пауър Пропъртис), and `loading="lazy"`.

Step 2: In `index.html`, insert four corresponding desktop panel elements for the four new milestones immediately adjacent to their tab buttons in the panels container. Each panel must include the project name heading, confirmed year, building type (Апартамент for Louis Ayer and Arcadia; Офисно-складова for Bebelan and Power Properties), service (Довършителни работи), partner attribution (АТИСТАТ for Louis Ayer and Arcadia; Инженерни Системи for Bebelan and Power Properties), a unique `data-project` attribute (louis-ayer, arcadia, bebelan, power-properties), and a `<button>` gallery launcher with the same `data-project` attribute containing a four-image mosaic element with up to four `<img>` elements referencing the first four photographs from each project's gallery directory.

Step 3: In `index.html`, add a `data-project` attribute and a mosaic gallery launcher `<button>` to each of the three existing gallery-intersection desktop panels: Montekanal (`data-project="montekanal"`), Elemag (`data-project="elemag"`), UBB Interlease (`data-project="ubb-interlease"`). Use the same launcher structure as in step 2.

Step 4: In `index.html`, locate the mobile card stack (`.at-tlcard` list). Insert four new mobile card list items for the four new milestones in the same chronological positions as their desktop counterparts. Each mobile card must include the project name, year, building type, service, partner attribution, `data-project` attribute, and a mosaic gallery launcher `<button>`. Also add a mosaic gallery launcher `<button>` with the correct `data-project` to the three existing gallery-intersection mobile cards (Montekanal, Elemag, UBB Interlease).

Step 5: In `index.html`, locate the selected-projects native `<dialog>` element. Inside the dialog body, wrap each project's existing image `<figure>` set in a `<div data-project="...">` container: Montekanal images → `data-project="montekanal"`, Elemag images → `data-project="elemag"`, UBB Interlease images → `data-project="ubb-interlease"`, Apartment Arcadia images → `data-project="arcadia"`, Apartment Louis Ayer images → `data-project="louis-ayer"`, Bebelan images → `data-project="bebelan"`, Power Properties images → `data-project="power-properties"`. Ensure all existing project images remain in their respective containers and no images are orphaned outside a container.

Step 6: In `index.html`, locate and remove the standalone Experience-header selected-projects trigger element (the `<button>` element with class `at-selected-projects__trigger` or equivalent). Verify that no `aria-controls`, `aria-labelledby`, or other ARIA attributes on remaining elements reference the removed element's ID.

Step 7: In `index.html`, locate both Viber anchor elements (the contact-row `<a class="at-viber">` and the floating `<a class="at-viber">`). In each, remove the existing inline SVG element (the Messenger lightning-bolt path) and replace it with `<img src="wp-content/themes/atistat/assets/images/viber-icon.png" alt="" aria-hidden="true" width="28" height="28">`. Confirm each anchor retains an `aria-label` or visible text node naming the Viber action.

### Task 3: Synchronize index-bg.html and index.html?lang=bg.html

#### Intent
Bring both Bulgarian alias documents to structural and content parity with the updated `index.html`.

#### Procedure
Step 1: In `index-bg.html`, apply all structural changes from Task 2 steps 1–7: insert the four new tab buttons and panels, add data-project launchers to the three gallery-intersection desktop panels and three mobile cards, insert four new mobile cards with launchers, restructure the dialog into project-specific containers, remove the standalone trigger, and replace the single Viber control SVG with the official icon `<img>`. Preserve `index-bg.html`'s own `<link rel="canonical">`, `og:url`, `lang="bg"` attribute on `<html>`, and any navigation element attributes that differ from `index.html`.

Step 2: In `index-bg.html`, locate the Why ATISTAT section (the section containing the `ЗАЩО АТИСТАТ` heading) and the FAQ section (the section containing the `ВЪПРОСИ И ОТГОВОРИ` heading). Compare every heading text, card title, card body text, question text, and answer text against the same sections in `index.html`. Correct any text, class name, or structural divergences so that these sections in `index-bg.html` are content-equivalent to `index.html`.

Step 3: In `index.html?lang=bg.html`, replicate all changes applied to `index-bg.html` in steps 1–2. Preserve this file's own canonical URL, og:url, and route-specific attributes.

### Task 4: Synchronize English homepage documents

#### Intent
Update `index-en.html` and `index.html?lang=en.html` with the thirteen-milestone English timeline, mosaic launchers, one filtered dialog, faithful Why/FAQ translations, and the official Viber icon.

#### Procedure
Step 1: In `index-en.html`, replicate the thirteen-milestone desktop tab rail from `index.html`, translating all visible Bulgarian text to English. Project name translations: ЕОС Матрикс → EOS Matrix, Монтеканал → Montekanal, ЕМА → EMA, Елемаг → Elemag, ап. Луи Айер → Apt. Louis Ayer, ATISTAT → ATISTAT, ОББ Интерлийз → UBB Interlease, ап. Аркадия → Apt. Arcadia, Бебелан → Bebelan, Пауър Пропъртис → Power Properties, Британско училище в София → British School of Sofia. Building type translations: Апартамент → Apartment, Офисно-складова → Office/Warehouse, Жилищна → Residential, Офисна → Office. Service translations: Довършителни работи → Fit-out Works, Строителство → Construction. Partner translations: Инженерни Системи → Engineering Systems, АТИСТАТ → ATISTAT.

Step 2: In `index-en.html`, add `data-project` mosaic gallery launcher `<button>` elements to the seven gallery-eligible desktop panels and mobile cards using the same language-independent `data-project` attribute values (arcadia, louis-ayer, bebelan, power-properties, montekanal, elemag, ubb-interlease) as in the Bulgarian documents.

Step 3: In `index-en.html`, restructure the selected-projects `<dialog>` into seven project-specific containers with matching `data-project` attributes. Use English alt text for all project `<img>` elements within each container.

Step 4: In `index-en.html`, locate and remove the standalone all-project selected-projects trigger element.

Step 5: In `index-en.html`, locate the Why ATISTAT section and FAQ section. Using the Bulgarian text in `index.html` as the only source of truth, produce faithful English translations for every visible text element: section heading, each differentiator card title, each differentiator card body paragraph, each FAQ question, and each FAQ answer. Replace the current English Why/FAQ content with these translations, preserving the element type, class name, and nesting structure of each element.

Step 6: In `index-en.html`, update the single Viber control by removing the existing SVG and inserting `<img src="wp-content/themes/atistat/assets/images/viber-icon.png" alt="" aria-hidden="true" width="28" height="28">`, preserving the anchor's `aria-label`.

Step 7: In `index.html?lang=en.html`, replicate all changes applied to `index-en.html` in steps 1–6. Preserve this file's own canonical URL, og:url, and route-specific attributes.

### Task 5: Synchronize opit/index.html

#### Intent
Update `opit/index.html` to deliver the thirteen-milestone English timeline and gallery content with correct relative asset paths for the /opit/ URL depth.

#### Procedure
Step 1: In `opit/index.html`, audit every relative URL in `<link href="">`, `<script src="">`, and `<img src="">` attributes. For each path beginning with `wp-content/` that is valid from the repository root, prepend `../` to make it valid from the `/opit/` URL depth (e.g., `wp-content/themes/...` → `../wp-content/themes/...`).

Step 2: In `opit/index.html`, replicate the thirteen-milestone desktop tab rail and mobile card stack from `index-en.html`, using the `../` prefix established in step 1 for all image `src` references (e.g., `../wp-content/uploads/2026/07/arcadia-timeline.webp`). Use the same English text, metadata, data-project values, mosaic launchers, and dialog containers as in `index-en.html`.

Step 3: In `opit/index.html`, remove any standalone all-project selected-projects trigger element if present. Confirm no Viber controls exist in this document and skip Viber updates.

### Task 6: Extend shared presentation in main.css

#### Intent
Add horizontally scrollable timeline, four-image mosaic CSS, Viber image sizing, shared Why/FAQ styles, and reduced-motion coverage to `main.css`; mirror to the version-suffixed copy; remove superseded document-local styles from `index.html`.

#### Procedure
Step 1: In `wp-content/themes/atistat/assets/css/main.css`, locate the desktop timeline rail container selector (the wrapping element for the `.at-tlb` tab buttons). Add `overflow-x: auto` to enable horizontal scrolling and `scroll-behavior: smooth`. Add a `:focus-within` rule on the same container that applies `outline: 2px solid currentColor` to satisfy visible focus requirements. Do not change any existing `width`, `min-width`, or `flex` values on the tab button items so all thirteen controls retain their current dimensions.

Step 2: In `wp-content/themes/atistat/assets/css/main.css`, add a new rule block for the four-image mosaic gallery launcher button targeting `button[data-project]` or a dedicated class (e.g., `.at-gallery-mosaic`). Define a 2-column CSS grid on the button, set each cell to a fixed thumbnail size (e.g., 56px × 56px), set `object-fit: cover` and `display: block` on `<img>` children, remove default button `background` and `border`, set `cursor: pointer`, and add a `:focus-visible` outline.

Step 3: In `wp-content/themes/atistat/assets/css/main.css`, locate the `.at-viber` selector block. Add a child rule for `img` that sets `width: 28px`, `height: 28px`, `display: block`, and `pointer-events: none`. Confirm no `filter`, `color`, `fill`, or `opacity` rule in the block would alter the icon's white appearance.

Step 4: In `wp-content/themes/atistat/assets/css/main.css`, append the complete Why/FAQ component CSS extracted verbatim from the document-local `<style>` block inside `index.html` (the block containing the ЗАЩО АТИСТАТ and ВЪПРОСИ И ОТГОВОРИ component rules). Include every selector, property, and responsive media query from that block without duplication.

Step 5: In `wp-content/themes/atistat/assets/css/main.css`, add or extend the `@media (prefers-reduced-motion: reduce)` block to set `transition: none` and `animation: none` on every selector newly added in steps 1–4 that defines a transition or animation.

Step 6: Copy `wp-content/themes/atistat/assets/css/main.css` byte-for-byte to `wp-content/themes/atistat/assets/css/main.css?ver=1784544801.css`. Run `cmp wp-content/themes/atistat/assets/css/main.css 'wp-content/themes/atistat/assets/css/main.css?ver=1784544801.css'`; the command must produce no output (zero differences).

Step 7: In `index.html`, locate and remove the entire document-local `<style>` block containing the Why/FAQ component rules (identified by the ЗАЩО АТИСТАТ and ВЪПРОСИ И ОТГОВОРИ selectors). Confirm that after removal the Why/FAQ sections render correctly using the shared stylesheet.

### Task 7: Generalize shared gallery interaction in main.js

#### Intent
Replace the single-trigger gallery initializer with a multi-launcher, project-filtering handler and mirror the updated file to the version-suffixed copy.

#### Procedure
Step 1: In `wp-content/themes/atistat/assets/js/main.js`, locate the code that selects the single `.at-selected-projects__trigger` element (or equivalent single-trigger selector) and attaches a click listener to open the gallery dialog. Remove this entire selection and binding block.

Step 2: In `wp-content/themes/atistat/assets/js/main.js`, add a new event listener on the document (or the Experience section container) listening for `click` events. In the handler, resolve the clicked gallery launcher by calling `event.target.closest('button[data-project]')`; if null, exit the handler. Otherwise: store the resolved element as `lastGalleryLauncher`; read `projectId` from its `dataset.project`; query all project container elements inside the dialog using `[data-project]` selector; set the `hidden` attribute on every container; remove `hidden` from the container whose `data-project` equals `projectId`; if no such container exists, log `console.warn('Gallery project not found: ' + projectId)` and ensure the close button is visible; set the dialog's `aria-label` to the text of the active project container's heading element; reset that container's `scrollTop` to 0; call `dialog.showModal()`; set focus to the first focusable element inside the visible container, or to the dialog close button if none exists.

Step 3: In `wp-content/themes/atistat/assets/js/main.js`, locate the dialog `close` event handler. After the existing close behavior, add `if (lastGalleryLauncher && document.contains(lastGalleryLauncher)) lastGalleryLauncher.focus()`.

Step 4: Copy `wp-content/themes/atistat/assets/js/main.js` byte-for-byte to `wp-content/themes/atistat/assets/js/main.js?ver=1784544801`. Run `cmp wp-content/themes/atistat/assets/js/main.js 'wp-content/themes/atistat/assets/js/main.js?ver=1784544801'`; the command must produce no output.

### Task 8: Verify and validate

#### Intent
Confirm all six documents pass static invariant checks, gallery interactions work correctly, asset pairs are byte-identical, and no missing resources exist.

#### Procedure
Step 1: From the repository root, run `python3 -m http.server 8765`. In a separate shell, confirm HTTP 200 for: `http://localhost:8765/`, `http://localhost:8765/index-bg.html`, `http://localhost:8765/index-en.html`, `http://localhost:8765/opit/`, `http://localhost:8765/wp-content/themes/atistat/assets/images/viber-icon.png`, and each of the four new WebP paths (`http://localhost:8765/wp-content/uploads/2026/07/arcadia-timeline.webp`, `louis-ayer-timeline.webp`, `bebelan-timeline.webp`, `power-properties-timeline.webp`).

Step 2: Open each of the six target documents in a browser and verify: thirteen milestone controls appear in the desktop tab rail in chronological order; the rail scrolls horizontally with keyboard navigation; seven gallery-eligible desktop panels show a four-image mosaic launcher button; clicking each launcher opens the dialog showing only that project's images; the dialog accessible name reflects the project name; pressing Escape returns focus to the launcher; no all-project standalone trigger button is present; no console 404 or JavaScript errors appear.

Step 3: In index.html, index-bg.html, and index.html?lang=bg.html, confirm that the Why ATISTAT and FAQ sections are structurally and textually identical across all three documents.

Step 4: In index-en.html and index.html?lang=en.html, confirm that the Why ATISTAT and FAQ sections contain faithful English translations and that the element structure matches the Bulgarian source structure.

Step 5: In all five root homepage documents, confirm every Viber control renders `viber-icon.png` on the ATISTAT green background with no inline SVG bolt path remaining.

Step 6: Run `cmp wp-content/themes/atistat/assets/css/main.css 'wp-content/themes/atistat/assets/css/main.css?ver=1784544801.css'`. Expected output: none (byte-identical).

Step 7: Run `cmp wp-content/themes/atistat/assets/js/main.js 'wp-content/themes/atistat/assets/js/main.js?ver=1784544801'`. Expected output: none (byte-identical).

Step 8: Run `ls -la -- 'index.html?lang=bg.html' 'index.html?lang=en.html'` from the repository root. Expected: two file entries with non-zero sizes.

### Task 9: Update product context

#### Intent
Synchronize `.aib_memory/context.md` with the implemented thirteen-milestone model, project-scoped gallery behavior, official Viber asset, English Why/FAQ parity, opit/ synchronization, and AI Agent critique findings not addressed by this request.

#### Procedure
Step 1: Delete the obsolete portfolio timeline statement from context.md Concepts section:
`python .aib_brain/tools/edit-context.py --workspace . --operation delete --area Concepts --text "Portfolio timeline combines three company milestones (Корект Проект 2006, Инженерни Системи 2008, ATISTAT 2024) with six delivered project records: EOS Matrix, Montekanal, EMA, Elemag, UBB Interlease, and British School of Sofia."`

Step 2: Insert the updated thirteen-milestone portfolio timeline statement into context.md Concepts section:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Concepts --text "Portfolio timeline merges thirteen milestones: three company records (Корект Проект 2006, Инженерни Системи 2008, ATISTAT 2024) and ten projects (EOS Matrix 2007, Montekanal 2011, EMA 2015, Elemag 2021, Apartment Louis Ayer 2024, UBB Interlease 2025, Apartment Arcadia 2025, Bebelan 2025, Power Properties 2025, and British School of Sofia 2026)."`

Step 3: Delete the obsolete gallery statement from context.md Solution section:
`python .aib_brain/tools/edit-context.py --workspace . --operation delete --area Solution --text "Selected-projects gallery opens as a native dialog modal triggered from the Experience section and displays all 7 submitted project image sets as a photo gallery without dedicated project routes."`

Step 4: Insert the updated gallery behavior statement into context.md Solution section:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Gallery-eligible projects (Elemag, Montekanal, UBB Interlease, Apartment Arcadia, Apartment Louis Ayer, Bebelan, Power Properties) expose a four-image mosaic launcher in the active desktop panel and eligible mobile card; launchers open a single reusable filtered native dialog showing only the invoked project's image set; focus is restored to the invoker on close."`

Step 5: Delete the obsolete nine-milestone timeline statement from context.md Solution section:
`python .aib_brain/tools/edit-context.py --workspace . --operation delete --area Solution --text "Homepage timeline renders accessible desktop tabs and alternate stacked mobile cards from the same nine company and project milestones."`

Step 6: Insert the updated thirteen-milestone scrollable rail statement into context.md Solution section:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Homepage timeline renders accessible desktop tabs and alternate stacked mobile cards from the same thirteen company and project milestones in a horizontally scrollable single rail on desktop."`

Step 7: Insert the gallery-only project timeline assets statement into context.md Solution section:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Gallery-only projects (Apartment Arcadia, Apartment Louis Ayer, Bebelan, Power Properties) use lightweight line-art WebP timeline assets generated from their first submitted photograph and stored in wp-content/uploads/2026/07/ with ASCII filenames."`

Step 8: Insert the official Viber icon statement into context.md Solution section:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Official white Viber icon is stored locally as viber-icon.png in wp-content/themes/atistat/assets/images/ and served on ATISTAT green controls without alteration across all five root homepage documents."`

Step 9: Insert the English Why/FAQ parity statement into context.md Solution section:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "English Why/FAQ sections are faithful translations of the Bulgarian source-of-truth sections and inherit CSS rules defined in main.css; Why/FAQ document-local styles in index.html are promoted to the shared stylesheet."`

Step 10: Insert the opit/index.html synchronization statement into context.md Solution section:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "opit/index.html is synchronized with the English homepage timeline and gallery content, including thirteen milestones, project metadata, and project-scoped gallery launchers."`

Step 11: Insert the timeline accessibility Issue entry from the AI Agent critique:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Issues --text "Homepage timeline tablist mixes button[role='tab'] project controls with company milestone links that have no role='tab'; panels lack aria-labelledby; arrow key navigation clamps at rail ends instead of wrapping; these accessibility gaps predate R-20260725-1900 and are not fixed by it. Source: main.js tab-key handling and index.html tablist markup."`

Step 12: Insert the mobile card keyboard semantics Issue entry from the AI Agent critique:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Issues --text "Mobile project and company cards use clickable li[data-href] elements without native keyboard interactivity; gallery launcher buttons must use the native button element to avoid repeating this pattern. Source: index.html mobile timeline card markup."`

Step 13: Verify all edit-context.py commands above exited with code 0. Run `python .aib_brain/tools/verify-context.py --workspace .`. Expected output: "Results: 12/12 checks passed."
