---
state:
  request_id: R-20260726-0108
  title: Партньори Adreo image and timeline arrow buttons
  status: questions_generated
  input_verification_result: valid
  context_verification_result: valid
options:
  minimum_questions: 13
  input_verification_enabled: true
  context_verification_enabled: true
---

## Input

## Questions

**Q001**: Should the Adreo partner image change be applied to `index.html` (primary Bulgarian homepage) in addition to the four explicit BG/EN variant files?
> **Why this matters:** If `index.html` is not updated, the primary homepage will show a text wordmark while the locale variants show a logo, creating visible inconsistency for visitors landing on the root URL.
- [x] Option A: Apply to all five documents — `index.html`, `index-bg.html`, `index-en.html`, `index.html?lang=bg.html`, `index.html?lang=en.html` *(recommended)*
- [ ] Option B: Apply only to the four explicit BG/EN variants, excluding `index.html`
- [ ] Other: ___

**Q002**: What `alt` attribute value should the new `adreo.png` image use?
> **Why this matters:** The alt text is the only textual identifier for this image if it fails to load; it must be accurate for accessibility and SEO.
- [ ] Option A: `alt="Адрео"` — Bulgarian Cyrillic, consistent with existing partner alt-text convention *(recommended)*
- [ ] Option B: `alt="Adreo"` — Latin script, matching the brand's own website and URL
- [x] Option C: Language-appropriate — `alt="Адрео"` in BG files, `alt="Adreo"` in EN files
- [ ] Other: ___

**Q003**: Where in the timeline DOM structure should the repositioned Previous/Next arrow buttons be placed?
> **Why this matters:** The DOM position determines which CSS positioning model is used, whether the arrows stay stationary as the rail scrolls, and how they interact with the existing edge fade spans.
- [x] Option A: Inside `at-tl-rail-shell` as absolutely-positioned overlays at left/right edges — on top of the existing gradient fades *(recommended)*
- [ ] Option B: Inside `at-tl-stage` as absolutely-positioned overlays, outside the rail shell
- [ ] Option C: Inside `at-tl-desktop` as siblings of `at-tl-stage`, with explicit offset CSS
- [ ] Other: ___

**Q004**: What format should the arrow button visuals use?
> **Why this matters:** The format determines whether new asset files are needed, how the arrows scale and theme, and how much HTML changes across the six documents.
- [x] Option A: SVG inline in HTML — embedded SVG path inside the button element *(recommended)*
- [ ] Option B: CSS-drawn arrows using `border` or `clip-path` pseudo-elements — no extra files
- [ ] Option C: External image file (.svg or .png) stored in `wp-content/themes/atistat/assets/images/`
- [ ] Option D: Unicode/text characters (current `←`/`→`) repositioned and styled with CSS only
- [ ] Other: ___

**Q005**: What visual shape should the arrow icons take?
> **Why this matters:** The icon shape defines the visual language of the timeline navigation and how it reads against the content at the rail edges.
- [x] Option A: Simple chevron (‹ ›) — thin, minimal directional lines *(recommended)*
- [ ] Option B: Filled directional triangle — solid filled arrowhead
- [ ] Option C: Circular button with arrow inside — icon inside a solid or outlined circle
- [ ] Other: ___

**Q006**: Should the repositioned arrow buttons retain visible text labels ("Предишен" / "Следващ")?
> **Why this matters:** Removing visible text in favour of icon-only arrows requires the `aria-label` to carry the full accessible name; retaining visible text may cause layout issues inside a narrow rail-end overlay.
- [x] Option A: Icon-only — remove visible text; retain `aria-label` attribute for screen readers *(recommended)*
- [ ] Option B: Retain visible text labels alongside the arrow image
- [ ] Option C: Hidden text — move labels to a visually hidden `<span>` so screen readers still announce them
- [ ] Other: ___

**Q007**: What should happen to the existing `at-tl-edge` gradient fade spans after the arrows are repositioned?
> **Why this matters:** The JS requires both edge spans to remain in the DOM; the visual relationship between the gradient fades and the new arrow overlays must be defined to avoid a cluttered appearance.
- [x] Option A: Keep edge fades unchanged; place arrow buttons on top (z-index above 7) *(recommended)*
- [ ] Option B: Remove the gradient from edge spans; let the arrow buttons alone signal the rail boundary
- [ ] Option C: Integrate the gradient into the arrow button's CSS background; edge spans become invisible placeholders
- [ ] Other: ___

**Q008**: After moving the Previous/Next buttons out, should the `.at-tl-controls` wrapper `<div>` be removed from the HTML?
> **Why this matters:** Removing the div eliminates dead markup and allows the associated `margin-top: 14px` CSS to be cleaned up; keeping it empty may cause phantom spacing.
- [ ] Option A: Remove the `.at-tl-controls` div entirely from each timeline document *(recommended)*
- [ ] Option B: Keep the `.at-tl-controls` div but leave it empty; suppress spacing via CSS if needed
- [x] Other: Make something similar to "fade-wrapper visible brands-gallery" in https://us.pg.com/

**Q009**: Should the new arrow button CSS be added to the shared `main.css` stylesheet or as document-local `<style>` blocks?
> **Why this matters:** Using the shared stylesheet propagates the change to all six timeline documents automatically and maintains the single-source CSS architecture; document-local blocks would require six separate edits.
- [x] Option A: Add to `wp-content/themes/atistat/assets/css/main.css` (shared stylesheet, both copies) *(recommended)*
- [ ] Option B: Add as document-local `<style>` blocks inside each HTML file
- [ ] Other: ___

**Q010**: For the EN-localized documents, should the arrow button `aria-label` values be updated to English?
> **Why this matters:** Current buttons carry Bulgarian labels in EN files — a pre-existing localization gap. The arrow redesign is a natural opportunity to correct it, but doing so adds scope beyond the stated request.
- [x] Option A: Update EN documents to use English aria-labels: "Previous stage" / "Next stage" *(recommended)*
- [ ] Option B: Retain Bulgarian aria-labels in all documents, including EN files
- [ ] Other: ___

**Q011**: Should the timeline arrow button change be applied to `opit/index.html` in addition to the five root homepage documents?
> **Why this matters:** All six timeline documents are expected to remain synchronized; leaving `opit/index.html` with the old below-rail buttons may cause static-site test parity failures.
- [x] Option A: Apply to all six timeline documents, including `opit/index.html` *(recommended)*
- [ ] Option B: Apply only to the five root homepage documents; leave `opit/index.html` unchanged
- [ ] Other: ___

**Q012**: How should the arrow buttons appear when they are in a disabled state (first or last milestone active)?
> **Why this matters:** The disabled visual determines whether boundary state is communicated clearly; hiding the button entirely removes a directional cue, while a faint arrow maintains orientation.
- [ ] Option A: Reduced opacity (inherit current `opacity: 0.38; cursor: not-allowed`) *(recommended)*
- [x] Option B: Fully hidden (`visibility: hidden` or `display: none`) when disabled
- [ ] Option C: Different boundary icon or stop indicator when disabled
- [ ] Other: ___

**Q013**: Should the arrow buttons be positioned as absolute overlays on a non-scrolling parent (e.g., `at-tl-rail-shell` or `at-tl-stage`) to ensure they stay stationary as the rail pans?
> **Why this matters:** If arrows are placed inside the scrollable `at-tl-buildings` container, they will scroll out of view when the user pans the timeline, losing the persistent navigation affordance.
- [x] Option A: Absolute overlay on `at-tl-rail-shell` — buttons stay fixed as the inner rail scrolls *(recommended)*
- [ ] Option B: Absolute overlay on `at-tl-stage` — wider parent, requires offset to align with rail edges
- [ ] Option C: `position: sticky` inside `at-tl-rail-shell` — experimental; browser support in nested scroll contexts may be unpredictable
- [ ] Other: ___
