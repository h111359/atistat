# Analysis: R-20260726-0108

## Overview

- **Request ID:** R-20260726-0108
- **Request title:** Партньори Adreo image and timeline arrow buttons

### Background

The partners section currently presents three partner entities — Инженерни Системи, Корект Проект, and Адрео — but only the first two render as logo images. Адрео is shown as a styled text wordmark (`<span class="at-partner__wordmark">Адрео</span>`) because no partner image was available at the time of the original implementation. The partner image file `wp-content/uploads/2026/07/adreo.png` is now present in the workspace and the developer wants it rendered consistently with the other partners.

Additionally, the desktop timeline navigation currently provides Previous and Next controls as a labelled button row placed below the timeline rail (`.at-tl-controls` div). The developer wants these navigation affordances relocated so they appear as arrow-image controls integrated into the ends of the timeline rail itself, providing more intuitive in-line navigation without a separate button strip below.

### Scope

- Аdreo partner list item in the partners section of all applicable BG and EN homepage documents: replacing the text wordmark span with an `<img>` element pointing to `wp-content/uploads/2026/07/adreo.png`.
- Desktop timeline previous/next navigation buttons in all timeline-bearing homepage documents: repositioning the buttons from the `.at-tl-controls` div below the timeline to the ends of the timeline rail as visually integrated arrow-image controls.
- CSS changes in the shared stylesheet (`wp-content/themes/atistat/assets/css/main.css`) and/or document-local style blocks to support the new arrow layout.
- `context.md` update to reflect the partners section now using a logo image for Адрео and to document the arrow-integrated timeline navigation model.

### Out of scope

- Mobile stacked-card timeline layout (`.at-tlcard` elements) — mobile navigation is unchanged.
- Project detail pages under `proekti/`.
- `opit/index.html` partners section (absent from that document).
- Server configuration, CMS, or WordPress changes.
- Content or structure of the six project JSON records in `wp-json/`.
- Any gallery dialog, FAQ, hero section, or contact block changes.

---

## Input Interpretation

### User Original Inputs

> In "Партньори" for Adreo use the image `wp-content/uploads/2026/07/adreo.png` as the partner image like for the other partners. For BG and EN pages
>
> Also instead of buttons below the timeline - interate the buttons as arrow images in the both ends of the timeline itself

### AI Interpretation

**Partners — Adreo logo image**

The Адрео partner list item in all applicable Bulgarian and English homepage documents must be updated to display a logo image element (`<img>`) using `wp-content/uploads/2026/07/adreo.png` as its source, exactly matching the markup pattern already used for Инженерни Системи and Корект Проект: a lazy-loaded, async-decoded image inside the existing `.at-partner__link` anchor element, constrained to the shared `max-height: 52px` CSS rule. The current `<span class="at-partner__wordmark">Адрео</span>` text element is to be replaced.

The phrase "For BG and EN pages" identifies the target document set. The primary Bulgarian homepage (`index.html`) is the canonical source; whether it is included alongside the four explicit BG/EN query-route and locale variants is a decision point. The partner image file has been confirmed to exist at the specified path.

**Timeline — arrow buttons integrated at rail ends**

The desktop timeline's Previous and Next navigation affordances, currently rendered as a separate labelled-button strip (`<div class="at-tl-controls">`) positioned below the timeline rail, must be redesigned to appear as directional arrow-image controls embedded visually within the ends of the timeline rail area itself. The developer's phrase "arrow images" indicates visible graphical arrows rather than text characters or labels.

The JavaScript initialization code requires both `<button data-timeline-previous aria-label="...">` and `<button data-timeline-next aria-label="...">` elements to be present in the DOM with those exact attributes intact — the JS queries for them with `timeline.querySelector("button[data-timeline-previous][aria-label]")`. The edge fade spans (`data-timeline-edge="previous"` and `data-timeline-edge="next"`) are also required by the JS and must remain. The new arrow buttons may be relocated within the document structure but must preserve these required attributes.

---

## Research Results

**Workspace pattern scan**

- `index.html` line 732–734: Partners list contains three `<li>` items; Инженерни Системи and Корект Проект use `<img>` elements with `wp-content/themes/atistat/assets/images/` paths; Адрео uses `<span class="at-partner__wordmark">Адрео</span>`.
- `index-bg.html` line 729, `index-en.html` line 729, `index.html?lang=bg.html` line 729, `index.html?lang=en.html` line 729: All carry the identical Adreo wordmark span.
- `opit/index.html`: No partners section present.
- `wp-content/uploads/2026/07/adreo.png`: Confirmed present in the workspace.
- `wp-content/themes/atistat/assets/css/main.css` lines 741–743: `.at-partner__link img` rule constrains all partner images to `max-height: 52px; width: auto; object-fit: contain`.
- `main.css` lines 748–752: `.at-partner__wordmark` rule applies text styling to the Adreo span; this rule will become dead code after the image swap unless retained for future use.
- `main.css` lines 375–389: `.at-tl-edge` and modifier rules define absolutely-positioned, pointer-events-none fade overlays at both ends of the rail shell; z-index 7; JS-toggled opacity.
- `main.css` lines 392–406: `.at-tl-controls` flex row; `.at-tl-control` styled as pill buttons with min 44×44px tap target, hover fill, focus ring, and disabled opacity.
- `main.js` lines 230–233: JS binds Previous and Next controls by querying `button[data-timeline-previous][aria-label]` and `button[data-timeline-next][aria-label]`; initialization aborts if either is absent or not an `HTMLButtonElement`.
- `main.js` lines 232–233: JS also queries both edge spans by `[data-timeline-edge="previous"]` and `[data-timeline-edge="next"]`; initialization aborts if either is absent or not an `HTMLElement`.
- `main.css` line 1133: `@media (prefers-reduced-motion: reduce)` block includes `.at-tl-control` in a `transition: none !important` rule, so any new arrow-style buttons reusing `.at-tl-control` CSS will inherit reduced-motion compliance automatically.
- Six homepage documents carry the timeline: `index.html`, `index-bg.html`, `index-en.html`, `index.html?lang=bg.html`, `index.html?lang=en.html`, and `opit/index.html`.
- The existing `.at-tl-edge` spans are `position: absolute` within `.at-tl-rail-shell` and have `pointer-events: none`, which means they cannot receive click events in their current form. Any click-interactive arrow control placed inside the same container must use a positive pointer-events value and a z-index above 7.

**Industry knowledge**

- *Overlay navigation pattern — Nielsen Norman Group:* Carousel and timeline controls embedded at the edges of the scroll track (chevron buttons overlaid on the scrollable area) are recommended over external button strips when horizontal space is at a premium. Key trade-off: the overlay must not obscure content near the edges; this is typically resolved by reserving a padding zone inside the scroll container or clipping content behind the overlay with a gradient fade. Applicability: directly relevant; the current `.at-tl-edge` gradient already fulfils the fade role, so extending it to include a clickable arrow on top is architecturally sound.

- *WCAG 2.1 — Success Criterion 2.5.5 (Target Size):* Interactive controls should meet a minimum 44×44 CSS-pixel touch target. The current `.at-tl-control` rule already specifies `min-width: 44px; min-height: 44px`. Repositioned arrow buttons must preserve this constraint to avoid accessibility regression. Applicability: high; the repositioned controls must not shrink below 44×44px regardless of their new position.

- *SVG icon conventions — Material Design and ARIA:* When an icon-only button conveys its entire meaning through a visible image (SVG or raster), the `aria-label` on the `<button>` element is the correct and sufficient text alternative; no additional visible text label is required. The icon-only pattern is standard for navigation arrows that pair with adjacent visible context (the timeline itself). Applicability: supports removing the visible text labels ("Предишен"/"Следващ") from the buttons if they are repositioned as icon-only arrow controls, provided `aria-label` values are retained.

**AI Agent critique**

- `.at-partner__wordmark` CSS rule (`main.css` lines 748–752) will become dead code after Adreo adopts an image element; the rule should be removed to keep the stylesheet clean, unless it is retained as a generic utility class.
- The `aria-label` on the Adreo link already differs between BG files (`"Адрео — отвори сайта на партньора"`) and EN files (`"Adreo — open partner website"`), but the `alt` text for the new image is not specified in the input; inconsistency between the link's label language and the image's alt language would be a minor but auditable gap.
- `index.html` (primary Bulgarian) and `index-bg.html` are known to diverge by approximately 400 lines (noted in `context.md` Issues); each implementation pass widens this divergence. This request should ideally treat `index.html` and `index-bg.html` identically for the Adreo change to avoid further divergence.
- The `.at-tl-controls` div is referenced in the CSS with `.at-tl-control` child selectors; if the div wrapper is removed but the buttons are retained, the class selectors remain valid on the button elements themselves. However, `display: flex; justify-content: center; gap: 12px; margin-top: 14px` on `.at-tl-controls` would need to be replaced by new positioning CSS for the arrow buttons.
- The current `.at-tl-edge` spans have `pointer-events: none`; placing interactive arrow buttons inside the same stacking context requires explicitly overriding this on the button element, not the span.
- The JS `initializeTimeline` function calls an early-exit guard that returns without error if either the previous or next control is absent. Removing or misidentifying these buttons during the HTML refactor would silently disable all timeline keyboard and click navigation with no visible error.
- The six timeline documents are known to be synchronized; any structural change to the timeline markup must be applied consistently across all six files to avoid test failures in `tests/test_static_site.py`.
- The `.at-tl-controls` CSS block's `margin-top: 14px` creates visual separation from the timeline; the repositioned arrows eliminate the need for this margin, so the CSS rule should be removed or repurposed.
- English-language pages currently use Bulgarian `aria-label` values on the timeline buttons (`"Предишен етап"` / `"Следващ етап"`); this is a pre-existing inconsistency unrelated to this request but may be worth correcting during the arrow redesign.
- Developer's Q008 answer references the P&G brands-gallery pattern (`us.pg.com`) as the model for the navigation wrapper. That page's brands section uses a horizontally scrollable container with always-visible gradient fades at the edges and arrow navigation buttons permanently overlaid at those same edges (not hidden on-idle). The term "fade-wrapper visible" describes a container that simultaneously shows gradient edge fades and clickable arrow controls — equivalent to reusing the existing `at-tl-rail-shell` (which already carries the gradient fades via `.at-tl-edge` spans) as the wrapper, with arrow buttons added inside it as always-present overlays. This confirms DP-003 (arrows inside rail-shell) and DP-007 (keep edge fades) as the intended structural approach.

### Edge Cases

- If `adreo.png` fails to load in a user's browser (network error or path mismatch), no fallback text is displayed for the partner; the anchor's `aria-label` remains the only textual identification. The alt text must be non-empty to provide a fallback.
- If the arrow buttons are placed inside the scrollable `.at-tl-buildings` container, they will scroll out of view when the user pans the rail, losing the persistent navigation affordance. Buttons must be placed outside the scroll container or positioned as non-scrolling overlays.
- The `at-tl-rail-shell` uses `position` context (implied by child absolute elements); arrow buttons added inside it and styled with `position: absolute` at the edges will remain fixed relative to the shell even as the inner rail scrolls — this is the correct placement if overlay is chosen.
- At the first and last milestone, the current disabled buttons use `opacity: 0.38`. If the arrows are styled differently (e.g., hidden when disabled), keyboard-only users relying on the `:disabled` state for boundary feedback would lose that affordance unless an alternative indicator is provided.
- The `opit/index.html` timeline currently uses identical HTML markup to the five root homepages. If the arrow change is applied to root homepages only, `opit/index.html` will diverge and the shared-asset tests may flag parity failures.
- The reduced-motion media query already covers `.at-tl-control` transitions; new arrow elements using the same class inherit this coverage automatically. New classes introduced for the arrows must be added to the reduced-motion block manually.
- The CSS duplicate (`wp-content/themes/atistat/assets/css/main.css` plus an identical copy) must be updated in both locations; editing only one will create a byte-inequivalence flagged by tests.

### Requirements Gate Evaluation

Requirements Gate: 8/8 PASS — all categories satisfied.

*Documented assumptions used:*
- Goal Clarity (multiple-goal concern): Both changes are co-located homepage UI improvements that share no functional dependencies and can be safely bundled in one iteration; treating them as a single coherent UI enhancement is a reasonable documented assumption.
- Stakeholder: Developer (Hristin Haralanov) is the requesting party; beneficiaries are website visitors who experience improved visual consistency and timeline navigation UX.
- Business value: Partner logo visual parity improves brand presentation; inline arrow navigation improves discoverability without requiring users to look below the timeline for controls.
- Scope adequacy: File-level scope for the timeline change is raised as Decision Points DP-011 and DP-001.
- Out of scope: Mobile layout, project pages, server/CMS, gallery dialog, and contact block are excluded by documented out-of-scope items in this analysis.
- Constraints: Vanilla-JS-only, no build pipeline; must preserve data-timeline-previous/next and data-timeline-edge attributes in the DOM.
- Success criteria: Adreo partner displays as logo image; timeline arrow controls appear at rail ends; no JS initialization regression; all six static-site tests pass.
- Context adequacy: Request terminology matches context.md; no context conflicts detected beyond those addressed in Context Update Analysis.

---

## Proposed Solution

### High-Level Concept

Replace the Adreo text wordmark in the partners list with a lazy-loaded `<img>` element using `wp-content/uploads/2026/07/adreo.png` across all five BG/EN homepage documents, applying language-appropriate alt text. Relocate the desktop timeline Previous/Next navigation buttons from the below-rail `.at-tl-controls` strip into `at-tl-rail-shell` as absolutely-positioned SVG-chevron arrow overlays at the rail edges — modelling the P&G brands-gallery "fade-wrapper visible" pattern — while preserving all required JS data attributes and adding English aria-labels to EN-localized documents.

### Execution Steps

#### Task 1: Replace Adreo text wordmark with partner image — five homepage documents

- `index.html`: Replace `<span class="at-partner__wordmark">Адрео</span>` with `<img src="wp-content/uploads/2026/07/adreo.png" alt="Адрео" loading="lazy" decoding="async">` inside the existing `<a class="at-partner__link">` anchor element (approximately line 734).
- `index-bg.html`: Same replacement at approximately line 729; use `alt="Адрео"`.
- `index-en.html`: Same replacement at approximately line 729; use `alt="Adreo"` (Latin script, EN file).
- `index.html?lang=bg.html`: Same replacement at approximately line 729; use `alt="Адрео"`.
- `index.html?lang=en.html`: Same replacement at approximately line 729; use `alt="Adreo"` (Latin script, EN file).

#### Task 2: Relocate timeline navigation buttons into at-tl-rail-shell as SVG chevron arrows — six timeline documents

- `index.html`, `index-bg.html`, `index.html?lang=bg.html`: For each BG file, move the `<button data-timeline-previous aria-label="Предишен етап">` element inside `at-tl-rail-shell`, replacing its current inner content (Unicode arrow + text span) with a single inline SVG element containing a left-pointing chevron path; position it at the left edge of the shell. Move the `<button data-timeline-next aria-label="Следващ етап">` element inside `at-tl-rail-shell`, replacing inner content with a right-pointing chevron SVG; position it at the right edge. Remove the `.at-tl-controls` wrapper div from each file.
  - Sub-note: Both `data-timeline-previous` / `data-timeline-next` data attributes and the `aria-label` attribute MUST be preserved on the `<button>` elements; the JS queries for `button[data-timeline-previous][aria-label]` and aborts silently if either attribute is absent.
  - Sub-note: The `at-tl-edge` spans (`data-timeline-edge="previous"` and `data-timeline-edge="next"`) MUST remain in the DOM immediately after relocation; they are required by JS initialization.
- `index-en.html`, `index.html?lang=en.html`, `opit/index.html`: Same structural change as BG files; additionally update `aria-label` to English on both buttons: `aria-label="Previous stage"` and `aria-label="Next stage"`.

#### Task 3: Update shared CSS for arrow positioning, disabled state, and dead-code removal — both main.css copies

- `wp-content/themes/atistat/assets/css/main.css`: Apply all of the following changes; then replicate identically in the byte-identical copy of main.css (path TBD by implement agent from asset directory listing).
  - Change `.at-tl-control:disabled` rule: replace `opacity: .38; cursor: not-allowed;` with `visibility: hidden;` — arrow buttons are fully hidden at timeline boundaries.
  - Add positioning rules for `.at-tl-control` buttons when inside `.at-tl-rail-shell`: `position: absolute; top: 0; bottom: 43px; width: 44px; min-height: 44px; z-index: 8; pointer-events: auto;` — this places them above the existing edge fade spans (z-index 7) and enables click events.
  - Add `left: 0` to the `at-tl-control--previous` rule and `right: 0` to the `at-tl-control--next` rule to anchor each button at its respective edge.
  - Remove the `.at-tl-controls { display: flex; justify-content: center; gap: 12px; margin-top: 14px; }` rule block — the wrapping div is removed from all HTML files.
  - Remove the `.at-partner__wordmark` rule block (lines 748–752) — the class becomes dead code after the Adreo image swap.
  - Ensure the `@media (prefers-reduced-motion: reduce)` block continues to cover `.at-tl-control` (already listed; verify no new classes require addition).

#### Task 4: Update context.md

- `.aib_memory/context.md`: Replace the Solution statement about the partners section to accurately reflect that all three partners — Инженерни Системи, Корект Проект, and Адрео — now display logo images; specify that Адрео uses `wp-content/uploads/2026/07/adreo.png`.
- `.aib_memory/context.md`: Add a Solution statement documenting that the desktop timeline previous/next controls are positioned as inline SVG chevron arrow overlays inside `at-tl-rail-shell` (replacing the separate `.at-tl-controls` strip below the rail); hidden via `visibility:hidden` when at boundary milestones; EN-localized documents carry English aria-labels.

---

## Context Update Analysis

**Context Elements to Add:**

- A new Solution statement documenting that the Adreo partner entry now displays a logo image (`wp-content/uploads/2026/07/adreo.png`) rather than a CSS text wordmark, keeping it consistent with Инженерни Системи and Корект Проект.
- A new Solution statement describing the desktop timeline navigation model after this change: previous/next controls appear as arrow-image buttons integrated at the ends of the timeline rail, replacing the separate below-rail button strip.

**Context Elements to Modify/Remove:**

- Current context.md Solution statement: *"A partners section with Инженерни Системи, Корект Проект, and Адрео logo-links appears between the Services and Why sections on all BG and EN homepage copies."* — This statement already uses the term "logo-links" for all three partners, which will become accurate once Adreo displays an image. The statement itself does not need wording change; a supplementary detail about the image source may be added.
- Current context.md Issues entry referencing the `.at-partner__wordmark` span: there is no explicit Issues entry for the Adreo wordmark, but the `.at-partner__wordmark` CSS rule will become dead code; this may be noted in Issues.
- After this change, the `.at-partner__wordmark` CSS class in `main.css` becomes unused; this may be flagged as a dead-code issue.

**Conflict Resolution & Intent Preservation:**

- The existing partners section Solution statement describes all three partners as "logo-links," which was aspirationally correct but factually inaccurate while Adreo used a text wordmark. Updating Adreo to use an image restores factual accuracy without changing intent: the section is meant to display partner brand identity via logo visuals.
- The existing timeline-related Solution entries describe the continuous viewport, localized controls, and milestone interaction semantics. Adding a description of arrow-integrated controls extends rather than contradicts those entries; the functional behavior of the timeline (tab semantics, keyboard support, panel display) is unchanged.

---

## Decision Register

### Decision Points

#### Decision Point: Adreo image scope (DP-001)

- **Tag:** resolved-by-user
- **Resolution:** Option A — Apply to all five BG/EN documents including `index.html` (primary Bulgarian). Chosen alternative: `index.html`, `index-bg.html`, `index-en.html`, `index.html?lang=bg.html`, `index.html?lang=en.html` all receive the Adreo image replacement.

#### Decision Point: Adreo alt text (DP-002)

- **Tag:** resolved-by-user
- **Resolution:** Option C — Language-appropriate alt text. BG files (`index.html`, `index-bg.html`, `index.html?lang=bg.html`) use `alt="Адрео"`; EN files (`index-en.html`, `index.html?lang=en.html`) use `alt="Adreo"`. Aligns with the existing language-differentiated `aria-label` pattern on the Adreo link element.

#### Decision Point: Arrow DOM position (DP-003)

- **Tag:** resolved-by-user
- **Resolution:** Option A — Arrow buttons placed inside `at-tl-rail-shell` as absolutely-positioned overlays at left/right edges, on top of the existing gradient fade spans. `pointer-events: auto` and `z-index: 8` required on each button. Consistent with the P&G fade-wrapper pattern (DP-008 resolution).

#### Decision Point: Arrow visual format (DP-004)

- **Tag:** resolved-by-user
- **Resolution:** Option A — SVG inline in HTML. A small inline SVG path is embedded inside each `<button>` element, duplicated across all six timeline documents. Scalable, no extra HTTP request, styleable via CSS.

#### Decision Point: Arrow visual style (DP-005)

- **Tag:** resolved-by-user
- **Resolution:** Option A — Simple chevron (‹ ›). Thin, minimal directional lines rendered as an inline SVG path. Lightweight and consistent with modern carousel/timeline patterns.

#### Decision Point: Arrow text labels (DP-006)

- **Tag:** resolved-by-user
- **Resolution:** Option A — Icon-only. Visible text labels removed from the button markup; `aria-label` attribute retained on each `<button>` element as the sole accessible name. The SVG chevron provides the visual affordance.

#### Decision Point: Edge fades fate (DP-007)

- **Tag:** resolved-by-user
- **Resolution:** Option A — Keep edge fades unchanged. Arrow buttons are placed on top of the gradient fades at z-index 8. The gradient fades continue to signal "more content in this direction"; the overlaid arrows serve as the interactive navigation affordance. Consistent with the P&G fade-wrapper visible pattern (DP-008).

#### Decision Point: at-tl-controls removal (DP-008)

- **Tag:** resolved-by-user
- **Resolution:** Other — Model after the P&G brands-gallery "fade-wrapper visible" pattern (`us.pg.com`). Remove the `.at-tl-controls` wrapper div entirely from all six timeline documents; arrow buttons move inside `at-tl-rail-shell`, which becomes the functional equivalent of the P&G fade-wrapper (gradient fades + visible arrow overlays in a single container). The `.at-tl-controls` CSS block and its `margin-top: 14px` rule are removed from `main.css`. The `.at-tl-rail-shell` structure already supplies the fade-wrapper role via the `.at-tl-edge` spans.

#### Decision Point: CSS placement (DP-009)

- **Tag:** resolved-by-user
- **Resolution:** Option A — Add all new CSS rules to `wp-content/themes/atistat/assets/css/main.css` (shared stylesheet). Both the canonical file and its byte-identical copy must be updated. No document-local style blocks.

#### Decision Point: EN aria-labels (DP-010)

- **Tag:** resolved-by-user
- **Resolution:** Option A — Update EN documents to English aria-labels. `index-en.html`, `index.html?lang=en.html`, and `opit/index.html` (in scope per DP-011) will use `aria-label="Previous stage"` and `aria-label="Next stage"` on their respective arrow buttons.

#### Decision Point: Timeline scope (DP-011)

- **Tag:** resolved-by-user
- **Resolution:** Option A — Apply to all six timeline documents: `index.html`, `index-bg.html`, `index-en.html`, `index.html?lang=bg.html`, `index.html?lang=en.html`, and `opit/index.html`. Maintains timeline markup parity and prevents static-site test failures.

#### Decision Point: Arrow disabled state (DP-012)

- **Tag:** resolved-by-user
- **Resolution:** Option B — Fully hidden when disabled. `.at-tl-control:disabled` uses `visibility: hidden` so the arrow button occupies no visual space at boundary milestones. The absolute positioning means no layout shift occurs. JS behavior (setting `disabled` attribute) is unchanged.

#### Decision Point: Arrow scroll-area constraint (DP-013)

- **Tag:** resolved-by-user
- **Resolution:** Option A — Absolute overlay on `at-tl-rail-shell`. The shell is a non-scrolling container; its absolutely-positioned children remain stationary as the inner `at-tl-buildings` rail scrolls. Aligns with DP-003 and DP-008 resolutions. Confirm or add `position: relative` on `.at-tl-rail-shell` in main.css if not already present.
