## Goal

Replace the Аdreo text wordmark in the partners section with a logo image across all five BG/EN homepage documents, and redesign the desktop timeline navigation from a below-rail labeled-button strip to inline SVG chevron arrow controls overlaid inside the timeline rail shell, following the P&G fade-wrapper visible pattern. Impacted components: five homepage HTML files (partners section), six timeline HTML files (navigation buttons), shared main.css stylesheet and its byte-identical copy, and context.md.

## Constraints

- Vanilla-JS-only constraint: no build pipeline, no third-party libraries may be introduced.
- JavaScript bindings in main.js require `<button data-timeline-previous aria-label="...">` and `<button data-timeline-next aria-label="...">` to exist in the DOM with those exact attributes; `data-timeline-edge="previous"` and `data-timeline-edge="next"` spans must also remain in the DOM untouched.
- The byte-identical CSS copy of main.css must mirror every CSS change applied to the canonical file to preserve asset-identity test assertions.
- Mobile stacked-card layout (`.at-tlcard` elements) is out of scope; no mobile markup changes.
- No changes to project detail pages under `proekti/`, to `wp-json/`, to `robots.txt`, or to any gallery dialog, FAQ, or hero section.

## Success criteria

- Аdreo partner entry in all five BG/EN homepage documents (`index.html`, `index-bg.html`, `index-en.html`, `index.html?lang=bg.html`, `index.html?lang=en.html`) displays an `<img src="wp-content/uploads/2026/07/adreo.png">` element with non-empty alt text.
- No `<span class="at-partner__wordmark">` element remains in any of those five files.
- The `.at-tl-controls` div is absent from all six timeline documents (`index.html`, `index-bg.html`, `index-en.html`, `index.html?lang=bg.html`, `index.html?lang=en.html`, `opit/index.html`).
- SVG chevron `<button>` elements with `data-timeline-previous` and `data-timeline-next` attributes and a non-empty `aria-label` exist inside `at-tl-rail-shell` in all six timeline documents.
- EN-localized timeline documents (`index-en.html`, `index.html?lang=en.html`, `opit/index.html`) carry `aria-label="Previous stage"` and `aria-label="Next stage"`.
- `.at-tl-control:disabled` CSS rule uses `visibility: hidden` in both main.css copies.
- `.at-partner__wordmark` CSS rule block is absent from both main.css copies.
- `.at-tl-controls { display: flex; ... }` CSS rule block is absent from both main.css copies.
- `tests/test_static_site.py` passes without regression.

## Plan

### Task 1: Replace Аdreo text wordmark with partner image — five homepage HTML files

#### Intent
Swap the `<span class="at-partner__wordmark">Адрео</span>` element for a lazy-loaded `<img>` element in the Аdreo partner `<li>` across five BG/EN homepage documents.

#### Procedure

In `index.html` (approximately line 734): locate the `<a class="at-partner__link" href="https://www.adreo.bg/" ...>` anchor element containing `<span class="at-partner__wordmark">Адрео</span>`; replace the span element with `<img src="wp-content/uploads/2026/07/adreo.png" alt="Адрео" loading="lazy" decoding="async">`.

In `index-bg.html` (approximately line 729): apply the identical replacement using `alt="Адрео"`.

In `index-en.html` (approximately line 729): apply the same replacement using `alt="Adreo"` (Latin script, EN document).

In `index.html?lang=bg.html` (approximately line 729): apply the replacement using `alt="Адрео"`.

In `index.html?lang=en.html` (approximately line 729): apply the replacement using `alt="Adreo"` (Latin script, EN document).

### Task 2: Relocate timeline navigation buttons into at-tl-rail-shell as SVG chevron arrows — six timeline HTML files

#### Intent
Remove the `.at-tl-controls` wrapper div and its two text-labeled child buttons from below the timeline; add two SVG-chevron `<button>` elements with identical data attributes inside `at-tl-rail-shell`, immediately after the closing `</div>` of `at-tl-buildings`.

#### Procedure

In `index.html`: inside the `at-tl-rail-shell` div, immediately before its closing `</div>`, insert two button elements. The first is `<button type="button" class="at-tl-control at-tl-control--previous" data-timeline-previous aria-label="Предишен етап">` with inner content consisting of an inline SVG element with `viewBox="0 0 10 18"`, `aria-hidden="true"`, and a single polyline element with `points="9,1 1,9 9,17"`, `stroke="currentColor"`, `fill="none"`, `stroke-width="2"`, `stroke-linecap="round"`, `stroke-linejoin="round"`. The second is `<button type="button" class="at-tl-control at-tl-control--next" data-timeline-next aria-label="Следващ етап">` with a right-pointing chevron polyline using `points="1,1 9,9 1,17"`. Remove the entire `<div class="at-tl-controls" aria-label="Навигация по времевата линия">` block (including its two original child buttons) that appears after the `at-tl-stage` closing div.

In `index-bg.html`: apply the identical structural change (BG aria-labels retained: `"Предишен етап"` / `"Следващ етап"`).

In `index-en.html`: apply the same structural change; use English aria-labels: `aria-label="Previous stage"` on the previous button and `aria-label="Next stage"` on the next button.

In `index.html?lang=bg.html`: apply the identical structural change (BG aria-labels).

In `index.html?lang=en.html`: apply the same structural change using English aria-labels: `aria-label="Previous stage"` / `aria-label="Next stage"`.

In `opit/index.html`: apply the same structural change using English aria-labels (document serves English-language content).

### Task 3: Update shared CSS stylesheet — main.css and byte-identical copy

#### Intent
Add absolute-overlay positioning rules for arrow buttons inside at-tl-rail-shell; change the disabled-state rule to visibility:hidden; remove the dead `.at-tl-controls` and `.at-partner__wordmark` rule blocks. Apply all changes to both CSS copies.

#### Procedure

In `wp-content/themes/atistat/assets/css/main.css`: locate the `.at-tl-control:disabled { opacity: .38; cursor: not-allowed; }` rule and replace it with `.at-tl-control:disabled { visibility: hidden; }`.

In `wp-content/themes/atistat/assets/css/main.css`: locate the `.at-tl-control { ... }` rule block and add `position: absolute; z-index: 8; pointer-events: auto;` to its declarations, establishing overlay positioning above the edge fades (z-index 7). Add a new rule `.at-tl-control--previous { left: 0; }` and a new rule `.at-tl-control--next { right: 0; }` immediately after the `.at-tl-control` block to anchor each button at its respective edge.

In `wp-content/themes/atistat/assets/css/main.css`: confirm that the `.at-tl-rail-shell` rule includes `position: relative`; if the property is absent, add it to the rule block so that the absolutely-positioned arrow buttons are contained within the shell.

In `wp-content/themes/atistat/assets/css/main.css`: locate the `.at-tl-controls { display: flex; justify-content: center; gap: 12px; margin-top: 14px; }` rule block and delete it entirely.

In `wp-content/themes/atistat/assets/css/main.css`: locate the `/* Adreo text partner: styled wordmark in lieu of a logo image */` comment and the `.at-partner__wordmark { font-size: 1.4rem; font-weight: 700; color: var(--ink); }` rule immediately following it; delete both the comment and the rule block.

Locate the second (byte-identical) CSS file in `wp-content/themes/atistat/assets/css/` and apply all five of the above changes identically so both copies remain byte-identical.

### Task 4: Automated tests verification

#### Intent
Confirm no static-site assertions are broken by the HTML and CSS changes across the affected documents.

#### Procedure

Run `python -m pytest tests/test_static_site.py -v` from the workspace root. Expected result: all tests pass with no failures.

If a test fails, inspect the failure message to identify which document and which structural assertion failed (e.g., missing `data-timeline-previous` attribute, parity mismatch between timeline documents, or unexpected span/div presence), then correct the affected HTML file before re-running.

### Task 5: Update context.md

#### Intent
Insert an Аdreo partner image statement and replace the outdated timeline navigation statement in the Solution section. Confirm no new Issues entries are required from the AI Agent critique.

#### Procedure

Run the following command to add a Solution statement for the Аdreo partner image:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Adreo partner logo-link uses wp-content/uploads/2026/07/adreo.png; BG-localized documents carry alt='Адрео', EN-localized documents carry alt='Adreo'."`

Run the following command to delete the outdated timeline navigation statement (exact text match required):
`python .aib_brain/tools/edit-context.py --workspace . --operation delete --area Solution --text "Across index.html, index-bg.html, index-en.html, index.html?lang=bg.html, index.html?lang=en.html, and opit/index.html, the thirteen-milestone desktop timeline uses a continuous viewport showing about five complete milestones, localized Previous/Next controls with disabled boundary states, subtle edge fades, free touch/trackpad panning, snap alignment, active-tab centering, and complete automatic-activation tab semantics; the stacked mobile cards remain in use."`

Run the following command to insert the updated timeline navigation statement:
`python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Across index.html, index-bg.html, index-en.html, index.html?lang=bg.html, index.html?lang=en.html, and opit/index.html, the thirteen-milestone desktop timeline uses a continuous viewport showing about five complete milestones, inline SVG-chevron Previous/Next arrow controls absolutely positioned inside at-tl-rail-shell at the left and right edges (z-index 8, above gradient edge fades), hidden via visibility:hidden at boundary milestones; EN-localized documents carry English aria-labels ('Previous stage'/'Next stage'); subtle edge fades, free touch/trackpad panning, snap alignment, active-tab centering, and complete automatic-activation tab semantics; the stacked mobile cards remain in use."`

AI Agent critique findings cross-check: all critique findings from analysis-R-20260726-0108.md are resolved by Tasks 1–3 (`.at-partner__wordmark` dead code removed, `.at-tl-controls` CSS removed, EN aria-label mismatch corrected) or are pre-existing issues already registered in context.md Issues. No additional `edit-context.py --area Issues` invocations are required.
